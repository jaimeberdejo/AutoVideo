---
phase: 10-contenido-page
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_bullets_gen.py
autonomous: true
requirements: [CNT-01, CNT-02, CNT-03]
must_haves:
  truths:
    - "RED tests exist for generate_bullets() with mocked call_structured"
    - "RED tests exist for bullets.yaml serialization round-trip matching BulletsInput"
    - "RED tests exist for duration validation bounds (min 15 s, max 1800 s)"
    - "All 350 existing tests continue to pass; new tests fail only with ImportError or AttributeError until Plan 02"
  artifacts:
    - path: "tests/test_bullets_gen.py"
      provides: "Unit test scaffolding for generate_bullets + serialization + validation"
      min_lines: 60
  key_links:
    - from: "tests/test_bullets_gen.py"
      to: "src/avideo/stages/bullets_gen.py"
      via: "deferred import inside test body (same pattern as test_storyboard.py)"
      pattern: "from avideo.stages.bullets_gen import generate_bullets"
    - from: "tests/test_bullets_gen.py"
      to: "avideo.models.bullets.BulletsInput"
      via: "round-trip: BulletsInput -> model_dump -> yaml.safe_dump -> load_bullets"
      pattern: "load_bullets"
---

<objective>
Wave 0: Write RED unit tests for Phase 10's testable pure logic before any implementation exists.

Purpose: Define the exact contracts that generate_bullets(), duration validation, and bullets.yaml serialization must satisfy. Tests fail until Plan 02 implements bullets_gen.py. This ensures implementation matches the spec.

Output: tests/test_bullets_gen.py with 3 test classes covering: (1) generate_bullets with mocked anthropic, (2) bullets.yaml serialization round-trip, (3) duration validation bounds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-contenido-page/10-CONTEXT.md

<!-- Key contracts this Wave 0 tests against -->
<interfaces>
<!-- BulletsInput — src/avideo/models/bullets.py -->
```python
class BulletsInput(BaseModel):
    title: str
    bullets: list[str]
```

<!-- load_bullets — src/avideo/utils/bullets.py -->
```python
def load_bullets(path: Path) -> BulletsInput:
    raw = yaml.safe_load(path.read_text(encoding="utf-8"))
    return BulletsInput.model_validate(raw)
```

<!-- call_structured — src/avideo/integrations/anthropic.py -->
```python
def call_structured(
    *,
    system: str,
    user: str,
    tool_name: str,
    tool_description: str,
    output_model: type[T],
    max_tokens: int = 8192,
) -> T: ...
```

<!-- Mock seam pattern (from test_storyboard.py) -->
# Patch at the import site inside the stage module:
# mocker.patch("avideo.stages.bullets_gen.call_structured", return_value=mock_result)
</interfaces>
</context>

<feature>
  <name>generate_bullets unit tests (RED until Plan 02)</name>
  <files>tests/test_bullets_gen.py</files>
  <behavior>
    <!-- generate_bullets contract to test against -->
    - generate_bullets("topic", 120, n=4) calls call_structured once; returns list of n strings
    - generate_bullets with mocked call_structured does NOT make any real network call
    - bullets count: n defaults to max(2, duration_seconds // 30); min 2, max 20 clamp
    - generate_bullets returns list[str], each element non-empty
    - Duration validation: values in [15, 1800] pass; 14 and 1801 raise ValueError
    - bullets.yaml round-trip: BulletsInput(title="T", bullets=["A","B"]) serialized via
      yaml.safe_dump(model.model_dump()) then loaded back via load_bullets() is equal
  </behavior>
  <implementation>
    Write all tests with deferred imports inside test body (same pattern as test_storyboard.py
    / test_bridge.py) so the file COLLECTS even before bullets_gen.py exists — it fails at
    import-inside-test time, which is an expected RED, not a collection error.
    Use pytest-mock's mocker.patch at the import site "avideo.stages.bullets_gen.call_structured".
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED test file tests/test_bullets_gen.py</name>
  <files>tests/test_bullets_gen.py</files>
  <action>
Create tests/test_bullets_gen.py with the following three test classes. All imports of
avideo.stages.bullets_gen must be INSIDE test bodies (deferred, same as test_storyboard.py)
so the file collects without error before the module exists.

--- CLASS 1: TestGenerateBullets ---
Mock seam: mocker.patch("avideo.stages.bullets_gen.call_structured", return_value=<BulletsListOutput>)

Where BulletsListOutput is a Pydantic model (defined in bullets_gen.py) with field `bullets: list[str]`.
In the test, construct the mock return value as a simple object with `.bullets = ["A", "B", "C", "D"]`.

Tests:
1. test_generate_bullets_calls_call_structured_once:
   - Call generate_bullets("My topic", duration_seconds=120, n=4)
   - Assert mock_call_structured.call_count == 1
   - Assert returned list == ["A", "B", "C", "D"]

2. test_generate_bullets_returns_list_of_strings:
   - Mock returns object with .bullets = ["X", "Y"]
   - Assert isinstance(result, list) and all(isinstance(b, str) for b in result)

3. test_generate_bullets_default_n_from_duration:
   - generate_bullets("topic", duration_seconds=60) (no explicit n)
   - Check that the user prompt passed to call_structured contains "60" (the duration)
   - The mock returns .bullets with the expected default count — just assert list is returned

4. test_generate_bullets_no_real_network_call:
   - With mock in place, assert result is returned without raising (network never called)

--- CLASS 2: TestBulletsYamlRoundTrip ---
No mocking needed — pure data transformation test.

Tests:
1. test_bullets_yaml_serialization_round_trip:
   - Build BulletsInput(title="Test Title", bullets=["Bullet A", "Bullet B", "Bullet C"])
   - Serialize: yaml.safe_dump(bi.model_dump()) → write to tmp_path / "bullets.yaml"
   - Load back: load_bullets(tmp_path / "bullets.yaml")
   - Assert loaded.title == "Test Title" and loaded.bullets == ["Bullet A", "Bullet B", "Bullet C"]

2. test_bullets_yaml_format_matches_engine_input:
   - Use the minimal_bullets fixture (from conftest: title="Test", bullets=["Point 1", "Point 2"])
   - Assert load_bullets(minimal_bullets).title == "Test"
   - Assert load_bullets(minimal_bullets).bullets == ["Point 1", "Point 2"]
   - This confirms the YAML format the page will produce is identical to what avideo generate --bullets consumes

--- CLASS 3: TestDurationValidation ---
Test the pure validation helper `validate_duration(seconds: int) -> int` from bullets_gen.py.
Deferred import: from avideo.stages.bullets_gen import validate_duration

Tests:
1. test_validate_duration_minimum_boundary: validate_duration(15) == 15 (no raise)
2. test_validate_duration_maximum_boundary: validate_duration(1800) == 1800 (no raise)
3. test_validate_duration_below_min_raises: validate_duration(14) raises ValueError
4. test_validate_duration_above_max_raises: validate_duration(1801) raises ValueError
5. test_validate_duration_typical_value: validate_duration(120) == 120

Run after writing: uv run pytest tests/test_bullets_gen.py -x 2>&1 | head -30
Expected: file COLLECTS but tests FAIL with ImportError/ModuleNotFoundError (bullets_gen not yet created).
The 350 existing tests must not be affected — confirm with: uv run pytest --ignore=tests/test_bullets_gen.py -q 2>&1 | tail -5
  </action>
  <verify>
    <automated>uv run pytest tests/test_bullets_gen.py --collect-only -q 2>&1 | grep -c "test_"</automated>
    <!-- Must collect >= 10 test functions (they will be RED — that is correct for Wave 0) -->
    <!-- Baseline regression: -->
    <automated>uv run pytest --ignore=tests/test_bullets_gen.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>
    - tests/test_bullets_gen.py exists with >= 10 test functions across 3 classes
    - pytest --collect-only reports >= 10 items from this file without collection error
    - uv run pytest --ignore=tests/test_bullets_gen.py passes with 350 tests (baseline unbroken)
    - Individual test run of tests/test_bullets_gen.py shows ImportError/ModuleNotFoundError (correct RED state)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → module | Deferred imports; no real network, no real files beyond tmp_path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Information Disclosure | test_bullets_gen.py | accept | Test file contains no secrets; mock return values are synthetic strings; no API keys exercised |
</threat_model>

<verification>
1. `uv run pytest tests/test_bullets_gen.py --collect-only -q` — no collection errors, >= 10 items listed
2. `uv run pytest --ignore=tests/test_bullets_gen.py -q` — 350 passed (baseline green)
3. `uv run pytest tests/test_bullets_gen.py -x` — fails with ImportError on `avideo.stages.bullets_gen` (correct RED)
</verification>

<success_criteria>
Wave 0 complete when:
- tests/test_bullets_gen.py has >= 10 test functions, collects cleanly
- 350 baseline tests still green
- New tests are correctly RED (fail on missing module, not on syntax/collection errors)
</success_criteria>

<output>
After completion, create `.planning/phases/10-contenido-page/10-01-SUMMARY.md`
</output>
