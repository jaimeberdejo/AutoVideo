---
phase: 04-voz-subtitulos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/avideo/models/timings.py
  - src/avideo/models/__init__.py
  - src/avideo/models/config.py
  - src/avideo/integrations/elevenlabs.py
  - src/avideo/stages/voice_elevenlabs.py
  - src/avideo/stages/voice.py
  - tests/conftest.py
  - tests/test_subtitles.py
  - tests/test_elevenlabs.py
  - tests/test_voice_elevenlabs.py
  - tests/test_voice_record.py
  - tests/test_align.py
autonomous: true
requirements: [VOICE-01, VOICE-02, ALIGN-02]
user_setup:
  - service: elevenlabs
    why: "TTS con timestamps por carácter (modo --voice elevenlabs, el por defecto)"
    env_vars:
      - name: ELEVENLABS_API_KEY
        source: "ElevenLabs Dashboard -> Profile -> API Key"

must_haves:
  truths:
    - "En modo elevenlabs el sistema genera un workdir/audio/slide_XX.mp3 por slide"
    - "Los timestamps por carácter se validan como estrictamente crecientes antes del checkpoint"
    - "Si los timestamps no son crecientes, la síntesis reintenta hasta 3 veces y luego falla con error claro"
    - "La voz produce SIEMPRE un UnifiedTimings (consumido por align y subtitles)"
    - "Importar integrations/elevenlabs.py no requiere ELEVENLABS_API_KEY (cliente lazy)"
  artifacts:
    - path: "src/avideo/models/timings.py"
      provides: "Modelo Pydantic UnifiedTimings (WordTiming, SlideTimings) compartido por ambos backends"
      contains: "class UnifiedTimings"
    - path: "src/avideo/integrations/elevenlabs.py"
      provides: "Cliente lazy + synthesize_slide (convert_with_timestamps + validación seconds-crecientes + retry<=3)"
      contains: "character_start_times_seconds"
    - path: "src/avideo/stages/voice_elevenlabs.py"
      provides: "Etapa de voz ElevenLabs por-slide → mp3 + UnifiedTimings"
      contains: "synthesize_slide"
    - path: "src/avideo/stages/voice.py"
      provides: "VoiceStage despachador (stage_name=voice) que selecciona backend por config.voice"
      contains: 'stage_name: str = "voice"'
  key_links:
    - from: "src/avideo/stages/voice_elevenlabs.py"
      to: "src/avideo/integrations/elevenlabs.py"
      via: "import a scope de módulo de synthesize_slide (mock point)"
      pattern: "from avideo.integrations.elevenlabs import"
    - from: "src/avideo/integrations/elevenlabs.py"
      to: "ElevenLabs SDK convert_with_timestamps"
      via: "resp.alignment.character_start_times_seconds"
      pattern: "character_start_times_seconds"
---

<objective>
Implementa el camino de voz por defecto (`--voice elevenlabs`) y la fundación que todo Phase 4 consume: el modelo Pydantic unificado de timings (`UnifiedTimings`), la integración ElevenLabs (cliente lazy + `convert_with_timestamps` + validación estrictamente-creciente + retry≤3), la etapa `voice_elevenlabs`, y el despachador `VoiceStage` que selecciona el backend por `config.voice`. También crea el scaffolding de tests de Wave 0 para TODA la fase y añade las dependencias.

Purpose: Es la base del DAG de Phase 4. `UnifiedTimings` es el contrato que `align` y `subtitles` (planes 04-02, 04-03) consumen; sin él no se pueden construir. El modo `elevenlabs` es el camino por defecto y entrega VOICE-01/VOICE-02 (audio por slide + timestamps validados) y prepara ALIGN-02 (la voz produce los timings directamente, sin alineación posterior).

Output: `models/timings.py`, `integrations/elevenlabs.py`, `stages/voice_elevenlabs.py`, `stages/voice.py`, campos nuevos en `RunConfig`, `elevenlabs` en pyproject + extra `record` declarado, y todos los archivos de test vacíos/scaffold de Wave 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voz-subtitulos/04-CONTEXT.md
@.planning/phases/04-voz-subtitulos/04-RESEARCH.md
@.planning/phases/04-voz-subtitulos/04-VALIDATION.md
@CLAUDE.md

# Patrón a replicar exactamente (cliente lazy + mock point a scope de módulo):
@src/avideo/integrations/anthropic.py
@src/avideo/stages/storyboard.py

# Contratos de entrada (Phase 2) y de salida a ampliar:
@src/avideo/models/script.py
@src/avideo/models/timing.py
@src/avideo/models/voice.py
@src/avideo/models/config.py
@src/avideo/models/__init__.py

# Contrato de etapa y filesystem:
@src/avideo/stages/base.py
@src/avideo/utils/workdir.py

# Patrón de tests (mock a scope de módulo, fixtures):
@tests/conftest.py
@tests/test_storyboard.py

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase — no explorar. -->

StageProtocol (src/avideo/stages/base.py):
```python
class CheckpointMixin:
    stage_name: str = ""
    @property
    def checkpoint_name(self) -> str: ...   # defaults to stage_name
    def is_done(self, workdir) -> bool: ...
    # Subclase implementa: def run(self, workdir, config) -> BaseModel
```

WorkdirManager (src/avideo/utils/workdir.py):
```python
self.root: Path                       # workdir root; subdir audio/ ya creado
def checkpoint_path(self, name: str) -> Path        # <root>/<name>.json
def write_checkpoint(self, name, model) -> None     # atómico; lo llama el orquestador, NO la etapa
def read_checkpoint(self, name, model_class) -> BaseModel
```
Nota: La etapa NO escribe checkpoints — el orquestador llama write_checkpoint + mark_done tras run().

Entradas de Phase 2 (leídas por la etapa de voz vía workdir.read_checkpoint):
```python
# script.json -> ScriptOutput
class SlideScript(BaseModel): slide_index: int; narration: str
class ScriptOutput(BaseModel): slides: list[SlideScript]; language: str = "es"
# timings.json -> TimingOutput
class SlideTiming(BaseModel): slide_index: int; seconds: float; word_budget: int
class TimingOutput(BaseModel): slides: list[SlideTiming]; total_seconds: float; wpm: int = 150
```

RunConfig (src/avideo/models/config.py) — ya expone:
```python
voice: VoiceMode = VoiceMode.elevenlabs   # enum {elevenlabs, record}
voice_id: str = "21m00Tcm4TlvDq8ikWAM"
language: str = "es"
burn_subs: bool = False
# env_prefix="AVIDEO_", yaml_file="config.yaml"
```

Patrón mock-point (de anthropic.py / storyboard.py): la etapa importa la función de
integración a scope de módulo (`from avideo.integrations.elevenlabs import synthesize_slide`)
para que los tests parcheen `avideo.stages.voice_elevenlabs.synthesize_slide`.

ElevenLabs SDK 2.x (VERIFIED en 04-RESEARCH.md contra el source del SDK):
```python
from elevenlabs import ElevenLabs
client = ElevenLabs()    # lee ELEVENLABS_API_KEY del entorno
resp = client.text_to_speech.convert_with_timestamps(
    voice_id=..., text=..., model_id="eleven_multilingual_v2",
    output_format="mp3_44100_128",
)
resp.audio_base64                                   # str base64 -> base64.b64decode -> mp3 bytes
resp.alignment.character_start_times_seconds        # list[float] EN SEGUNDOS (NO _ms)
resp.alignment.character_end_times_seconds          # list[float] EN SEGUNDOS
resp.alignment.characters                           # list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencias + scaffolding de tests Wave 0</name>
  <read_first>pyproject.toml, tests/conftest.py, .planning/phases/04-voz-subtitulos/04-VALIDATION.md (sección "Wave 0 Requirements")</read_first>
  <files>pyproject.toml, tests/conftest.py, tests/test_subtitles.py, tests/test_elevenlabs.py, tests/test_voice_elevenlabs.py, tests/test_voice_record.py, tests/test_align.py</files>
  <action>
1. `uv add "elevenlabs>=2.49.0"` (modo elevenlabs por defecto; ligero, sin torch).
2. Declara el modo `record` como extra opcional en pyproject.toml (no lo añadas a `dependencies`):
   ```toml
   [project.optional-dependencies]
   record = [
       "sounddevice>=0.5.5",
       "soundfile>=0.13.1",
       "whisperx>=3.8.5",
       "torch==2.5.1",
       "torchaudio==2.5.1",
   ]
   ```
   Documenta en un comentario que torch se pinea a 2.5.1 por el pitfall weights_only de WhisperX VAD (torch>=2.6 rompe la carga de pyannote VAD; ver 04-RESEARCH.md Pitfall 2). NO ejecutes `uv add` del extra record en este task (evita arrastrar ~2GB de torch en CI/el camino por defecto); basta con declararlo.
3. Crea los CINCO archivos de test como scaffold de Wave 0 (uno por área de la VALIDATION.md). Cada archivo: docstring de módulo describiendo qué requisito cubre, y al menos un test placeholder marcado `@pytest.mark.skip(reason="Wave 0 scaffold — implementado en task posterior")` para que la colección de pytest no falle. Los tasks/planes posteriores rellenan estos archivos con tests reales:
   - tests/test_subtitles.py → SUB-01/SUB-02 (lo rellena 04-03)
   - tests/test_elevenlabs.py → VOICE-02 (lo rellena Task 3 de este plan)
   - tests/test_voice_elevenlabs.py → VOICE-01 (lo rellena Task 4 de este plan)
   - tests/test_voice_record.py → VOICE-03 (lo rellena 04-02)
   - tests/test_align.py → ALIGN-01/ALIGN-02 (lo rellena 04-02)
4. Añade fixtures a tests/conftest.py (lazy imports donde haga falta, igual que las fixtures Phase 2):
   - `fake_elevenlabs_response`: objeto con atributos `.audio_base64` (un base64 válido de unos pocos bytes) y `.alignment` con `.character_start_times_seconds` (lista estrictamente creciente, p.ej. [0.0, 0.1, 0.25]), `.character_end_times_seconds`, `.characters`. Usa `types.SimpleNamespace` o una clase mínima.
   - `fake_word_segments`: lista de dicts `[{"word": "hola", "start": 0.0, "end": 0.4}, {"word": "mundo", "start": 0.5, "end": 0.9}]` (para whisperx en 04-02).
   - `voice_config`: helper/fixture que construye un RunConfig mínimo en tmp_path (mira `_build_config` en test_storyboard.py) — reutilizable por los tests de voz.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "elevenlabs" pyproject.toml` (dependencia añadida)
    - `grep -q "optional-dependencies" pyproject.toml && grep -q "torch==2.5.1" pyproject.toml` (extra record con torch pineado)
    - `ls tests/test_subtitles.py tests/test_elevenlabs.py tests/test_voice_elevenlabs.py tests/test_voice_record.py tests/test_align.py` (5 archivos existen)
    - `grep -q "fake_elevenlabs_response" tests/conftest.py && grep -q "fake_word_segments" tests/conftest.py`
    - `uv run pytest -q` colecciona sin errores (los placeholders están skipped, no failed)
  </acceptance_criteria>
  <done>pytest colecciona y pasa la suite completa (con los nuevos archivos en skip); elevenlabs en deps; extra record declarado con torch 2.5.1 pineado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelo UnifiedTimings + RunConfig (voice_id default + whisperx_model)</name>
  <read_first>src/avideo/models/timing.py, src/avideo/models/voice.py, src/avideo/models/config.py, src/avideo/models/__init__.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Pattern 4 + Open Question 1)</read_first>
  <files>src/avideo/models/timings.py, src/avideo/models/__init__.py, src/avideo/models/config.py</files>
  <behavior>
    - UnifiedTimings se construye y serializa/deserializa con model_dump_json/model_validate_json (round-trip).
    - SlideTimings.words puede estar vacío (elevenlabs puede no segmentar a palabra) o lleno.
    - RunConfig acepta whisperx_model con default "small" y lo sobreescribe vía init kwarg.
    - RunConfig sigue cargando con los campos existentes (no rompe tests Phase 1/2).
  </behavior>
  <action>
1. Crea `src/avideo/models/timings.py` con el modelo unificado (D-11). Forma recomendada (Claude's discretion sobre detalles finos, sigue 04-RESEARCH.md Pattern 4):
   ```python
   from __future__ import annotations
   from pydantic import BaseModel

   class WordTiming(BaseModel):
       text: str
       start: float   # SEGUNDOS, RELATIVOS al inicio del clip del slide
       end: float

   class SlideTimings(BaseModel):
       slide_index: int
       audio_path: str            # "audio/slide_00.mp3" | "audio/slide_00.wav"
       duration: float            # segundos (último character_end_times_seconds o ffprobe en Phase 5)
       words: list[WordTiming] = []

   class UnifiedTimings(BaseModel):
       source: str                # "elevenlabs" | "whisperx"
       slides: list[SlideTimings]
   ```
   DOCUMENTA en el docstring del módulo y de WordTiming que start/end son RELATIVOS por slide; el offset global se acumula en subtitles.py (offset = Σ duraciones de slides previos). Esto resuelve Open Question 1 de RESEARCH y es la decisión que Phase 5 consume.
2. Re-exporta en `src/avideo/models/__init__.py`: importa `UnifiedTimings, SlideTimings, WordTiming` desde `.timings` y añádelos a `__all__`.
3. En `src/avideo/models/config.py`: añade `whisperx_model: str = "small"` (configurable vía AVIDEO_ y config.yaml; D-05/Open Question 5). `voice_id` ya existe — añade un comentario indicando que el default es un placeholder (Rachel) movible a config.yaml. No cambies el orden de fuentes ni el env_prefix.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run python -c "from avideo.models import UnifiedTimings, SlideTimings, WordTiming, RunConfig; from pathlib import Path; u=UnifiedTimings(source='elevenlabs', slides=[SlideTimings(slide_index=0, audio_path='audio/slide_00.mp3', duration=1.0, words=[WordTiming(text='hi', start=0.0, end=0.5)])]); assert UnifiedTimings.model_validate_json(u.model_dump_json()).slides[0].words[0].text=='hi'; c=RunConfig(bullets=Path('b.yaml'), duration=60, whisperx_model='large-v3'); assert c.whisperx_model=='large-v3'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class UnifiedTimings" src/avideo/models/timings.py`
    - `grep -q "UnifiedTimings" src/avideo/models/__init__.py`
    - `grep -v '^#' src/avideo/models/config.py | grep -q "whisperx_model"`
    - El comando -c imprime OK (round-trip + whisperx_model override)
    - `uv run pytest -q` sigue verde (no rompe Phase 1/2)
  </acceptance_criteria>
  <done>UnifiedTimings/SlideTimings/WordTiming existen, re-exportados, round-trip JSON verificado; whisperx_model en RunConfig con default "small"; documentado timestamps relativos por slide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: integrations/elevenlabs.py (synthesize_slide + validación creciente + retry≤3)</name>
  <read_first>src/avideo/integrations/anthropic.py, src/avideo/models/timings.py, tests/conftest.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 1, Pitfall 3, Security Domain)</read_first>
  <files>src/avideo/integrations/elevenlabs.py, tests/test_elevenlabs.py</files>
  <behavior>
    - is_strictly_increasing([0.0, 0.1, 0.2]) → True; ([0.0, 0.1, 0.1]) → False; ([0.2, 0.1]) → False; ([]) y ([0.0]) → True (vacío/un elemento no degeneran).
    - synthesize_slide con un mock que devuelve timestamps crecientes: escribe el mp3 (b64decode) y devuelve un SlideTimings con duration = último character_end_times_seconds.
    - synthesize_slide con un mock que SIEMPRE devuelve timestamps planos: reintenta exactamente 3 veces (3 llamadas al SDK) y luego lanza VoiceTimestampError con mensaje claro.
    - synthesize_slide con un mock que devuelve planos 1ª vez y crecientes 2ª: tiene éxito en 2 llamadas (retry funciona).
    - Importar el módulo NO instancia el cliente ni requiere ELEVENLABS_API_KEY.
  </behavior>
  <action>
Implementa `integrations/elevenlabs.py` replicando EXACTAMENTE el patrón de `anthropic.py` (cliente lazy `_get_client()`, constantes de modelo en un solo sitio, docstrings, sección de seguridad). Decisiones D-01/D-02/D-03 + RESEARCH Pattern 1/2:
- Constantes: `MODEL_ID = "eleven_multilingual_v2"` (D-01, single source of truth), `OUTPUT_FORMAT = "mp3_44100_128"`.
- `_client = None` + `_get_client()` que importa `from elevenlabs import ElevenLabs` DENTRO de la función y devuelve `ElevenLabs()` (lee la key del entorno, D-03). Docstring de seguridad: importar el módulo NO requiere ELEVENLABS_API_KEY; NUNCA loguear la key ni el audio_base64 completo (T-04 Information Disclosure).
- `is_strictly_increasing(xs: list[float]) -> bool`: función PURA (`all(b > a for a, b in zip(xs, xs[1:]))`); listas de 0 o 1 elemento devuelven True.
- `class VoiceTimestampError(Exception)`: error de dominio para timestamps degenerados tras los reintentos.
- `synthesize_slide(text, voice_id, out_path, *, model_id=MODEL_ID, output_format=OUTPUT_FORMAT) -> SlideTimings`:
  loop `for attempt in range(3)` (D-02 retry≤3, SOLO para timestamps degenerados — NO para errores de red; el SDK ya reintenta 429/5xx, RESEARCH Anti-patterns):
  - llama `_get_client().text_to_speech.convert_with_timestamps(voice_id=, text=, model_id=, output_format=)`
  - lee `starts = resp.alignment.character_start_times_seconds` (SEGUNDOS, NO `_ms` — Pitfall 1; si lees `_ms` es un bug).
  - si `is_strictly_increasing(starts)`: `out_path.write_bytes(base64.b64decode(resp.audio_base64))`, calcula `duration = resp.alignment.character_end_times_seconds[-1]` (o 0.0 si vacío), construye un `SlideTimings` (slide_index lo decide el caller — puedes aceptarlo como arg o que el caller lo rellene; documenta tu elección) con `words=[]` (elevenlabs no segmenta a palabra aquí) y `audio_path=str(out_path)`, y `return`.
  - si no es creciente: continúa el loop.
  - tras agotar 3 intentos: `raise VoiceTimestampError(...)` con mensaje claro mencionando "timestamps degenerados/congelados" (documenta que es salvaguarda general, NO un fix de #607 — Pitfall 3).
Escribe los tests en `tests/test_elevenlabs.py` cubriendo TODOS los casos de <behavior> usando `fake_elevenlabs_response` de conftest y `mocker.patch("avideo.integrations.elevenlabs._get_client")` (o parchea el método convert_with_timestamps). Incluye un test con `-k increasing` para que el comando de VALIDATION funcione. NUNCA hagas llamadas de red reales.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_elevenlabs.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "character_start_times_seconds" src/avideo/integrations/elevenlabs.py` (lee campo seconds correcto)
    - `! grep -q "character_start_times_ms\|character_durations_ms" src/avideo/integrations/elevenlabs.py` (NO usa campos ms obsoletos)
    - `grep -q "def is_strictly_increasing" src/avideo/integrations/elevenlabs.py`
    - `grep -q "for attempt in range(3)\|range(3)" src/avideo/integrations/elevenlabs.py` (retry≤3)
    - `grep -q "class VoiceTimestampError" src/avideo/integrations/elevenlabs.py`
    - `grep -q "from elevenlabs import" src/avideo/integrations/elevenlabs.py && grep -A6 "_get_client" src/avideo/integrations/elevenlabs.py | grep -q "from elevenlabs"` (import lazy dentro de la función)
    - `uv run pytest tests/test_elevenlabs.py -k increasing -x -q` pasa
  </acceptance_criteria>
  <done>integrations/elevenlabs.py implementado con cliente lazy, validación seconds-creciente pura, retry≤3 solo-timestamps, error claro; tests verdes incluido el caso -k increasing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: stages/voice_elevenlabs.py + VoiceStage despachador</name>
  <read_first>src/avideo/stages/storyboard.py, src/avideo/stages/base.py, src/avideo/stages/stubs.py (VoiceStub), src/avideo/integrations/elevenlabs.py, src/avideo/models/timings.py, src/avideo/utils/workdir.py</read_first>
  <files>src/avideo/stages/voice_elevenlabs.py, src/avideo/stages/voice.py, tests/test_voice_elevenlabs.py</files>
  <behavior>
    - VoiceElevenlabsStage.run lee script.json (ScriptOutput), llama synthesize_slide una vez por slide, escribe workdir/audio/slide_XX.mp3 (vía mock) y devuelve un UnifiedTimings(source="elevenlabs", slides=[...]) con un SlideTimings por slide.
    - El test parchea avideo.stages.voice_elevenlabs.synthesize_slide (mock point a scope de módulo) — sin red.
    - VoiceStage.stage_name == "voice" (contrato de checkpoint intacto, D-12).
    - VoiceStage.run despacha a VoiceElevenlabsStage cuando config.voice == VoiceMode.elevenlabs.
    - El checkpoint de voz se escribe como UnifiedTimings (nombre de checkpoint "voice"); el orquestador lo persiste.
  </behavior>
  <action>
1. `stages/voice_elevenlabs.py` (D-10/D-12): clase `VoiceElevenlabsStage(CheckpointMixin)` con `stage_name = "voice"`. Importa `synthesize_slide` a SCOPE DE MÓDULO (`from avideo.integrations.elevenlabs import synthesize_slide`) para que los tests parcheen `avideo.stages.voice_elevenlabs.synthesize_slide` (patrón storyboard.py). `run(workdir, config) -> UnifiedTimings`:
   - lee `script = workdir.read_checkpoint("script", ScriptOutput)`.
   - por cada `SlideScript` en `script.slides`: construye `out_path = workdir.root / "audio" / f"slide_{slide.slide_index:02d}.mp3"`, llama `synthesize_slide(text=slide.narration, voice_id=config.voice_id, out_path=out_path, ...)`, recoge el `SlideTimings` (rellena/confirma `slide_index` y `audio_path` relativos a workdir.root — decide y documenta si guardas path relativo "audio/slide_XX.mp3" o absoluto; recomendado RELATIVO para portabilidad de checkpoints).
   - devuelve `UnifiedTimings(source="elevenlabs", slides=[...])`. NO escribe checkpoint (lo hace el orquestador). Usa rich opcionalmente para progreso por slide (no obligatorio).
2. `stages/voice.py` (D-12, RESEARCH Open Question 3): clase `VoiceStage(CheckpointMixin)` con `stage_name = "voice"`. `run(workdir, config) -> UnifiedTimings` que despacha por `config.voice`:
   - `if config.voice == VoiceMode.elevenlabs:` delega a `VoiceElevenlabsStage().run(workdir, config)`.
   - `else:` (record) delega a `VoiceRecordStage().run(workdir, config)` — importa VoiceRecordStage DENTRO del branch (import perezoso; lo crea 04-02 y trae deps pesadas). Documenta que record se implementa en 04-02; por ahora el branch puede importar de forma que falle con mensaje claro si el módulo aún no existe (try/except ImportError → error claro "modo record no disponible"). El swap en PIPELINE_STAGES lo hace 04-03; este plan NO toca stubs.py.
   IMPORTANTE: `checkpoint_name` de VoiceStage debe seguir siendo "voice" (default = stage_name) para preservar el contrato de checkpoint del orquestador.
3. Tests en `tests/test_voice_elevenlabs.py`: parchea `avideo.stages.voice_elevenlabs.synthesize_slide` con un side_effect que devuelve un SlideTimings por slide y (opcionalmente) escribe el mp3 en out_path; escribe un script.json de 2 slides vía WorkdirManager; asserta: (a) synthesize_slide llamado una vez por slide, (b) el resultado es UnifiedTimings(source="elevenlabs") con 2 SlideTimings, (c) stage_name == "voice". Añade un test para VoiceStage que con config.voice=elevenlabs despacha a la rama elevenlabs (parchea VoiceElevenlabsStage.run o synthesize_slide). Sin red.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_voice_elevenlabs.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from avideo.integrations.elevenlabs import synthesize_slide" src/avideo/stages/voice_elevenlabs.py` (mock point a scope de módulo)
    - `grep -q 'stage_name: str = "voice"' src/avideo/stages/voice_elevenlabs.py`
    - `grep -q 'stage_name: str = "voice"' src/avideo/stages/voice.py`
    - `grep -q "UnifiedTimings" src/avideo/stages/voice_elevenlabs.py && grep -q 'source="elevenlabs"\|source=.elevenlabs' src/avideo/stages/voice_elevenlabs.py`
    - `grep -q "config.voice" src/avideo/stages/voice.py` (despacho por config)
    - `uv run pytest tests/test_voice_elevenlabs.py -x -q` pasa
    - `uv run pytest -q` suite completa verde
  </acceptance_criteria>
  <done>VoiceElevenlabsStage produce UnifiedTimings(source=elevenlabs) por slide con mp3 escrito; VoiceStage(stage_name=voice) despacha por config.voice; tests verdes con mock a scope de módulo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proceso → ElevenLabs API | El texto del guion (propio, de Phase 2) sale; la API key cruza al SDK desde el entorno |
| ElevenLabs API → proceso | audio_base64 + alignment entran (no se ejecutan como código; se decodifican y validan) |
| proceso → filesystem | Se escriben audio/slide_XX.mp3 SOLO bajo workdir.root/audio (paths vía WorkdirManager) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | integrations/elevenlabs.py (ELEVENLABS_API_KEY) | mitigate | Cliente lazy: importar el módulo no requiere la key; el SDK la lee del entorno (D-03). NUNCA loguear la key ni el audio_base64 completo; sin headers en logs. Espejo del patrón T-02-07 de anthropic.py |
| T-04-02 | Tampering | synthesize_slide (timestamps degenerados de la API) | mitigate | Validación pura estrictamente-creciente sobre character_start_times_seconds antes de aceptar; retry≤3; VoiceTimestampError claro si persiste (D-02). Salvaguarda general de timestamps degenerados (NO fix de #607) |
| T-04-03 | Tampering | escritura de audio/slide_XX.mp3 | mitigate | out_path construido SOLO desde workdir.root/audio con f"slide_{index:02d}.mp3" (sin entrada de usuario en el nombre) → sin path traversal |
| T-04-04 | Tampering | texto del guion enviado a TTS | accept | Bajo riesgo: el guion es nuestro (Phase 2, validado por Pydantic) y no se ejecuta como código; TTS solo lo vocaliza |
</threat_model>

<verification>
- `uv run pytest -q` — suite completa verde (incluye test_elevenlabs.py + test_voice_elevenlabs.py).
- `uv run pytest tests/test_elevenlabs.py -k increasing -x -q` — validación creciente verde (comando de VALIDATION.md).
- `uv run python -c "import avideo.integrations.elevenlabs"` no requiere ELEVENLABS_API_KEY (import-safe).
- `grep` gates de acceptance_criteria de cada task en verde.
</verification>

<success_criteria>
- VOICE-01: modo elevenlabs genera un mp3 por slide vía convert_with_timestamps (mock en CI; real con key) y produce UnifiedTimings.
- VOICE-02: timestamps validados estrictamente crecientes (campo _seconds) con retry≤3 y error claro.
- ALIGN-02 (preparado): la voz produce los timings directamente; align será no-op en elevenlabs (lo cierra 04-02/04-03).
- UnifiedTimings (D-11) existe y es el contrato compartido; whisperx_model en RunConfig.
- Cliente lazy + mock point a scope de módulo replicando anthropic.py; sin red en tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voz-subtitulos/04-01-SUMMARY.md`
</output>
