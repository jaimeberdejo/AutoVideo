---
phase: 04-voz-subtitulos
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/avideo/utils/subtitle_format.py
  - src/avideo/stages/subtitles.py
  - src/avideo/stages/stubs.py
  - tests/test_subtitles.py
  - tests/test_orchestrator.py
autonomous: true
requirements: [SUB-01, SUB-02]

must_haves:
  truths:
    - "El sistema SIEMPRE genera workdir/subs/output.srt y workdir/subs/output.vtt desde UnifiedTimings"
    - "Los timestamps por-slide relativos se acumulan a globales (offset = Σ duraciones previas) al serializar"
    - "Las cues respetan ~42 chars/línea, ≤2 líneas, ≤5s/cue y un cap de CPS (~17)"
    - "El SRT usa coma en los timestamps; el VTT usa punto y cabecera WEBVTT"
    - "Phase 4 NO quema subtítulos; --burn-subs solo deja los archivos listos para Phase 5"
    - "Las etapas voice/align/subs reales reemplazan los stubs en PIPELINE_STAGES preservando stage_names"
  artifacts:
    - path: "src/avideo/utils/subtitle_format.py"
      provides: "Lógica pura: segmentación de palabras en cues + serialización SRT/VTT (fmt_ts coma vs punto)"
      contains: "WEBVTT"
    - path: "src/avideo/stages/subtitles.py"
      provides: "SubtitlesStage: UnifiedTimings → output.srt + output.vtt (acumula offset global)"
      contains: 'stage_name: str = "subs"'
    - path: "src/avideo/stages/stubs.py"
      provides: "PIPELINE_STAGES con VoiceStage/AlignStage/SubtitlesStage reales (swap de stubs)"
      contains: "VoiceStage()"
  key_links:
    - from: "src/avideo/stages/subtitles.py"
      to: "src/avideo/utils/subtitle_format.py"
      via: "import de segmentación + serialización pura"
      pattern: "from avideo.utils.subtitle_format import"
    - from: "src/avideo/orchestrator.py"
      to: "src/avideo/stages/stubs.py PIPELINE_STAGES"
      via: "VoiceStage/AlignStage/SubtitlesStage en la lista canónica"
      pattern: "VoiceStage\\(\\)"
---

<objective>
Implementa la generación de subtítulos (SUB-01/SUB-02): la lógica PURA de segmentación de palabras en cues y serialización SRT/VTT (`utils/subtitle_format.py`), la etapa `subtitles` que SIEMPRE produce `output.srt` + `output.vtt` desde `UnifiedTimings` acumulando el offset global por slide, y el SWAP FINAL de `voice`/`align`/`subs` en `PIPELINE_STAGES` (reemplaza los stubs por las etapas reales de 04-01/04-02/04-03 preservando los `stage_names` y los checkpoints).

Purpose: Cierra la fase. Toma el `UnifiedTimings` agnóstico de la fuente (de elevenlabs o whisperx) y lo convierte en subtítulos listos para el montaje. La lógica de cues/serialización es 100% pura y testeable sin red/audio (el corazón testeable de la fase). El swap conecta las tres etapas reales al orquestador end-to-end.

Output: `utils/subtitle_format.py`, `stages/subtitles.py`, `PIPELINE_STAGES` actualizado, y los tests reales de `test_subtitles.py` (scaffold de 04-01) + actualización de `test_orchestrator.py` para el nuevo wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voz-subtitulos/04-CONTEXT.md
@.planning/phases/04-voz-subtitulos/04-RESEARCH.md
@.planning/phases/04-voz-subtitulos/04-VALIDATION.md
@CLAUDE.md

# Contratos creados en planes previos de la fase:
@.planning/phases/04-voz-subtitulos/04-01-PLAN.md
@.planning/phases/04-voz-subtitulos/04-02-PLAN.md

# Lista canónica a actualizar + patrón del swap de Phase 2:
@src/avideo/stages/stubs.py
@src/avideo/orchestrator.py
@.planning/phases/02-llm-pipeline/02-03-SUMMARY.md

# Contratos de etapa, filesystem, config:
@src/avideo/stages/base.py
@src/avideo/utils/workdir.py
@src/avideo/models/config.py
@tests/test_orchestrator.py

<interfaces>
<!-- Contratos del codebase + de 04-01/04-02. No explorar; usar directamente. -->

UnifiedTimings (src/avideo/models/timings.py — 04-01):
```python
class WordTiming(BaseModel): text: str; start: float; end: float   # SEGUNDOS, RELATIVOS al slide
class SlideTimings(BaseModel): slide_index: int; audio_path: str; duration: float; words: list[WordTiming] = []
class UnifiedTimings(BaseModel): source: str; slides: list[SlideTimings]
```
IMPORTANTE (decisión documentada en 04-01): los start/end de WordTiming son RELATIVOS al inicio del clip del slide. Para subtítulos del vídeo final se necesitan timestamps GLOBALES → subtitles.py acumula offset = Σ duraciones de slides previos (slide.duration).

Etapas reales creadas en planes previos (a insertar en PIPELINE_STAGES):
- src/avideo/stages/voice.py     → class VoiceStage(CheckpointMixin), stage_name="voice"  (04-01; despacha por config.voice)
- src/avideo/stages/align.py     → class AlignStage(CheckpointMixin), stage_name="align"  (04-02)
- src/avideo/stages/subtitles.py → class SubtitlesStage(CheckpointMixin), stage_name="subs" (este plan)

PIPELINE_STAGES actual (src/avideo/stages/stubs.py) — orden canónico:
```python
PIPELINE_STAGES: list = [
    ContextStage(), StoryboardStage(), TimingStage(), ScriptwriterStage(),  # Phase 2 reales
    SlidesStub(),          # Phase 3
    VerifyStub(),          # Phase 6
    VoiceStub(),           # ← reemplazar por VoiceStage()      (este plan)
    AlignStub(),           # ← reemplazar por AlignStage()      (este plan)
    SubsStub(),            # ← reemplazar por SubtitlesStage()  (este plan)
    AssembleStub(),        # Phase 5
]
```
El orquestador (orchestrator.py) llama stage.run() → write_checkpoint(stage.checkpoint_name, output) → mark_done(stage.stage_name). checkpoint_name: voice→"voice", align→"align", subs→"subs". El orquestador es agnóstico de las clases concretas (StageProtocol).

WorkdirManager: self.root; subdir subs/ ya creado; checkpoint_path; read_checkpoint(name, cls). La etapa NO escribe checkpoints; SÍ escribe los archivos .srt/.vtt bajo workdir.root/subs.

RunConfig: burn_subs: bool = False (ya existe, Phase 1). Phase 4 NO quema (D-09).

Serialización (VERIFIED en 04-RESEARCH.md Code Examples):
```python
def fmt_ts(seconds, *, vtt):  # SRT usa "," ; VTT usa "."
    ...  # "HH:MM:SS,mmm" (SRT) / "HH:MM:SS.mmm" (VTT)
# SRT: índice 1-based + "start --> end" + texto + línea en blanco
# VTT: cabecera "WEBVTT" + línea en blanco + cues (sin índice)
```
Restricciones de cue (D-08 + RESEARCH Pitfall 6): ~42 chars/línea, ≤2 líneas, ≤5s/cue, CPS ≤ ~17.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: utils/subtitle_format.py (segmentación de cues + SRT/VTT, lógica pura)</name>
  <read_first>.planning/phases/04-voz-subtitulos/04-RESEARCH.md (Code Examples SRT/VTT, Pitfall 6, Pattern 4 nota offset), src/avideo/models/timings.py</read_first>
  <files>src/avideo/utils/subtitle_format.py, tests/test_subtitles.py</files>
  <behavior>
    - fmt_ts(3661.5, vtt=False) == "01:01:01,500"; fmt_ts(3661.5, vtt=True) == "01:01:01.500" (coma vs punto).
    - to_srt([cues]) produce índices 1-based, "start --> end", texto, línea en blanco entre cues.
    - to_vtt([cues]) empieza por "WEBVTT" + línea en blanco; sin índices.
    - segment_words(words, ...) rompe cue al exceder cualquiera: ~42 chars/línea, ≤2 líneas, ≤5s/cue, CPS≤17. Una palabra cuya inclusión rompa el límite arranca una nueva cue.
    - Caso límite: lista de palabras vacía → cero cues. Una sola palabra larga → una cue (no se pierde texto).
  </behavior>
  <action>
Crea `utils/subtitle_format.py` con SOLO lógica pura (sin I/O, sin red — es el núcleo testeable de la fase, RESEARCH "Don't Hand-Roll" key insight):
1. `@dataclass`/`class Cue` (start: float, end: float, text: str) — text con ≤2 líneas separadas por "\n".
2. `fmt_ts(seconds: float, *, vtt: bool) -> str` exactamente como RESEARCH Code Examples: h/m/s/ms, `sep = "." if vtt else ","`, formato `f"{h:02d}:{m:02d}:{s:02d}{sep}{ms:03d}"`. Redondea ms con `round(...)`.
3. `segment_words(words: list[WordTiming], *, max_chars_per_line=42, max_lines=2, max_cue_seconds=5.0, max_cps=17.0) -> list[Cue]` (D-08 + Pitfall 6): agrupa palabras consecutivas en cues; rompe la cue cuando añadir la siguiente palabra excedería cualquier límite (chars totales / líneas / duración / CPS). Reparte el texto en ≤2 líneas (wrap por ~42 chars). start = primera palabra, end = última palabra de la cue. Maneja vacío → []. Documenta los parámetros (Claude's discretion sobre el wrapping fino).
4. `to_srt(cues: list[Cue]) -> str` y `to_vtt(cues: list[Cue]) -> str` exactamente como RESEARCH Code Examples (SRT índice 1-based + coma; VTT cabecera WEBVTT + punto, sin índice).
Tests en tests/test_subtitles.py (PUROS, sin mocks) cubriendo TODO <behavior>: fmt_ts coma vs punto (incluye un timestamp con horas), to_srt índices/blank lines, to_vtt cabecera WEBVTT, segment_words con casos límite (vacío, palabra única, ruptura por chars, ruptura por duración >5s, ruptura por CPS). Estos cubren SUB-01.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_subtitles.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def fmt_ts" src/avideo/utils/subtitle_format.py && grep -q "def segment_words" src/avideo/utils/subtitle_format.py`
    - `grep -q "def to_srt" src/avideo/utils/subtitle_format.py && grep -q "def to_vtt" src/avideo/utils/subtitle_format.py`
    - `grep -q "WEBVTT" src/avideo/utils/subtitle_format.py` (cabecera VTT)
    - `grep -v '^#' src/avideo/utils/subtitle_format.py | grep -q '","' && grep -v '^#' src/avideo/utils/subtitle_format.py | grep -q '"\."'` (coma para SRT, punto para VTT)
    - `uv run pytest tests/test_subtitles.py -x -q` pasa (casos límite incluidos)
  </acceptance_criteria>
  <done>subtitle_format.py con fmt_ts (coma/punto), segment_words (42/2/5s/17CPS) y to_srt/to_vtt puros; tests puros verdes incluyendo casos límite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stages/subtitles.py (UnifiedTimings → output.srt + output.vtt con offset global)</name>
  <read_first>src/avideo/stages/stubs.py (SubsStub), src/avideo/utils/subtitle_format.py, src/avideo/models/timings.py, src/avideo/utils/workdir.py, src/avideo/models/config.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Pattern 4 nota offset, Open Question 1, Pitfall 4)</read_first>
  <files>src/avideo/stages/subtitles.py, tests/test_subtitles.py</files>
  <behavior>
    - SubtitlesStage.run lee el checkpoint "align" (UnifiedTimings), acumula offset global por slide (offset += slide.duration tras cada slide), segmenta las palabras (con offset) en cues, y escribe workdir/subs/output.srt + output.vtt.
    - Con 2 slides, las cues del segundo slide tienen timestamps desplazados por la duración del primero (offset global verificado).
    - Phase 4 NO quema: aunque config.burn_subs sea True, solo se escriben los archivos (SUB-02); no se invoca ffmpeg.
    - Devuelve un modelo de salida con las rutas .srt/.vtt (o reusa una salida válida) y stage_name == "subs".
  </behavior>
  <action>
`stages/subtitles.py` (reemplaza SubsStub; D-07/D-08/D-09/D-12): clase `SubtitlesStage(CheckpointMixin)` con `stage_name = "subs"` (checkpoint_name default "subs"). Importa `segment_words, to_srt, to_vtt, Cue` a scope de módulo desde `avideo.utils.subtitle_format`. `run(workdir, config) -> BaseModel`:
1. Lee `timings = workdir.read_checkpoint("align", UnifiedTimings)` (align siempre produce el unified final: whisperx en record, passthrough en elevenlabs — D-11). Si elevenlabs no segmentó a palabra (words vacío), genera una cue por slide con el texto completo del guion como fallback razonable (lee script.json si hace falta) O documenta que elevenlabs sí trae words; decide y documenta el fallback para words=[] (no debe perderse texto). Recomendado: si words está vacío, crear una sola cue por slide [offset, offset+duration] con un texto placeholder/del guion — documenta la elección.
2. Acumula offset GLOBAL (Pattern 4 nota, Open Question 1): `offset = 0.0`; por cada slide, desplaza cada WordTiming a global (`start+offset`, `end+offset`) ANTES de segmentar, segmenta con `segment_words`, acumula las cues; luego `offset += slide.duration`. Documenta que esto convierte relativos-por-slide en globales para el vídeo final.
3. Serializa: `srt = to_srt(all_cues)`, `vtt = to_vtt(all_cues)`. Escribe `workdir.root/subs/output.srt` y `workdir.root/subs/output.vtt` (UTF-8). Paths SOLO vía workdir.root.
4. SUB-02: NO quemar. Aunque `config.burn_subs` sea True, este stage solo deja los archivos (el quemado es Phase 5). Documenta.
5. Devuelve un modelo de salida (Claude's discretion: reusar un modelo existente o un pequeño SubtitlesOutput; si creas uno nuevo, re-expórtalo en models/__init__.py). Debe ser serializable por write_checkpoint. NO escribe checkpoint (lo hace el orquestador).
Tests en tests/test_subtitles.py (amplía el archivo): (a) escribe checkpoint "align" = UnifiedTimings con 2 slides (cada uno con words y duration); ejecuta el stage; asserta que output.srt y output.vtt existen, que el .vtt empieza por "WEBVTT", que el .srt usa coma, y que las cues del 2º slide están desplazadas por la duración del 1º (offset global). (b) `-k burn`: con config.burn_subs=True, asserta que los archivos se escriben y NO se invoca ningún quemado (cubre SUB-02). (c) stage_name == "subs". Sin red/audio.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_subtitles.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from avideo.utils.subtitle_format import" src/avideo/stages/subtitles.py`
    - `grep -q 'stage_name: str = "subs"' src/avideo/stages/subtitles.py`
    - `grep -q "offset" src/avideo/stages/subtitles.py` (acumulación de offset global)
    - `grep -q "output.srt" src/avideo/stages/subtitles.py && grep -q "output.vtt" src/avideo/stages/subtitles.py`
    - `grep -q 'read_checkpoint("align"' src/avideo/stages/subtitles.py` (lee el unified final)
    - `uv run pytest tests/test_subtitles.py -k burn -x -q` pasa (SUB-02: no quema)
    - `uv run pytest tests/test_subtitles.py -x -q` pasa (offset global verificado)
  </acceptance_criteria>
  <done>SubtitlesStage lee align UnifiedTimings, acumula offset global, escribe output.srt (coma) + output.vtt (WEBVTT), no quema con burn_subs; tests verdes con offset multi-slide y caso burn.</done>
</task>

<task type="auto">
  <name>Task 3: Swap voice/align/subs en PIPELINE_STAGES + actualizar test_orchestrator</name>
  <read_first>src/avideo/stages/stubs.py (PIPELINE_STAGES + imports), src/avideo/orchestrator.py, tests/test_orchestrator.py, .planning/phases/02-llm-pipeline/02-03-SUMMARY.md (cómo se hizo el swap de Phase 2)</read_first>
  <files>src/avideo/stages/stubs.py, tests/test_orchestrator.py</files>
  <action>
1. En `src/avideo/stages/stubs.py`: importa las etapas reales al inicio del módulo junto a las Phase-2 (`from avideo.stages.voice import VoiceStage`, `from avideo.stages.align import AlignStage`, `from avideo.stages.subtitles import SubtitlesStage`). En `PIPELINE_STAGES`, reemplaza `VoiceStub()` → `VoiceStage()`, `AlignStub()` → `AlignStage()`, `SubsStub()` → `SubtitlesStage()`, conservando EL ORDEN y los comentarios actualizados (igual que el swap de Phase 2 en plan 02-03). MANTÉN las clases stub `VoiceStub/AlignStub/SubsStub` definidas (los tests que las importan directamente siguen funcionando — patrón Phase 2). Verifica que los stage_names siguen siendo voice/align/subs (contrato de checkpoint intacto, D-12).
2. Actualiza `tests/test_orchestrator.py` si algún test asume los stubs en esas posiciones: ajusta las aserciones para esperar VoiceStage/AlignStage/SubtitlesStage (o que PIPELINE_STAGES tenga 10 etapas con los stage_names correctos en orden). NO debilites las garantías existentes (skip-done, orden, write_checkpoint antes de mark_done). Si los tests del orquestador ya son agnósticos de las clases concretas, basta con confirmar que pasan.
3. Verifica el wiring end-to-end con elevenlabs mockeado: el orquestador recorre voice→align→subs sin red. Si hace falta, añade/ajusta un test que parchee `avideo.stages.voice_elevenlabs.synthesize_slide` (o VoiceStage) y `avideo.stages.align.align_wav` y confirme que tras run_pipeline existen los checkpoints voice.json/align.json/subs.json y los subs/output.srt/.vtt (en un workdir tmp, modo elevenlabs).
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "VoiceStage()" src/avideo/stages/stubs.py && grep -q "AlignStage()" src/avideo/stages/stubs.py && grep -q "SubtitlesStage()" src/avideo/stages/stubs.py`
    - `! grep -q "VoiceStub()," src/avideo/stages/stubs.py && ! grep -q "AlignStub()," src/avideo/stages/stubs.py && ! grep -q "SubsStub()," src/avideo/stages/stubs.py` (ya no en la lista; las clases siguen definidas)
    - `grep -q "class VoiceStub\|class AlignStub\|class SubsStub" src/avideo/stages/stubs.py` (clases stub conservadas)
    - `grep -q "from avideo.stages.voice import VoiceStage" src/avideo/stages/stubs.py`
    - `uv run pytest -q` suite completa verde (incluido test_orchestrator)
  </acceptance_criteria>
  <done>PIPELINE_STAGES usa VoiceStage/AlignStage/SubtitlesStage reales (stubs conservados como clases); test_orchestrator actualizado; suite completa verde; pipeline recorre voice→align→subs end-to-end con elevenlabs mockeado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UnifiedTimings (checkpoint) → subtitles | Texto de palabras (de Phase 2 vía voz/align) entra a la serialización |
| proceso → filesystem | Se escriben subs/output.srt y output.vtt SOLO bajo workdir.root/subs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | texto de subtítulo en .srt/.vtt | mitigate | El texto proviene del guion propio (Phase 2, validado Pydantic) y/o de la transcripción whisperx del propio audio; se escribe como texto plano (no se ejecuta). El formato SRT/VTT no interpreta el contenido como instrucciones (V5 Input Validation) |
| T-04-10 | Tampering | paths de output.srt/.vtt | mitigate | Paths construidos SOLO desde workdir.root/subs con nombres fijos ("output.srt"/"output.vtt") → sin path traversal |
| T-04-11 | Repudiation / Integrity | escritura de los archivos de subs | accept | Escritura directa de archivos de salida (no checkpoint atómico) es aceptable: son artefactos regenerables e idempotentes desde el checkpoint align |
</threat_model>

<verification>
- `uv run pytest -q` — suite completa verde (test_subtitles.py reales + test_orchestrator actualizado).
- `uv run pytest tests/test_subtitles.py -x -q` y `-k burn` en verde (SUB-01 + SUB-02).
- End-to-end con elevenlabs mockeado: run_pipeline deja voice/align/subs checkpoints + subs/output.srt/.vtt.
- `grep` gates de acceptance_criteria en verde (swap correcto, stubs conservados, imports puros).
</verification>

<success_criteria>
- SUB-01: SIEMPRE genera output.srt (coma) + output.vtt (WEBVTT) desde UnifiedTimings, con offset global por slide y cues legibles (42/2/5s/17CPS).
- SUB-02: --burn-subs solo deja los archivos listos; Phase 4 NO quema (el quemado es Phase 5).
- Lógica de cues/serialización 100% pura y testeada con casos límite.
- PIPELINE_STAGES con VoiceStage/AlignStage/SubtitlesStage reales (D-12); stage_names voice/align/subs preservados; pipeline end-to-end verde.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voz-subtitulos/04-03-SUMMARY.md`
</output>
