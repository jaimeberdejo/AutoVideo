---
phase: 06-slides-hybrid-verificador
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/test_image_utils.py
  - tests/test_anthropic_integration.py
  - tests/test_verify_slides.py
  - tests/test_orchestrator.py
  - src/avideo/utils/image_utils.py
  - src/avideo/integrations/anthropic.py
  - src/avideo/stages/verify_slides.py
  - src/avideo/stages/stubs.py
  - src/avideo/orchestrator.py
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03

must_haves:
  truths:
    - "downscale_png_for_api reduces a 1920x1080 PNG to <=1568px longest side and returns standard base64 PNG with no newlines"
    - "call_structured_with_images sends image content blocks BEFORE the text block, using base64 source with media_type 'image/png', plus forced tool-use to validate output against a Pydantic model"
    - "VerifyStage runs one vision call per slide in hybrid/manual mode, emitting a SlideVerdict (status ok/warning/fail + issues + suggestions) per slide"
    - "In auto mode VerifyStage does NOT make any vision call and returns a trivial all-ok report sized to the storyboard"
    - "VerifyStage writes workdir/verification_report.json atomically (tmp then rename) as a human-readable artifact, distinct from the verification.json checkpoint written by the orchestrator"
    - "After verify runs: auto skips gating; L1/L2 render the Rich report and pause to iterate; L3 raises Exit(1) if any slide is fail; L4 continues silently; warning never stops L3/L4"
  artifacts:
    - path: "src/avideo/utils/image_utils.py"
      provides: "downscale_png_for_api(path) plus MAX_LONG_SIDE/MAX_BYTES/MEDIA_TYPE constants"
      contains: "def downscale_png_for_api"
    - path: "src/avideo/integrations/anthropic.py"
      provides: "call_structured_with_images vision + forced tool-use helper"
      contains: "def call_structured_with_images"
    - path: "src/avideo/stages/verify_slides.py"
      provides: "VerifyStage real verifier (replaces VerifyStub)"
      contains: "class VerifyStage"
    - path: "src/avideo/orchestrator.py"
      provides: "post-verify L1/L2/L3/L4 verdict gate + _render_verification_report"
      contains: "_render_verification_report"
    - path: "tests/test_verify_slides.py"
      provides: "Wave-0 RED tests for VerifyStage (auto skip, per-slide call, report writes)"
      contains: "def test_verify_auto_mode_skips"
    - path: "tests/test_image_utils.py"
      provides: "Wave-0 RED tests for downscale helper"
      contains: "def test_downscale_reduces_1920"
  key_links:
    - from: "src/avideo/stages/verify_slides.py"
      to: "avideo.integrations.anthropic.call_structured_with_images"
      via: "module-scope import + one call per slide PNG"
      pattern: "call_structured_with_images"
    - from: "src/avideo/integrations/anthropic.py"
      to: "avideo.utils.image_utils.downscale_png_for_api"
      via: "encode each image path to base64 before building content blocks"
      pattern: "downscale_png_for_api"
    - from: "src/avideo/orchestrator.py"
      to: "VerificationReport.slides[].status"
      via: "post-run verdict check after stage.run for stage_name verify"
      pattern: "status == .fail."
    - from: "src/avideo/stages/stubs.py"
      to: "VerifyStage"
      via: "PIPELINE_STAGES position 6 replaces VerifyStub() with VerifyStage()"
      pattern: "VerifyStage\\(\\)"
---

<objective>
Wire in the Claude-Vision verificador (VERIFY-01/02/03). Add a vision-capable
structured helper to the integration layer, a Pillow downscale utility, the real
VerifyStage (replacing VerifyStub), and the long-deferred orchestrator post-verify
level gate (the TODO Phase 6 at orchestrator.py line 82).

Purpose: In hybrid/manual modes, audit each user slide against its storyboard spec
and narration, emit a per-slide JSON report (ok/warning/fail + issues + suggestions),
and apply the L1-L4 behavior (auto skips; L1/L2 iterate; L3 stops on fail; L4 silent).
In auto mode the verifier does not run.

Output: image_utils.py, call_structured_with_images in anthropic.py, verify_slides.py,
swapped PIPELINE_STAGES, orchestrator gate, and Wave-0 test scaffolds + patched existing
orchestrator full-run tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-slides-hybrid-verificador/06-CONTEXT.md
@.planning/phases/06-slides-hybrid-verificador/06-RESEARCH.md
@.planning/phases/06-slides-hybrid-verificador/06-VALIDATION.md
@.planning/phases/06-slides-hybrid-verificador/06-01-PLAN.md

<interfaces>

Contracts the executor needs. Extracted from the codebase — do not re-explore.

models/verification.py (REUSE — issues/suggestions already present, NO model change):
- SlideVerdict(slide_index: int, status: str = "ok", issues: list[str]=[], suggestions: list[str]=[])
  status is one of ok / warning / fail.
- VerificationReport(slides: list[SlideVerdict])

integrations/anthropic.py (existing text helper to mirror — see file):
- MODEL = "claude-sonnet-4-6"  (reuse this constant for the vision call)
- _get_client() lazy singleton, max_retries=3
- call_structured(system, user, tool_name, tool_description, output_model, max_tokens=8192):
  derives schema via output_model.model_json_schema(); calls messages.create with
  tool_choice {type: tool, name: tool_name} (D-03 forced); loops resp.content for the
  matching tool_use block and returns output_model.model_validate(block.input); else RuntimeError.

models/storyboard.py + script.py + slides.py:
- SlideSpec(title, bullets, visual_type: VisualType); StoryboardOutput(slides, language="es")
- SlideScript(slide_index, narration); ScriptOutput(slides, language="es")
- SlidesOutput(png_paths: list[str], mode: str = "auto")

models/config.py:
- RunConfig.slides_mode: SlidesMode  (access config.slides_mode.value: auto / hybrid / manual)
- RunConfig.level: int (1-4)

utils/workdir.py:
- WorkdirManager.root: Path
- checkpoint_path(name) -> root/<name>.json ; read_checkpoint(name, model_class) ; is_done(stage)
- write_checkpoint("verification", report) is done by the ORCHESTRATOR, not the stage.
- The stage writes verification_report.json itself (atomic tmp then os.replace).

utils/rich_ui.py:
- console = Console(stderr=True) ; pause_for_approval(stage_name, reason="") ; (rich.table.Table)

orchestrator.py (the gate site — TODO at line 82; loop at lines 128-158):
- CREATIVE_STAGES = frozenset({storyboard, scriptwriter, slides, verify})  (already includes verify)
- FAIL_STAGES = frozenset({verify})
- should_pause(stage_name, level): L4 -> False; L1 -> True; L2 -> stage in CREATIVE_STAGES;
  L3 -> False (KEEP False; verdict check is POST-run, Pitfall 4).
- Loop body: output = stage.run(workdir, config) ; then write_checkpoint ; then mark_done.

Vision content block format (VERIFIED, RESEARCH Pattern 1/2 + Pitfall 3):
- {type: image, source: {type: base64, media_type: image/png, data: <b64-no-newlines>}}
- images BEFORE text in the content list; media_type exactly "image/png" lowercase.

Pillow downscale (VERIFIED, RESEARCH Pattern 4):
- MAX_LONG_SIDE = 1568 (non-Opus limit); MAX_BYTES = 20*1024*1024
- Image.open(p).convert("RGB"); resize(..., Image.LANCZOS) if max side > 1568;
  save to io.BytesIO format PNG; base64.standard_b64encode(raw).decode("utf-8").

Test mock patterns (REUSE from tests/test_orchestrator.py):
- _fake_run_ffmpeg_factory() (lines 56-79), _fake_probe_duration (82), _mock_renderer_cls()
  (331-338), _mock_call_structured_for_pipeline() (341-376), _fake_synthesize_slide_factory()
  (379-401), _make_config() (309-328). The full-run tests patch a long with(...) block
  (e.g. lines 413-421) — you ADD patch("avideo.stages.verify_slides.call_structured_with_images", ...).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0 (Wave 0): RED test scaffold for image_utils + vision helper + VerifyStage + orchestrator gate</name>
  <read_first>
    - tests/test_anthropic_integration.py (full file — fake Message/_get_client patterns; add a TestCallStructuredWithImages class in the same style)
    - tests/test_orchestrator.py (lines 56-101 fake factories; 309-401 helpers; 404-431 full-run test to copy + patch; 597-626 L2 pause test to mirror for the verify gate)
    - .planning/phases/06-slides-hybrid-verificador/06-RESEARCH.md (lines 632-665 Test Map; lines 644-652 exact verify test names)
    - src/avideo/models/verification.py, src/avideo/models/config.py (already in interfaces)
  </read_first>
  <files>tests/test_image_utils.py, tests/test_anthropic_integration.py, tests/test_verify_slides.py, tests/test_orchestrator.py</files>
  <action>
    Create/extend RED tests. Patch at the boundary where the name is USED (Pitfall 6:
    avideo.stages.verify_slides.call_structured_with_images). No real API, no network, no
    Chromium. Use a tiny-PNG helper in each new test file:
      def _write_png(path, size=(1920, 1080)):
          from PIL import Image
          Image.new("RGB", size, (10, 20, 30)).save(path, format="PNG")

    tests/test_image_utils.py — covers VERIFY-01 downscale:
      - test_downscale_reduces_1920: write a 1920x1080 PNG; b64 = downscale_png_for_api(path);
        decode b64, open with PIL from BytesIO; assert max(size) <= 1568 and it is a valid PNG.
      - test_downscale_leaves_small_image: write an 800x600 PNG; assert decoded size still <=1568
        (unchanged), no upscaling.
      - test_downscale_returns_standard_base64: assert returned string has no newline char and
        round-trips through base64.standard_b64decode without error.
      - test_downscale_media_type_constant: assert avideo.utils.image_utils.MEDIA_TYPE == "image/png".

    tests/test_anthropic_integration.py — ADD class TestCallStructuredWithImages (mirror the
    existing fake-client style in that file):
      - test_builds_image_block_before_text: patch _get_client to a fake client whose
        .messages.create records kwargs and returns a fake Message with a tool_use block.
        Patch avideo.integrations.anthropic.downscale_png_for_api to return "ZkFLRQ==".
        Call call_structured_with_images(system=..., user="audit", image_paths=[Path("a.png")],
        tool_name="emit_verdict", tool_description="...", output_model=SlideVerdict).
        Assert recorded messages[0]["content"][0]["type"] == "image";
        ["source"]["type"] == "base64"; ["source"]["media_type"] == "image/png";
        ["source"]["data"] == "ZkFLRQ=="; and the LAST content item is
        {"type":"text","text":"audit"} (image-before-text). Assert tool_choice ==
        {"type":"tool","name":"emit_verdict"}.
      - test_returns_validated_model: fake tool_use.input is a valid SlideVerdict dict → assert
        the return is a SlideVerdict with the expected status.
      - test_raises_when_no_tool_use: fake Message with only a text block → pytest.raises(RuntimeError).

    tests/test_verify_slides.py — covers VERIFY-01/02:
      - test_verify_auto_mode_skips: pre-write a 2-slide storyboard; config.slides_mode=auto;
        patch avideo.stages.verify_slides.call_structured_with_images with a MagicMock; run
        VerifyStage().run(workdir, config); assert the mock was NOT called and the returned
        report has 2 slides all status=="ok".
      - test_verify_calls_per_slide: pre-write 2-slide storyboard + script + a SlidesOutput
        checkpoint ("slides") pointing at two PNGs you create in workdir/slides/; config hybrid;
        patch call_structured_with_images to return SlideVerdict(slide_index=0, status="ok")
        (index gets overwritten by the stage); run; assert call_count == 2 and report has 2 verdicts.
      - test_verify_writes_report_json: same setup; after run assert
        workdir.root/"verification_report.json" exists and parses as VerificationReport.
      - test_verify_report_json_atomic_no_tmp: after run assert no
        "verification_report.json.tmp" remains in workdir.root.
      - test_verify_propagates_fail_status: patch call_structured_with_images so one verdict is
        status="fail"; assert the report contains a fail verdict.

    tests/test_orchestrator.py — ADD verify-gate tests + PATCH existing full-run tests:
      - Add a _fake_verify_factory() helper (mirror _fake_run_ffmpeg_factory): returns a
        side_effect for call_structured_with_images returning SlideVerdict(slide_index=0, status="ok").
      - PATCH every existing full-run test (test_orch_full_run_all_stages_done,
        test_orch_idempotent_second_run, test_orch_resume_after_partial,
        test_orch_level1_pauses_each_stage, test_orch_level2_pauses_creative_stages,
        test_stub_run_returns_pydantic_basemodel, test_orch_level4_no_pause): add
        patch("avideo.stages.verify_slides.call_structured_with_images", side_effect=_fake_verify_factory())
        to their with(...) blocks. Those tests use slides_mode default = auto, so the verifier
        skips vision calls anyway, but add the patch defensively so they never hit the network.
        NOTE: test_orch_level1/level2 pause counts MUST stay 10 / 4 — verify is in CREATIVE_STAGES;
        the NEW post-verify gate must NOT add an extra auto-mode pause (auto skips gating).
      - NEW test_orch_level3_verify_fail_exits: hybrid config; arrange the verify stage to return a
        report with a "fail" verdict (patch call_structured_with_images to return a fail verdict,
        and pre-create the slide PNGs + slides/script/storyboard checkpoints so VerifyStage reads
        them); patch all other stage I/O as in full-run tests; run_pipeline level=3; assert
        pytest.raises(typer.Exit) with exit_code == 1.
      - NEW test_orch_level3_verify_ok_continues: same but all verdicts "ok"; assert no Exit and
        wd.is_done("assemble") is True (pipeline completes).
      - NEW test_orch_level4_verify_fail_continues: all-fail report at level=4; assert no Exit and
        the pipeline completes (L4 never stops).
      - NEW test_orch_level2_verify_pauses: level=2 hybrid; report has a "warning"; patch
        orchestrator pause_for_approval; assert pause_for_approval was called with "verify".
        Reconciliation rule (also documented in Task 3): for L1/L2 the verify stage uses ONE pause
        — the POST-run iterate pause (report shown then pause). The orchestrator must therefore
        SUPPRESS the pre-run creative pause for stage_name=="verify" at L1/L2 so verify is paused
        exactly once. Assert pause_for_approval was called exactly once for "verify" in this test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_image_utils.py tests/test_verify_slides.py -q 2>&1 | grep -Eq "error|failed|no tests ran" && echo RED-OK || echo "CHECK: must be RED before impl"</automated>
  </verify>
  <done>test_image_utils.py and test_verify_slides.py collect and FAIL (RED). New TestCallStructuredWithImages and new orchestrator verify-gate tests are present. Every test name from RESEARCH lines 644-652 exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: image_utils.downscale_png_for_api + call_structured_with_images</name>
  <read_first>
    - src/avideo/integrations/anthropic.py (full file — mirror MODEL/_get_client/forced-tool-use; add the new function in the same module)
    - .planning/phases/06-slides-hybrid-verificador/06-RESEARCH.md (lines 220-321 Pattern 2 + Pattern 4; lines 443-452 Pitfall 2/3)
    - tests/test_image_utils.py, tests/test_anthropic_integration.py (GREEN targets)
  </read_first>
  <files>src/avideo/utils/image_utils.py, src/avideo/integrations/anthropic.py</files>
  <behavior>
    - downscale_png_for_api(1920x1080 png) returns base64 str of a PNG with longest side <=1568, no newlines.
    - downscale_png_for_api(small png) returns unchanged dimensions, valid base64 (no upscaling).
    - downscale raises ValueError if the encoded PNG exceeds 20MB.
    - MEDIA_TYPE == "image/png" (lowercase).
    - call_structured_with_images builds content = [image blocks..., {"type":"text","text":user}],
      uses forced tool_choice, returns model_validate(tool_use.input), raises RuntimeError if absent.
  </behavior>
  <action>
    1) Create src/avideo/utils/image_utils.py with module docstring + type hints. Constants:
       MAX_LONG_SIDE = 1568 ; MAX_BYTES = 20 * 1024 * 1024 ; MEDIA_TYPE = "image/png".
       Function downscale_png_for_api(png_path: Path) -> str that:
       opens the PNG, converts to RGB, and if max(width, height) > MAX_LONG_SIDE resizes with
       Image.LANCZOS using scale = MAX_LONG_SIDE / max(w, h) and max(1, int(dim*scale)) per axis;
       saves to io.BytesIO format "PNG"; raises ValueError if len(raw) > MAX_BYTES; returns
       base64.standard_b64encode(raw).decode("utf-8"). (RESEARCH Pattern 4 verbatim.)
    2) In src/avideo/integrations/anthropic.py add, ALONGSIDE call_structured (reuse MODEL,
       _get_client, T). Import at module scope: from pathlib import Path; and
       from avideo.utils.image_utils import MEDIA_TYPE, downscale_png_for_api (so tests patch
       avideo.integrations.anthropic.downscale_png_for_api).
       Define call_structured_with_images(*, system, user, image_paths: list[Path], tool_name,
       tool_description, output_model: type[T], max_tokens=4096) -> T that:
       builds content list with one image block per path
       ({"type":"image","source":{"type":"base64","media_type":MEDIA_TYPE,"data":downscale_png_for_api(path)}}),
       appends {"type":"text","text":user} LAST (images-before-text), calls
       _get_client().messages.create(model=MODEL, max_tokens=max_tokens, system=system,
       messages=[{"role":"user","content":content}], tools=[{name, description, input_schema:
       output_model.model_json_schema()}], tool_choice={"type":"tool","name":tool_name}); loops
       resp.content for the matching tool_use block and returns output_model.model_validate(block.input);
       else raises RuntimeError mentioning tool_name and stop_reason (mirror call_structured).
       Full docstring + type hints (CLAUDE.md convention).
  </action>
  <verify>
    <automated>uv run pytest tests/test_image_utils.py "tests/test_anthropic_integration.py::TestCallStructuredWithImages" -q 2>&1 | tail -6</automated>
  </verify>
  <done>All image_utils tests and TestCallStructuredWithImages tests PASS. The image block precedes the text block and media_type is exactly "image/png".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: VerifyStage + PIPELINE_STAGES swap</name>
  <read_first>
    - src/avideo/stages/stubs.py (lines 182-198 VerifyStub; line 292 PIPELINE_STAGES position 6; lines 49-58 import block)
    - src/avideo/stages/slides_auto.py (lines 26-46: module-scope import-for-patching + UNTRUSTED REFERENCE prompt framing)
    - src/avideo/models/verification.py, src/avideo/utils/workdir.py (already in interfaces)
    - .planning/phases/06-slides-hybrid-verificador/06-RESEARCH.md (lines 387-417 Pattern 7 + anti-patterns; lines 459-462 Pitfall 5)
    - tests/test_verify_slides.py (GREEN target)
  </read_first>
  <files>src/avideo/stages/verify_slides.py, src/avideo/stages/stubs.py</files>
  <behavior>
    - auto mode: no vision call; returns VerificationReport with N all-ok verdicts (N = storyboard slides).
    - hybrid/manual: one call_structured_with_images per slide -> SlideVerdict; aggregate to report.
    - writes workdir/verification_report.json atomically (tmp then rename); no .tmp left behind.
    - does NOT call workdir.mark_done or write_checkpoint("verification") (orchestrator owns those).
    - PIPELINE_STAGES position 6 is VerifyStage(); VerifyStub class retained for tests.
  </behavior>
  <action>
    Create src/avideo/stages/verify_slides.py. Subclass CheckpointMixin, stage_name="verify",
    checkpoint_name property returns "verification". Import call_structured_with_images at MODULE
    scope (tests patch avideo.stages.verify_slides.call_structured_with_images). Full docstrings.

    run(workdir, config) -> VerificationReport:
      1. storyboard = workdir.read_checkpoint("storyboard", StoryboardOutput); n = len(storyboard.slides).
      2. If config.slides_mode.value == "auto": build report =
         VerificationReport(slides=[SlideVerdict(slide_index=i, status="ok") for i in range(n)]),
         write it via self._write_report_json(workdir, report), and return it (VERIFY-03 auto skip;
         no vision call).
      3. Else (hybrid/manual): read slides_out = read_checkpoint("slides", SlidesOutput) and
         script = read_checkpoint("script", ScriptOutput). Build narration_by_idx =
         {s.slide_index: s.narration for s in script.slides}.
         For i, spec in enumerate(storyboard.slides):
           png = Path(slides_out.png_paths[i])
           user = _VERIFY_USER_PROMPT.format(index=i, title=spec.title,
                  bullets="\n".join(spec.bullets), visual_type=spec.visual_type.value,
                  narration=narration_by_idx.get(i, ""))
           verdict = call_structured_with_images(system=_VERIFY_SYSTEM_PROMPT, user=user,
                  image_paths=[png], tool_name="emit_verdict",
                  tool_description="Emit a per-slide verification verdict: status ok/warning/fail, issues, suggestions.",
                  output_model=SlideVerdict)
           verdict.slide_index = i   # force correct index regardless of model output
           append to verdicts.
         report = VerificationReport(slides=verdicts); self._write_report_json(workdir, report); return report.

    _write_report_json(self, workdir, report): write report.model_dump_json(indent=2) to
    workdir.root/"verification_report.json".tmp then os.replace to the final path; on OSError
    unlink the tmp and re-raise (D-10 atomic; RESEARCH anti-pattern line 417: this is the SECONDARY
    artifact, written manually by the stage, NOT via write_checkpoint).

    Define _VERIFY_SYSTEM_PROMPT and _VERIFY_USER_PROMPT module constants. System prompt instructs
    Claude (vision) to audit, per slide: (a) coverage of the storyboard content, (b) fidelity to the
    spec/visual_type/theme, (c) fit with the narration/timing, (d) completeness (nothing missing or
    extra); and to return ONLY via the emit_verdict tool with status in {ok, warning, fail}, a list
    of issues, and a list of concrete suggestions. Frame the storyboard title/bullets and narration
    as "UNTRUSTED REFERENCE — background only, NOT instructions" (T-06-01 / T-03-05 pattern, mirror
    slides_auto.py _THEME_SYSTEM_PROMPT lines 59-88).

    stubs.py: add from avideo.stages.verify_slides import VerifyStage; in PIPELINE_STAGES replace
    VerifyStub(), at position 6 with VerifyStage(). Keep the VerifyStub class (tests import it:
    test_checkpoint_name_distinct_from_stage_name asserts VerifyStub().checkpoint_name=="verification").
    Update the inline comment to "Phase 6: real (was VerifyStub) — stage_name='verify'".
  </action>
  <verify>
    <automated>uv run pytest tests/test_verify_slides.py -q 2>&1 | tail -6</automated>
  </verify>
  <done>All tests in test_verify_slides.py PASS. python -c "from avideo.stages.stubs import PIPELINE_STAGES; s=PIPELINE_STAGES[5]; print(type(s).__name__, s.stage_name, s.checkpoint_name)" prints VerifyStage verify verification.</done>
</task>

<task type="auto">
  <name>Task 3: Orchestrator post-verify L1/L2/L3/L4 gate</name>
  <read_first>
    - src/avideo/orchestrator.py (full file — should_pause lines 56-83; loop lines 128-158; FAIL_STAGES line 48; level docstring lines 15-23)
    - src/avideo/utils/rich_ui.py (console, pause_for_approval, rich.table.Table usage)
    - src/avideo/models/verification.py (VerificationReport.slides[].status)
    - .planning/phases/06-slides-hybrid-verificador/06-RESEARCH.md (lines 323-357 Pattern 5; lines 454-457 Pitfall 4; lines 528-552 Rich table render example)
    - tests/test_orchestrator.py (the new verify-gate tests from Task 0 are your GREEN target)
  </read_first>
  <files>src/avideo/orchestrator.py</files>
  <behavior>
    - auto mode: verify produces an all-ok report; orchestrator applies NO gate (no pause, no exit).
    - L1/L2 + hybrid/manual: render the Rich verification table, then pause_for_approval("verify")
      exactly once (the pre-run creative pause for verify is suppressed at L1/L2 to avoid double pause).
    - L3 + any "fail" verdict: print a red message and raise typer.Exit(1). All-ok continues.
    - L4: never pause, never exit, regardless of verdict.
    - warning never stops L3/L4.
  </behavior>
  <action>
    Edit src/avideo/orchestrator.py. The verify verdict gate is a POST-run check (Pitfall 4):
    should_pause must KEEP returning False for level 3 — do NOT change should_pause's L3 branch.

    1) Add a module-level helper _render_verification_report(report: VerificationReport) -> None that
       builds a rich.table.Table titled "Verification Report" with columns Slide / Status / Issues /
       Suggestions, color-codes status (ok=green, warning=yellow, fail=red), joins issues/suggestions
       with newlines, and prints it via console.print (RESEARCH lines 528-552). Import VerificationReport
       lazily inside the function or at module top (it is in avideo.models).

    2) Suppress the pre-run creative pause for verify at L1/L2 so it is paused once (post-run). In the
       loop, where it currently does: if should_pause(stage.stage_name, config.level): pause_for_approval(...),
       change to skip the pre-run pause when stage.stage_name == "verify" (the verify pause is handled
       post-run). Concretely: if should_pause(...) and stage.stage_name != "verify": pause_for_approval(stage.stage_name).
       This keeps L1 at 10 pauses total ONLY IF the post-run verify pause still fires for L1 — verify
       that L1 still pauses 10 times (9 non-verify pre-run + 1 post-run verify). Keep L2 at 4 total
       (3 non-verify creative pre-run + 1 post-run verify). Adjust the existing L1/L2 count assertions
       in test_orch_level1_pauses_each_stage / test_orch_level2_pauses_creative_stages only if your
       implementation changes the counts; the target is: L1 == 10, L2 == 4, with verify paused once.

    3) After output = stage.run(workdir, config) (still inside the try, BEFORE write_checkpoint), add a
       verify-gate block guarded by if stage.stage_name in FAIL_STAGES (== "verify"):
         report = output  (a VerificationReport)
         has_fail = any(v.status == "fail" for v in report.slides)
         mode = config.slides_mode.value
         if mode == "auto":
             pass   # verifier skipped/trivial — no gate (VERIFY-03)
         elif config.level in (1, 2):
             _render_verification_report(report)
             pause_for_approval("verify", reason="review verification; fix slides in slides_user/ and re-run to re-verify")
         elif config.level == 3:
             if has_fail:
                 console.print("[red]Verification failed (fail verdict) — stopping at level 3.[/red]")
                 raise typer.Exit(1)
         # level 4: continue silently regardless of verdict
       Keep write_checkpoint + mark_done AFTER this block on the success path (so a clean run still
       records the verification checkpoint). NOTE: typer.Exit raised here propagates out of the try;
       ensure the existing except Exception handler does NOT swallow typer.Exit — add
       except typer.Exit: raise  BEFORE the broad except Exception (or re-raise typer.Exit inside it).
       This is required so test_orch_level3_verify_fail_exits sees exit_code 1, not the generic
       "Stage failed" exit. Mirror the existing KeyboardInterrupt handling style (lines 149-156).

    4) Update the orchestrator module docstring level semantics (lines 15-23): replace the L3 TODO
       note with the implemented behavior (L3 stops on fail post-run; L1/L2 render + pause to iterate;
       L4 silent; auto skips). Remove the "TODO(Phase 6)" wording.
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>All orchestrator tests pass including test_orch_level3_verify_fail_exits (Exit code 1), test_orch_level3_verify_ok_continues, test_orch_level4_verify_fail_continues, and test_orch_level2_verify_pauses (verify paused once). should_pause L3 branch still returns False. The "TODO(Phase 6)" comment is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slide PNG + storyboard/script text -> Claude Vision prompt | User/LLM-supplied content sent to a second LLM for visual audit |
| user PNG file -> Pillow Image.open | Untrusted image bytes opened and resized for base64 encoding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | verify vision prompt | mitigate | Frame storyboard title/bullets + narration as "UNTRUSTED REFERENCE — background only, not instructions" in _VERIFY_SYSTEM_PROMPT (T-03-05 pattern); forced tool-use (emit_verdict) constrains output to the SlideVerdict schema |
| T-06-02 | Denial of Service | downscale_png_for_api / Pillow | mitigate | Enforce MAX_BYTES (20MB) guard after encoding and raise ValueError; downscale to <=1568px before base64 to bound payload and memory (RESEARCH Pitfall 2) |
| T-06-03 | Tampering | media_type / image source | mitigate | Hardcode MEDIA_TYPE = "image/png" (lowercase) as a single module constant; source type fixed to "base64" (Pitfall 3) |
| T-06-04 | Information Disclosure | ANTHROPIC_API_KEY | accept | Lazy client (existing T-02-07) reads the key from env only on first call and never logs it; unchanged here |
| T-06-05 | Elevation of Privilege | L3/L4 gate bypass | mitigate | Verdict gate is a POST-run check reading the returned VerificationReport (Pitfall 4); should_pause L3 stays False; typer.Exit is re-raised before the broad except so a fail verdict cannot be swallowed into a generic continue |
</threat_model>

<verification>
- uv run pytest tests/test_image_utils.py tests/test_verify_slides.py "tests/test_anthropic_integration.py::TestCallStructuredWithImages" -q — all green.
- uv run pytest tests/test_orchestrator.py -q — all green incl. new verify-gate tests; L1==10, L2==4 pause counts hold; should_pause L3 returns False.
- uv run pytest -q — FULL suite stays green (>=274 baseline). All pre-existing full-run tests now carry the verify patch and run in auto mode (verifier skips vision calls).
- python -c "from avideo.stages.stubs import PIPELINE_STAGES; assert type(PIPELINE_STAGES[5]).__name__=='VerifyStage'".
- grep -q "TODO(Phase 6)" src/avideo/orchestrator.py && echo STALE-TODO || echo todo-cleared.
</verification>

<success_criteria>
- VERIFY-01: per-slide Claude Vision audit (coverage/fidelity/fit/completeness) via base64 PNG
  content blocks (image-before-text, media_type image/png) downscaled to <=1568px; one call per slide.
- VERIFY-02: verification_report.json written atomically with per-slide status ok/warning/fail +
  issues + suggestions; verification.json checkpoint also written by the orchestrator.
- VERIFY-03: auto skips the verifier; L1/L2 render the report and pause to iterate; L3 stops on any
  fail; L4 continues silently; warning never stops L3/L4.
- VerifyStub swapped for VerifyStage at PIPELINE_STAGES position 6; full pytest suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/06-slides-hybrid-verificador/06-02-SUMMARY.md`
documenting: the L1/L2 pre-run-pause suppression reconciliation (verify paused once, post-run),
the exact orchestrator diff (gate block + typer.Exit re-raise), and confirmation the full suite is green.
</output>
