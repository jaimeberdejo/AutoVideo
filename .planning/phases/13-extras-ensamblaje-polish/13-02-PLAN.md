---
phase: 13-extras-ensamblaje-polish
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/avideo/ui/pipeline_ops.py
  - src/avideo/ui/pages/phase_5_extras.py
autonomous: true
requirements:
  - EXT-01
must_haves:
  truths:
    - "write_uploaded_music saves uploaded bytes to workdir/music/<filename> and raises ValueError on path traversal"
    - "extras_to_run_config maps the five widget values into a RunConfig-compatible kwargs dict"
    - "read_qa_report returns a QAReport when qa_report.json is valid, None otherwise"
    - "phase_5_extras.render(workdir) shows burn_subs toggle, music upload + volume slider + preview, crossfade slider; approving merges config and returns True"
    - "The 9 RED tests in test_extras_pipeline_ops.py all pass (GREEN)"
  artifacts:
    - path: "src/avideo/ui/pipeline_ops.py"
      provides: "Three new helpers: write_uploaded_music, extras_to_run_config, read_qa_report"
      contains: "write_uploaded_music"
    - path: "src/avideo/ui/pages/phase_5_extras.py"
      provides: "Real Fase 5 wizard page — EXT-01"
      contains: "burn_subs"
  key_links:
    - from: "phase_5_extras.py"
      to: "avideo.ui.pipeline_ops.extras_to_run_config"
      via: "lazy import inside render()"
      pattern: "extras_to_run_config"
    - from: "phase_5_extras.py"
      to: "st.session_state[\"run_config\"]"
      via: "dict merge before Approve gate"
      pattern: "session_state.*run_config"
---

<objective>
Turn 9 RED tests GREEN by adding three helpers to pipeline_ops.py, then replace the phase_5_extras.py placeholder with the real Fase 5 wizard page.

Purpose: Deliver EXT-01 — user configures burned subtitles, background music (file upload + volume), and crossfade; config is persisted into session_state["run_config"]; gate is immediate (config-only page, no long stage).

Output: pipeline_ops.py extended with 3 helpers; phase_5_extras.py replaced with real page; 390 tests passing.
</objective>

<execution_context>
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.planning/PROJECT.md
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.planning/ROADMAP.md
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.planning/phases/13-extras-ensamblaje-polish/13-CONTEXT.md
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.planning/phases/11-guion-slides-pages/11-02-SUMMARY.md
@/Users/jaimeberdejosanchez/projects/auto-video-narrado/.planning/phases/12-voz-page/12-02-SUMMARY.md
</context>

<interfaces>
<!-- Existing pipeline_ops.py module-scope import (DO NOT CHANGE — it is the mock seam): -->
```python
from avideo.ui.bridge import run_stage  # noqa: E402 — module-level mock seam for tests
```

<!-- New helpers to append after "Voice helpers (Phase 12)" section: -->
```python
# ---------------------------------------------------------------------------
# Extras helpers (Phase 13)
# ---------------------------------------------------------------------------

def write_uploaded_music(workdir: "WorkdirManager", filename: str, data: bytes) -> Path:
    # Path traversal guard: raise ValueError on "/" "\\" ".." prefix
    # dest = workdir.root / "music" / filename
    # mkdir exist_ok=True; write_bytes; return dest

def extras_to_run_config(
    burn_subs: bool,
    bg_music_path: "Optional[Path]",
    bg_music_volume: float,
    bg_music_fade_out_s: float,
    crossfade_seconds: float,
) -> dict:
    # Returns dict with the five RunConfig field names as keys
    # Used by phase_5_extras.py to update session_state["run_config"]

def read_qa_report(workdir: "WorkdirManager") -> "Optional[QAReport]":
    # Reads workdir.root / "qa_report.json"
    # Returns QAReport if parseable; None on FileNotFoundError or ValidationError or any exception
```

<!-- RunConfig fields this maps to (from src/avideo/models/config.py): -->
```python
crossfade_seconds: float = 0.5
bg_music_path: Optional[Path] = None
bg_music_volume: float = 0.12      # linear 0.0–1.0
bg_music_fade_out_s: float = 3.0
burn_subs: bool = False
```

<!-- phase_4_voz.py page pattern to follow for phase_5_extras.py: -->
<!-- - render(workdir) -> bool signature, lazy imports inside body -->
<!-- - Build rc_dict from st.session_state.get("run_config", {}) at top -->
<!-- - Persist updated rc_dict back into st.session_state["run_config"] -->
<!-- - Return gate_met boolean (True when user clicks Aprobar extras) -->
<!-- - No long stage: gate is immediate (just config widgets) -->
<!-- - Approving with no extras selected is VALID -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend pipeline_ops.py with three Extras helpers</name>
  <files>src/avideo/ui/pipeline_ops.py</files>
  <read_first>
    - src/avideo/ui/pipeline_ops.py (full file — understand existing structure and mock seam)
    - tests/test_extras_pipeline_ops.py (exact contracts the helpers must satisfy)
    - src/avideo/models/assembly.py (QAReport model fields)
    - src/avideo/models/config.py lines 84-104 (bg_music_* and crossfade_seconds fields)
  </read_first>
  <action>
Append a new "Extras helpers (Phase 13)" section to src/avideo/ui/pipeline_ops.py AFTER the "Voice helpers (Phase 12)" section and BEFORE the "Badge mapping" section. Add three functions:

1. write_uploaded_music(workdir, filename, data) -> Path
   - Path traversal guard: if "/" in filename or "\\" in filename or filename.startswith(".."):
       raise ValueError(f"Unsafe filename rejected (path traversal attempt): {filename!r}")
   - dest_dir = workdir.root / "music"; dest_dir.mkdir(exist_ok=True)
   - dest = dest_dir / filename; dest.write_bytes(data); return dest
   - Docstring: mirrors write_uploaded_audio but targets workdir/music/ (T-13-02-01)

2. extras_to_run_config(burn_subs, bg_music_path, bg_music_volume, bg_music_fade_out_s, crossfade_seconds) -> dict
   - Returns {"burn_subs": burn_subs, "bg_music_path": bg_music_path, "bg_music_volume": bg_music_volume, "bg_music_fade_out_s": bg_music_fade_out_s, "crossfade_seconds": crossfade_seconds}
   - Pure function, no I/O, no Streamlit.
   - Docstring: "Maps Fase 5 widget values to RunConfig kwargs. Returns a dict safe to merge into session_state['run_config']."

3. read_qa_report(workdir) -> Optional["QAReport"]
   - qa_path = workdir.root / "qa_report.json"
   - Wrap in try/except Exception: return None on any error (FileNotFoundError, ValidationError, JSONDecodeError)
   - On success: import QAReport from avideo.models.assembly (lazy import inside function body); return QAReport.model_validate_json(qa_path.read_text(encoding="utf-8"))
   - Docstring: "Read qa_report.json from workdir; returns None when absent or invalid (T-13-02-02)."

After adding, add TYPE_CHECKING import for QAReport in the TYPE_CHECKING block at top:
  from avideo.models.assembly import QAReport

Verify no Streamlit import is introduced. Run the 9 RED tests after writing to confirm they pass.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && python -m pytest tests/test_extras_pipeline_ops.py -q 2>&1 | tail -4</automated>
  </verify>
  <done>
    - `grep -c "write_uploaded_music\|extras_to_run_config\|read_qa_report" src/avideo/ui/pipeline_ops.py` returns at least 3 (one per function definition)
    - `grep -v "^#" src/avideo/ui/pipeline_ops.py | grep -c "import streamlit"` returns 0
    - All 9 tests in test_extras_pipeline_ops.py pass
    - Full suite: `python -m pytest -q 2>&1 | tail -1` shows "390 passed"
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace phase_5_extras.py with real Fase 5 Extras page</name>
  <files>src/avideo/ui/pages/phase_5_extras.py</files>
  <read_first>
    - src/avideo/ui/pages/phase_5_extras.py (current placeholder — read full file)
    - src/avideo/ui/pages/phase_4_voz.py (page pattern: render(), rc_dict build, session_state persist, gate)
    - src/avideo/ui/pages/phase_3_slides.py (for st.file_uploader + st.audio preview pattern)
    - src/avideo/ui/pipeline_ops.py (write_uploaded_music, extras_to_run_config signatures)
    - .planning/phases/13-extras-ensamblaje-polish/13-CONTEXT.md (decisions section)
  </read_first>
  <action>
Replace phase_5_extras.py completely. Keep the module docstring updated and preserve the render(workdir: WorkdirManager) -> bool signature.

Module docstring: document that this replaces the Phase 9 placeholder and implements EXT-01.

Implementation of render(workdir):

1. Build rc_dict from st.session_state.get("run_config", {}); ensure "bullets" key present if missing (mirror phase_4_voz.py pattern).

2. SECTION: Subtítulos
   - st.subheader("Subtítulos")
   - burn_subs = st.toggle("Quemar subtítulos en el vídeo", key="ext_burn_subs", value=bool(rc_dict.get("burn_subs", False)))
   - If burn_subs: st.info("Los subtítulos se quemarán permanentemente en el vídeo.")

3. SECTION: Música de fondo
   - st.subheader("Música de fondo")
   - uploaded_music = st.file_uploader("Sube una pista de música (MP3/WAV)", key="ext_music_upload", type=["mp3", "wav"])
   - If uploaded_music is not None:
     * suffix = Path(uploaded_music.name).suffix or ".mp3"; filename = f"bg_music{suffix}"
     * Lazy import write_uploaded_music; call write_uploaded_music(workdir, filename, uploaded_music.read())
     * music_path = workdir.root / "music" / filename
     * Persist to rc_dict: rc_dict["bg_music_path"] = str(music_path)
     * st.success(f"Música subida: {uploaded_music.name}")
   - Show st.audio preview if music exists on disk:
     * music_on_disk_str = rc_dict.get("bg_music_path") — check Path(music_on_disk_str).exists() if not None
     * If exists: st.audio(music_on_disk_str, format="audio/mp3")
   - bg_music_volume = st.slider("Volumen de la música", min_value=0.0, max_value=1.0, step=0.01, value=float(rc_dict.get("bg_music_volume", 0.12)), key="ext_music_volume", help="0 = silencio, 1 = volumen máximo. 0.12 (~-18 dBFS) recomendado.")
   - bg_music_fade_out_s = st.slider("Fundido final de la música (segundos)", min_value=0.0, max_value=10.0, step=0.5, value=float(rc_dict.get("bg_music_fade_out_s", 3.0)), key="ext_music_fade")

4. SECTION: Transiciones
   - st.subheader("Transiciones")
   - crossfade_seconds = st.slider("Crossfade entre diapositivas (segundos)", min_value=0.0, max_value=3.0, step=0.1, value=float(rc_dict.get("crossfade_seconds", 0.5)), key="ext_crossfade", help="0 = corte duro, 0.5 = suave por defecto.")

5. Resolve bg_music_path as Optional[Path]:
   - bg_music_path_val = Path(rc_dict["bg_music_path"]) if rc_dict.get("bg_music_path") else None

6. Build extras kwargs via lazy import:
   from avideo.ui.pipeline_ops import extras_to_run_config
   extras_kwargs = extras_to_run_config(burn_subs=burn_subs, bg_music_path=bg_music_path_val, bg_music_volume=bg_music_volume, bg_music_fade_out_s=bg_music_fade_out_s, crossfade_seconds=crossfade_seconds)

7. Merge extras_kwargs into rc_dict and persist:
   rc_dict.update({k: (str(v) if isinstance(v, Path) else v) for k, v in extras_kwargs.items()})
   st.session_state["run_config"] = rc_dict

8. Gate — immediate (config-only, no stage to run):
   - st.divider()
   - st.info("Los extras son opcionales. Puedes continuar sin seleccionar ninguno.") if no music and not burn_subs
   - gate_met = st.session_state.get("extras_approved", False)
   - if st.button("Aprobar extras y continuar", key="btn_approve_extras", type="primary"):
       st.session_state["extras_approved"] = True; gate_met = True; st.rerun()
   - return gate_met

All stage/model imports inside the function body (lazy). Keep NO Streamlit import at module level other than the `import streamlit as st` line at the top.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && python -c "import importlib; m = importlib.import_module('avideo.ui.pages.phase_5_extras'); print('ok') if callable(m.render) else print('FAIL')" 2>&1</automated>
  </verify>
  <done>
    - `grep -c "burn_subs" src/avideo/ui/pages/phase_5_extras.py` returns >= 1
    - `grep -c "bg_music_volume" src/avideo/ui/pages/phase_5_extras.py` returns >= 1
    - `grep -c "crossfade_seconds" src/avideo/ui/pages/phase_5_extras.py` returns >= 1
    - `grep -c "extras_to_run_config" src/avideo/ui/pages/phase_5_extras.py` returns >= 1
    - `grep -c "st.file_uploader" src/avideo/ui/pages/phase_5_extras.py` returns >= 1
    - `python -m pytest -q 2>&1 | tail -1` shows "390 passed"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → phase_5_extras.py | User uploads music file via st.file_uploader |
| pipeline_ops.write_uploaded_music | Writes to workdir/music/ — path traversal attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | write_uploaded_music path guard | mitigate | ValueError raised on '/', '\\', '..' prefix before any filesystem write — identical to T-12-02-01 pattern |
| T-13-02-02 | Denial of Service | read_qa_report on corrupt file | mitigate | try/except Exception returns None; never propagates parse errors to the wizard |
| T-13-02-03 | Information Disclosure | bg_music_path stored as str in session_state | accept | session_state is in-process memory; single-user local app; no remote storage |
| T-13-02-04 | Tampering | music file written to workdir/music/ | mitigate | filename sanitised by write_uploaded_music guard; content not executed, only passed to FFmpeg subprocess as file path |
</threat_model>

<verification>
- `python -m pytest tests/test_extras_pipeline_ops.py -v 2>&1 | grep -E "PASSED|FAILED"` — all 9 PASSED
- `python -m pytest -q 2>&1 | tail -1` — 390 passed, 0 failed
- `grep -c "write_uploaded_music" src/avideo/ui/pipeline_ops.py` — returns >= 1
- `grep -c "burn_subs" src/avideo/ui/pages/phase_5_extras.py` — returns >= 1
- `grep -c "bg_music_volume" src/avideo/ui/pages/phase_5_extras.py` — returns >= 1
</verification>

<success_criteria>
- All 9 RED tests green (390 total passing)
- phase_5_extras.py implements burn_subs toggle, music upload + st.audio preview + volume slider, crossfade slider, immediate approve gate
- No Streamlit import in pipeline_ops.py
- render(workdir)->bool signature preserved
</success_criteria>

<output>
After completion, create `.planning/phases/13-extras-ensamblaje-polish/13-02-SUMMARY.md`
</output>
