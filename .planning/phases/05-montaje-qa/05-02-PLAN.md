---
phase: 05-montaje-qa
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/test_assemble.py
  - src/avideo/models/assembly.py
  - src/avideo/integrations/ffmpeg.py
  - src/avideo/stages/qa.py
  - src/avideo/stages/assemble.py
  - src/avideo/stages/stubs.py
autonomous: true
requirements: [QA-01, QA-02]
must_haves:
  truths:
    - "parse_loudnorm_json extracts the LAST {...} block from noisy ffmpeg stderr and returns measured_I/TP/LRA/thresh + offset as floats (raises on garbage)"
    - "Two-pass loudnorm: pass 1 measures (print_format=json on stderr), pass 2 applies measured_* + offset with linear=true, re-encodes audio, copies video, AND re-adds -movflags +faststart"
    - "QAReport carries target_seconds, actual_seconds, duration_deviation, measured_lufs and normalized_lufs"
    - "duration_deviation = actual_seconds - target_seconds (RunConfig.duration is the target); compared with tolerance, never strict equality"
    - "AssembleStage runs QA as a sub-step after the encode, writes workdir/qa_report.json, attaches QAReport to AssemblyOutput, and shows a Rich QA table"
    - "PIPELINE_STAGES replaces AssembleStub() with AssembleStage(), preserving stage_name=='assemble' and checkpoint=='assembly'"
    - "QA is idempotent within the assemble checkpoint: a resumed/redone run does not re-run loudnorm if output.mp4 + qa_report.json already exist"
  artifacts:
    - path: "src/avideo/stages/qa.py"
      provides: "Pure QA logic: parse_loudnorm_json, loudnorm pass-1/pass-2 arg builders, duration_deviation, build_qa_report"
      contains: "def parse_loudnorm_json"
    - path: "src/avideo/models/assembly.py"
      provides: "Extended QAReport with measured_lufs + normalized_lufs"
      contains: "measured_lufs"
    - path: "src/avideo/stages/stubs.py"
      provides: "PIPELINE_STAGES with AssembleStage() swapped in for AssembleStub()"
      contains: "AssembleStage()"
  key_links:
    - from: "src/avideo/stages/assemble.py"
      to: "src/avideo/stages/qa.py"
      via: "imports two-pass loudnorm builders + parse + build_qa_report as a sub-step"
      pattern: "from avideo.stages.qa import"
    - from: "src/avideo/stages/assemble.py"
      to: "workdir/qa_report.json"
      via: "writes the QAReport JSON side artifact"
      pattern: "qa_report.json"
    - from: "src/avideo/stages/stubs.py"
      to: "src/avideo/stages/assemble.py"
      via: "PIPELINE_STAGES swap AssembleStub -> AssembleStage"
      pattern: "AssembleStage()"
---

<objective>
Layer QA onto the assembly: implement two-pass EBU R128 `loudnorm` (measure → apply, landing ≈ -16 LUFS), the duration-deviation report, and the `QAReport` JSON + Rich table — wired as a SUB-STEP inside `AssembleStage.run` (per 05-RESEARCH Open Question 1: one checkpoint, one idempotence boundary). Then swap the `AssembleStub` for the real `AssembleStage` in `PIPELINE_STAGES`, completing Phase 5.

Purpose: Satisfy QA-01 (real-vs-target duration deviation) and QA-02 (two-pass loudnorm measure+apply with a report). Folding QA into assemble keeps a single `assembly` checkpoint and avoids touching the orchestrator's flat stage list beyond the stub swap.

Output: `src/avideo/stages/qa.py` (pure logic), extended `QAReport` in `models/assembly.py`, loudnorm arg builders in `integrations/ffmpeg.py`, QA sub-step + Rich table in `stages/assemble.py`, `workdir/qa_report.json`, and the `PIPELINE_STAGES` swap in `stubs.py`. Additional tests appended to `tests/test_assemble.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-montaje-qa/05-CONTEXT.md
@.planning/phases/05-montaje-qa/05-RESEARCH.md
@.planning/phases/05-montaje-qa/05-VALIDATION.md
@CLAUDE.md

Plan 05-01 produced these (read its SUMMARY + the files it created):
@.planning/phases/05-montaje-qa/05-01-SUMMARY.md
@src/avideo/integrations/ffmpeg.py
@src/avideo/stages/assemble.py
@src/avideo/models/assembly.py
@src/avideo/models/config.py
@src/avideo/stages/stubs.py
@src/avideo/utils/rich_ui.py
@tests/test_assemble.py
@tests/conftest.py

<interfaces>
<!-- Contracts the executor needs. Use directly. -->

QAReport / AssemblyOutput (src/avideo/models/assembly.py) — EXTEND QAReport here:
```python
class QAReport(BaseModel):
    target_seconds: float
    actual_seconds: float
    duration_deviation: float
    lufs: Optional[float] = None        # existing single field
    # ADD: measured_lufs: Optional[float] = None      # pre-normalization (pass-1 input_i)
    # ADD: normalized_lufs: Optional[float] = None     # post-normalization (re-measured / pass-2 output_i)
class AssemblyOutput(BaseModel):
    output_path: str
    qa: Optional[QAReport] = None       # attach the QAReport here in AssembleStage
```

From integrations/ffmpeg.py (plan 05-01 — already present):
```python
def run_ffmpeg(args: list[str]) -> subprocess.CompletedProcess[str]   # raises RuntimeError on nonzero exit
def probe_duration(path: str) -> float                                # ffprobe format=duration
def ffmpeg_available() -> bool
# import subprocess  (patchable as avideo.integrations.ffmpeg.subprocess.run)
```

AssembleStage (src/avideo/stages/assemble.py — plan 05-01): stage_name="assemble", checkpoint="assembly";
run() currently returns AssemblyOutput(output_path=..., qa=None). This plan adds the QA sub-step before the return.

WorkdirManager (src/avideo/utils/workdir.py): root; output.mp4 at workdir.root/"output.mp4"; qa_report.json side artifact at workdir.root/"qa_report.json".

Rich UI (src/avideo/utils/rich_ui.py): `from avideo.utils.rich_ui import console`; build a `rich.table.Table` for the QA display (D-08), print via `console.print(table)`.

RunConfig (src/avideo/models/config.py — plan 05-01): `duration: int` (QA TARGET, QA-01); `target_lufs: float = -16.0` (QA-02 loudnorm target).

PIPELINE_STAGES (src/avideo/stages/stubs.py) — current last entry:
```python
PIPELINE_STAGES: list = [ ..., SubtitlesStage(), AssembleStub() ]   # swap AssembleStub() -> AssembleStage()
```
AssembleStub class is RETAINED (tests import it directly — mirror the Phase-4 swap pattern in 04-03-SUMMARY).

Canned loudnorm stderr fixture `loudnorm_pass1_stderr` was added to tests/conftest.py in plan 05-01 Task 1.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QAReport extension + stages/qa.py (pure loudnorm parse + arg builders + deviation)</name>
  <files>src/avideo/models/assembly.py, src/avideo/integrations/ffmpeg.py, src/avideo/stages/qa.py, tests/test_assemble.py</files>
  <behavior>
    - `parse_loudnorm_json(loudnorm_pass1_stderr)` → dict with `measured_I≈-22.01`, `measured_TP`, `measured_LRA`, `measured_thresh`, `offset` (all floats); also exposes `input_i` (measured loudness)
    - `parse_loudnorm_json("garbage no braces")` raises ValueError
    - `parse_loudnorm_json('{"oops": 1}')` raises (missing fields) — KeyError/ValueError
    - `duration_deviation(actual=8.533, target=8.5)` ≈ 0.033; sign = actual - target
    - `loudnorm_pass1_args(input_mp4, target_lufs=-16.0)` contains `loudnorm=` , `print_format=json`, `-f null`
    - `loudnorm_pass2_args(input, output, measured, target_lufs)` contains `measured_I=`, `offset=`, `linear=true`, `-c:v copy`, AND `-movflags +faststart` (Pitfall 2)
    - `build_qa_report(target, actual, measured_lufs, normalized_lufs)` → QAReport with correct deviation
  </behavior>
  <read_first>
    - src/avideo/models/assembly.py (current QAReport/AssemblyOutput — extend, do not break existing `lufs` field)
    - .planning/phases/05-montaje-qa/05-RESEARCH.md Pattern 5 (VERIFIED two-pass loudnorm, exact field names input_i/input_tp/input_lra/input_thresh/target_offset/output_i), "Code Examples" parse_loudnorm_json, Pitfall 2 (faststart after -c:v copy), Pitfall 4 (parse last {...} block), Pitfall 7 (deviation tolerance)
    - src/avideo/integrations/ffmpeg.py (plan 05-01 — append the loudnorm arg builders here next to the other pure builders)
    - tests/conftest.py (the `loudnorm_pass1_stderr` fixture added in 05-01 Task 1)
  </read_first>
  <action>
    (A) Extend `QAReport` in `src/avideo/models/assembly.py`:
      - add `measured_lufs: Optional[float] = None` and `normalized_lufs: Optional[float] = None`.
      - keep the existing `lufs` field (do not remove — back-compat with the stub-era checkpoint) or alias it to normalized_lufs; minimum requirement is the two new fields exist (Assumption A6).

    (B) Append the loudnorm ARG builders + PURE parse to `src/avideo/integrations/ffmpeg.py` (they shell out via the existing run_ffmpeg; the parse is pure):
      - `loudnorm_pass1_args(input_path: str, target_lufs: float = -16.0) -> list[str]` — `["ffmpeg","-hide_banner","-i",input,"-af",f"loudnorm=I={target_lufs}:TP=-1.5:LRA=11:print_format=json","-f","null","-"]` (Pattern 5 pass 1).
      - `parse_loudnorm_json(stderr: str) -> dict[str, float]` — EXACT verified shape from 05-RESEARCH "Code Examples": `re.findall(r"\{[^{}]*\}", stderr, re.DOTALL)`; raise ValueError if no block; `json.loads(blocks[-1])`; return `{"measured_I": float(raw["input_i"]), "measured_TP": float(raw["input_tp"]), "measured_LRA": float(raw["input_lra"]), "measured_thresh": float(raw["input_thresh"]), "offset": float(raw["target_offset"]), "input_i": float(raw["input_i"])}`. (Pitfall 4 — last block; values are strings → float().)
      - `loudnorm_pass2_args(input_path: str, output_path: str, measured: dict[str, float], target_lufs: float = -16.0) -> list[str]` — `-af loudnorm=I={target}:TP=-1.5:LRA=11:measured_I=...:measured_TP=...:measured_LRA=...:measured_thresh=...:offset=...:linear=true:print_format=json`, then `-c:v copy -c:a aac -b:a 192k -ar 48000 -movflags +faststart` and output LAST (Pattern 5 pass 2 — re-add +faststart, Pitfall 2). Add `-y`.
      (`import re`/`import json` at module top if not already present.)

    (C) Create `src/avideo/stages/qa.py` as a PURE-LOGIC module (NOT a StageProtocol stage — imported by AssembleStage per Open Question 1):
      - `duration_deviation(actual_seconds: float, target_seconds: float) -> float` — `actual_seconds - target_seconds`.
      - `build_qa_report(target_seconds, actual_seconds, measured_lufs, normalized_lufs) -> QAReport` — constructs the QAReport (deviation = duration_deviation(...)). Import QAReport from models.assembly.
      - (Optional thin helper) `within_tolerance(deviation, tol=0.5) -> bool` for the Rich table flagging (Pitfall 7 — never strict equality).
      Keep this module free of subprocess calls (running loudnorm uses `integrations/ffmpeg.run_ffmpeg`; this module only parses/computes/builds the model).

    (D) Append tests to `tests/test_assemble.py` matching the `loudnorm`/`deviation` `-k` selectors from 05-VALIDATION:
      - `test_parse_loudnorm_json_*` (uses the `loudnorm_pass1_stderr` fixture; floats; ValueError on garbage; raises on missing fields).
      - `test_loudnorm_pass2_args_has_faststart` (asserts `+faststart` AND `-c:v copy` AND `linear=true` present — Pitfall 2).
      - `test_deviation_*` (actual - target; tolerance flag).
      - `test_build_qa_report_*` (QAReport has measured_lufs + normalized_lufs + deviation).
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble.py -k "loudnorm or deviation or qa_report" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'measured_lufs' src/avideo/models/assembly.py` succeeds
    - `grep -q 'normalized_lufs' src/avideo/models/assembly.py` succeeds
    - `grep -q 'def parse_loudnorm_json' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def loudnorm_pass1_args' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def loudnorm_pass2_args' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'linear=true' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'print_format=json' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def duration_deviation' src/avideo/stages/qa.py` succeeds
    - `grep -q 'def build_qa_report' src/avideo/stages/qa.py` succeeds
    - In loudnorm_pass2_args, both `+faststart` and `-c:v copy` appear (re-added faststart — Pitfall 2): `grep -q 'faststart' src/avideo/integrations/ffmpeg.py`
    - `uv run pytest tests/test_assemble.py -k "loudnorm or deviation or qa_report" -x -q` passes
  </acceptance_criteria>
  <done>QAReport carries measured_lufs + normalized_lufs; integrations/ffmpeg.py has loudnorm pass-1/pass-2 arg builders + a pure parse_loudnorm_json (last-block, float-cast); stages/qa.py provides duration_deviation + build_qa_report. The loudnorm/deviation/qa_report tests are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wire QA sub-step into AssembleStage + Rich table + qa_report.json + PIPELINE_STAGES swap</name>
  <files>src/avideo/stages/assemble.py, src/avideo/stages/stubs.py, tests/test_assemble.py</files>
  <behavior>
    - After the encode, AssembleStage runs loudnorm pass-1 (on output.mp4) → parse → pass-2 → re-measure → builds QAReport(target=config.duration, actual=probe_duration(output.mp4), measured_lufs, normalized_lufs)
    - writes `workdir/qa_report.json` (QAReport.model_dump_json) atomically; attaches qa to the returned AssemblyOutput
    - prints a Rich table of the QA metrics (D-08)
    - QA sub-step is idempotent within the assemble checkpoint: if output.mp4 + qa_report.json exist, skip re-running loudnorm
    - PIPELINE_STAGES last entry is AssembleStage() (not AssembleStub()); stage_name=="assemble", checkpoint=="assembly" preserved
    - full pipeline end-to-end test (mocked ffmpeg/probe) writes assembly.json + qa_report.json and the assemble done-marker
  </behavior>
  <read_first>
    - src/avideo/stages/assemble.py (plan 05-01 — the run() to extend with the QA sub-step before return)
    - src/avideo/stages/qa.py + the loudnorm builders in integrations/ffmpeg.py (Task 1)
    - src/avideo/utils/rich_ui.py (console; rich.table.Table import pattern)
    - src/avideo/stages/stubs.py (PIPELINE_STAGES — the swap target; AssembleStub retained; mirror the 04-03 swap pattern)
    - .planning/phases/04-voz-subtitulos/04-03-SUMMARY.md (Task 3 — exact PIPELINE_STAGES swap pattern: import real stage, replace in list, keep stub class, patch orchestrator end-to-end tests if they invoke the real run)
    - .planning/phases/05-montaje-qa/05-RESEARCH.md Pattern 5 + Pitfall 2 + Open Question 1 (QA-as-substep) + Pitfall 6 (burn re-encode then copy-video loudnorm)
  </read_first>
  <action>
    (A) Extend `AssembleStage.run` in `src/avideo/stages/assemble.py` with a QA SUB-STEP after the atomic publish of output.mp4 and BEFORE the return:
      1. IDEMPOTENCE: if `output.mp4` AND `qa_report.json` already exist (and the top-level idempotence guard from 05-01 already returns early when assembly.json exists), do not re-run loudnorm — read and attach the existing QAReport. (Single idempotence boundary, D-10.)
      2. Two-pass loudnorm (QA-02): run `loudnorm_pass1_args(output.mp4, config.target_lufs)` via `run_ffmpeg`; `parse_loudnorm_json(proc.stderr)` → measured dict. Run `loudnorm_pass2_args(output.mp4, tmp2, measured, config.target_lufs)` writing to `workdir.root/"output.mp4.norm.tmp"`, then `os.replace` over output.mp4 (atomic; final mp4 has +faststart — Pitfall 2). Optionally re-measure (pass-1 again) for `normalized_lufs`, or use pass-2 reported `output_i`; store `measured_lufs = measured["input_i"]`, `normalized_lufs ≈ target` (both ≈ -16 per Pattern 5).
      3. Ordering when burn AND loudnorm both active (Pitfall 6): burn already happened during the 05-01 assembly encode (re-encoded video), so loudnorm pass-2 still `-c:v copy` the already-burned video — no special handling needed beyond running QA after assembly.
      4. QA report (QA-01): `actual = probe_duration(output.mp4)`; `target = float(config.duration)`; `report = build_qa_report(target, actual, measured_lufs, normalized_lufs)`.
      5. Write `workdir/qa_report.json` atomically (write to `qa_report.json.tmp`, `os.replace`) using `report.model_dump_json(indent=2)`.
      6. Rich table (D-08): build a `rich.table.Table` with rows Target/Actual/Deviation(s) and Measured LUFS / Normalized LUFS; `console.print(table)`. Flag deviation with within_tolerance (Pitfall 7 — no strict equality).
      7. Return `AssemblyOutput(output_path=str(output.mp4), qa=report)`.
      Guard each ffmpeg call with the existing clean-error behavior of run_ffmpeg (D-04). If `ffmpeg_available()` is False the smoke path is skipped by tests; production assumes ffmpeg present (documented for Phase 7 Docker).

    (B) PIPELINE_STAGES swap in `src/avideo/stages/stubs.py` (mirror 04-03):
      - add `from avideo.stages.assemble import AssembleStage` to the real-stage imports block.
      - in `PIPELINE_STAGES`, replace `AssembleStub()` with `AssembleStage()  # Phase 5: real (was AssembleStub) — stage_name='assemble'`.
      - KEEP the `AssembleStub` class definition (tests import it directly).

    (C) Tests appended to `tests/test_assemble.py`:
      - `test_assemble_end_to_end_writes_qa_report`: mock `run_ffmpeg` (return a CompletedProcess whose stderr is the canned loudnorm block for pass-1) + `probe_duration` (fixed floats); create placeholder slide_XX.png + audio_XX.mp3 + slides.json/voice.json checkpoints; run `AssembleStage().run(workdir, config)`; assert `output.mp4`, `assembly.json` (if the test writes the checkpoint) and `qa_report.json` exist, and the returned AssemblyOutput.qa has measured_lufs + normalized_lufs + duration_deviation.
      - `test_pipeline_stages_has_assemble_stage`: import `PIPELINE_STAGES`; assert the last entry is an `AssembleStage` instance with `stage_name=="assemble"` and `checkpoint_name=="assembly"`.
      - Update any orchestrator end-to-end test that now hits the real AssembleStage: patch `avideo.integrations.ffmpeg.run_ffmpeg` and `probe_duration` (or `avideo.stages.assemble`'s imported names) so no real ffmpeg runs in the orchestrator suite — mirror how 04-03 patched `synthesize_slide`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble.py tests/test_orchestrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'from avideo.stages.qa import' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'qa_report.json' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'loudnorm_pass1_args' src/avideo/stages/assemble.py && grep -q 'loudnorm_pass2_args' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'parse_loudnorm_json' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'Table' src/avideo/stages/assemble.py` (Rich QA table — D-08)
    - `grep -q 'AssembleStage()' src/avideo/stages/stubs.py` succeeds
    - `grep -q 'from avideo.stages.assemble import AssembleStage' src/avideo/stages/stubs.py` succeeds
    - `grep -q 'class AssembleStub' src/avideo/stages/stubs.py` succeeds (stub class retained)
    - `! grep -q 'AssembleStub()' src/avideo/stages/stubs.py` (no AssembleStub INSTANCE left in PIPELINE_STAGES) — verify by checking the list line specifically
    - `uv run pytest tests/test_assemble.py tests/test_orchestrator.py -x -q` passes
    - `uv run pytest -q` full suite green
  </acceptance_criteria>
  <done>AssembleStage runs two-pass loudnorm + duration QA as a sub-step, writes qa_report.json atomically, attaches the QAReport, and prints a Rich table; QA is idempotent within the assemble checkpoint. PIPELINE_STAGES uses AssembleStage() (stub class retained). Full suite green. Phase 5 complete (ASMB-01/02/03 + QA-01/02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → ffmpeg subprocess | output.mp4 path + loudnorm filter string cross into spawned ffmpeg processes (pass 1 + pass 2) |
| ffmpeg stderr → parser | untrusted, noisy stderr text parsed for the loudnorm JSON block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering / EoP | loudnorm pass-1/pass-2 invocation | mitigate | Both passes build a Python `list[str]` and call `run_ffmpeg` (subprocess, default `shell=False`); the `loudnorm=...` filter is ONE list element; paths are the fixed `workdir.root/output.mp4` + tmp names (no user component). NEVER shell=True. |
| T-05-07 | Tampering | `parse_loudnorm_json` on ffmpeg stderr | mitigate | Parser isolates the LAST `{...}` block with a bounded regex (`\{[^{}]*\}`), `json.loads` only that block, and float-casts known fields; raises ValueError on missing block (Pitfall 4). It does not `eval` or execute any stderr content. |
| T-05-08 | Tampering | qa_report.json + normalized output.mp4 write | mitigate | Both written atomically (`*.tmp` → `os.replace`) under `workdir.root` with fixed names; no partial/poisoned artifacts on crash (D-10). |
| T-05-09 | Information Disclosure | QA Rich table / qa_report.json contents | accept | Contains only duration + LUFS numbers (no secrets, no PII); local CLI output. |
| T-05-10 | DoS | extra loudnorm passes lengthen runtime | accept | Two passes are mandated by QA-02; pass-2 copies video (`-c:v copy`) so only audio is re-encoded — fast. Bounded by a single output.mp4. |
</threat_model>

<verification>
- `uv run pytest tests/test_assemble.py -k "loudnorm or deviation or qa_report or end_to_end or pipeline_stages" -x -q` green.
- `uv run pytest -q` — full suite green (orchestrator end-to-end tests patched so no real ffmpeg runs).
- `grep` gates above all pass (faststart re-added after copy; AssembleStage swapped in; stub class retained).
- Manual (deferred to 05-VALIDATION Manual-Only): run the full pipeline, inspect `workdir/qa_report.json` (deviation + measured/normalized LUFS ≈ -16), and confirm the Rich QA table renders.
</verification>

<success_criteria>
- `parse_loudnorm_json` reliably extracts measured loudness fields from noisy ffmpeg stderr (QA-02).
- Two-pass loudnorm applies the measured values with `linear=true`, copies video, and re-adds `-movflags +faststart` (Pitfall 2 fixed) — output lands ≈ -16 LUFS.
- `QAReport` records target/actual/deviation + measured_lufs + normalized_lufs; `workdir/qa_report.json` is written atomically and shown as a Rich table (QA-01, QA-02, D-07, D-08).
- QA runs as a sub-step of `AssembleStage` (single `assembly` checkpoint / idempotence boundary).
- `PIPELINE_STAGES` uses `AssembleStage()` (real); `AssembleStub` class retained for tests.
- Full test suite green — Phase 5 complete (ASMB-01/02/03, QA-01/02).
</success_criteria>

<output>
After completion, create `.planning/phases/05-montaje-qa/05-02-SUMMARY.md`.
</output>
