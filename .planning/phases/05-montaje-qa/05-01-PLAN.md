---
phase: 05-montaje-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_assemble.py
  - tests/conftest.py
  - src/avideo/models/config.py
  - src/avideo/integrations/ffmpeg.py
  - src/avideo/stages/assemble.py
autonomous: true
requirements: [ASMB-01, ASMB-02, ASMB-03]
must_haves:
  truths:
    - "Wave-0 test file tests/test_assemble.py exists and exercises crossfade offsets, expected_total, clamp, filtergraph substrings, arg-list (+faststart, no shell=True), and ffprobe-driven slide duration"
    - "RunConfig accepts crossfade_seconds (default 0.5) and target_lufs (default -16) from config.yaml and defaults"
    - "integrations/ffmpeg.py measures real audio durations via ffprobe (format=duration) — never WPM"
    - "crossfade_offsets returns offset_k = (cumulative merged duration) - XF for each join; expected_total = sum(d) - (N-1)*XF"
    - "build_filtergraph normalizes every input to scale 1920x1080 + pad + setsar=1 + fps + format=yuv420p before xfade/acrossfade"
    - "When crossfade==0 the concat filter path is used (NOT xfade offset 0); when a slide is shorter than the crossfade the per-boundary crossfade is clamped to min(XF, prev, next) and falls back to a hard cut at <=0"
    - "run_ffmpeg invokes subprocess with a list[str] and shell=False, captures stderr, and raises a clear RuntimeError on nonzero exit"
    - "AssembleStage produces workdir/output.mp4 (1080p 16:9 H.264 yuv420p) using real ffprobe durations, written atomically tmp->rename, idempotent if output.mp4 + checkpoint already exist"
  artifacts:
    - path: "tests/test_assemble.py"
      provides: "Wave-0 unit tests + one guarded real-ffmpeg smoke test for ASMB-01/02/03"
      contains: "def test_"
    - path: "src/avideo/integrations/ffmpeg.py"
      provides: "ffprobe duration, crossfade math, filtergraph builder, arg-list builder, run_ffmpeg subprocess wrapper"
      contains: "def crossfade_offsets"
    - path: "src/avideo/stages/assemble.py"
      provides: "AssembleStage (stage_name='assemble', checkpoint='assembly') driving the ffmpeg builder"
      contains: "stage_name"
    - path: "src/avideo/models/config.py"
      provides: "crossfade_seconds + target_lufs config fields"
      contains: "crossfade_seconds"
  key_links:
    - from: "src/avideo/stages/assemble.py"
      to: "src/avideo/integrations/ffmpeg.py"
      via: "imports probe_duration + build_assemble_args + run_ffmpeg"
      pattern: "from avideo.integrations.ffmpeg import"
    - from: "src/avideo/stages/assemble.py"
      to: "workdir/audio/slide_XX.*"
      via: "ffprobe real audio durations drive slide length"
      pattern: "probe_duration"
---

<objective>
Build the FFmpeg assembly foundation: the Wave-0 test scaffold, the `integrations/ffmpeg.py` module (pure crossfade math + filtergraph/arg-list builders + a mockable subprocess wrapper + ffprobe duration helper), the two new config fields, and the real `AssembleStage` that stitches slides + audio into `workdir/output.mp4` (1080p 16:9 H.264) using REAL ffprobe durations and a configurable crossfade.

Purpose: Replace the empty-file `AssembleStub` output with a real, correctly-synced video, satisfying ASMB-01 (ffprobe-driven durations), ASMB-02 (configurable crossfade), and ASMB-03 (1080p 16:9). QA (loudnorm two-pass + deviation report) is layered on top in plan 05-02 as a sub-step of this same stage — so this plan deliberately does NOT swap `PIPELINE_STAGES` yet (that swap waits until QA is wired, plan 05-02).

Output: `tests/test_assemble.py`, `tests/conftest.py` fixtures, `src/avideo/integrations/ffmpeg.py`, `src/avideo/stages/assemble.py`, extended `src/avideo/models/config.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-montaje-qa/05-CONTEXT.md
@.planning/phases/05-montaje-qa/05-RESEARCH.md
@.planning/phases/05-montaje-qa/05-VALIDATION.md
@CLAUDE.md

Source files to read FIRST (contracts this plan builds against):
@src/avideo/models/slides.py
@src/avideo/models/voice.py
@src/avideo/models/timing.py
@src/avideo/models/assembly.py
@src/avideo/models/config.py
@src/avideo/stages/base.py
@src/avideo/stages/subtitles.py
@src/avideo/utils/workdir.py
@src/avideo/integrations/whisperx.py
@tests/conftest.py
@tests/test_slides_render.py

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — use directly, no exploration needed. -->

StageProtocol / CheckpointMixin (src/avideo/stages/base.py):
```python
class CheckpointMixin:
    stage_name: str = ""           # set as a class attribute, e.g. "assemble"
    @property
    def checkpoint_name(self) -> str:   # defaults to stage_name; OVERRIDE to "assembly"
        return self.stage_name
    def is_done(self, workdir) -> bool: ...   # provided
    # Implement: def run(self, workdir: WorkdirManager, config: RunConfig) -> BaseModel
    # The ORCHESTRATOR writes the checkpoint + marks done. Stages MUST NOT call write_checkpoint/mark_done.
```

WorkdirManager (src/avideo/utils/workdir.py):
```python
class WorkdirManager:
    root: Path                              # workdir root; subdirs slides/ audio/ subs/ already created
    def checkpoint_path(self, name: str) -> Path        # <root>/<name>.json
    def is_done(self, stage: str) -> bool
    def read_checkpoint(self, name: str, model_class: type[BaseModel]) -> BaseModel
    # write_checkpoint/mark_done are called by the orchestrator, not by stages.
```

Input contracts (read these checkpoints in AssembleStage):
```python
# models/slides.py
class SlidesOutput(BaseModel):
    png_paths: list[str]        # checkpoint name: "slides"
    mode: str = "auto"
# models/voice.py
class VoiceOutput(BaseModel):
    audio_paths: list[str]      # checkpoint name: "voice" — one audio per slide, index-aligned with png_paths (Assumption A1)
    voice_mode: str = "elevenlabs"
# models/subtitles.py (only needed when config.burn_subs)
class SubtitlesOutput(BaseModel):
    srt_path: str               # checkpoint name: "subs" — relative path "subs/output.srt"
    vtt_path: str
    cue_count: int = 0
# models/assembly.py — output contract for this stage (DO NOT extend QAReport here; that is plan 05-02)
class QAReport(BaseModel):
    target_seconds: float; actual_seconds: float; duration_deviation: float; lufs: Optional[float] = None
class AssemblyOutput(BaseModel):
    output_path: str
    qa: Optional[QAReport] = None
```

RunConfig (src/avideo/models/config.py) — extend with two fields:
```python
class RunConfig(BaseSettings):
    duration: int = Field(gt=0)         # the QA TARGET (not used for segment lengths)
    burn_subs: bool = False             # already exists
    workdir: Path = Path("workdir")
    # ADD: crossfade_seconds: float = Field(default=0.5, ge=0, ...)
    # ADD: target_lufs: float = Field(default=-16.0, ...)   # consumed by plan 05-02 QA
```

Established integration-module pattern (src/avideo/integrations/whisperx.py):
- module docstring citing decisions/pitfalls; `from __future__ import annotations`
- pure helpers at top (no I/O); subprocess/external calls in dedicated functions
- TYPE_CHECKING import guard for model types
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — test scaffold (tests/test_assemble.py + conftest fixtures)</name>
  <files>tests/test_assemble.py, tests/conftest.py</files>
  <read_first>
    - tests/test_slides_render.py (the guarded-smoke skip pattern to mirror: pytest.importorskip / shutil.which guard + skip inside body)
    - tests/conftest.py (fixture style; lazy imports; tmp_path usage)
    - .planning/phases/05-montaje-qa/05-RESEARCH.md "Validation Architecture" + "Code Examples" (verified offset values 2.5/6.0/8.0; loudnorm stderr field names; parse function shape)
    - .planning/phases/05-montaje-qa/05-VALIDATION.md (Per-Task Verification Map — the exact `-k` selectors)
  </read_first>
  <action>
    Create `tests/test_assemble.py` with tests that are RED now (the implementation modules do not exist yet) and turn GREEN as Tasks 2–4 land. Defer `from avideo.integrations.ffmpeg import ...` and `from avideo.stages.assemble import AssembleStage` to INSIDE each test body (so collection does not error before the modules exist), mirroring test_slides_render.py.

    Write tests using these `-k` selectors so they match 05-VALIDATION.md exactly:
    - `crossfade`: assert `crossfade_offsets([3.0,4.0,2.5], 0.5) == [2.5, 6.0]` and for 4 slides `[3.0,4.0,2.5,3.5]` → `[2.5, 6.0, 8.0]`; assert `expected_total([3.0,4.0,2.5], 0.5)` ≈ 8.5 and with XF=0 → 9.5 (full sum); assert the clamp helper returns `min(XF, prev, next)` and that a boundary with eff_XF<=0 signals a hard cut.
    - `probe_drives_duration`: patch `avideo.integrations.ffmpeg.subprocess.run` to return a fake CompletedProcess whose stdout is `{"format": {"duration": "3.000000"}}`; assert `probe_duration(path)` == 3.0 and that the args passed contain `format=duration` and `-of json` (ASMB-01: durations come from ffprobe, NOT timings.json).
    - `smoke_dimensions`: a real-ffmpeg smoke test guarded by `pytest.mark.skipif(shutil.which("ffmpeg") is None or shutil.which("ffprobe") is None, ...)`. Use the conftest helpers (below) to make 2–3 tiny PNGs + 2–3 ~1s sine audios, run the REAL assemble path, then ffprobe-assert width==1920, height==1080, pix_fmt=="yuv420p", and `abs(duration - expected_total) < 0.1` (±1-frame tolerance — Pitfall 7; do NOT assert exact equality).
    - Also add `test_build_filtergraph_*` substring assertions (no ffmpeg): the N>=2 crossfade graph contains `scale=1920:1080`, `setsar=1`, `format=yuv420p`, `xfade=` and `acrossfade=`; the XF==0 graph contains `concat=` and NOT `xfade=`; the single-slide graph contains neither `xfade=` nor `concat=`.
    - Add `test_build_assemble_args_*`: the builder returns a `list[str]`; `-movflags` followed by `+faststart` is present; the `-filter_complex` value is exactly ONE list element; assert NO element equals `shell=True` and that the function is invoked without `shell=`. (build_assemble_args is pure: assert on the returned list, do not run ffmpeg.)
    - Add `test_assemble_idempotent`: with `output.mp4` + `assembly.json` already present, mock `run_ffmpeg`/`probe_duration` and assert `AssembleStage.run` does NOT call `run_ffmpeg` (re-mounting is skipped — D-10).

    Add to `tests/conftest.py` (new fixtures, lazy imports, no top-level new deps):
    - `loudnorm_pass1_stderr` fixture returning the canned stderr string with the verified JSON block (field names `input_i`,`input_tp`,`input_lra`,`input_thresh`,`output_i`,`target_offset` — values from 05-RESEARCH Pattern 5). This is consumed by plan 05-02's `parse_loudnorm_json` tests; add it here in Wave 0 so 05-02 needs no conftest change.
    - `tiny_av_assets(tmp_path)` helper fixture: generate N tiny PNGs with Pillow (`from PIL import Image`, lazy import) and N short sine WAV/MP3 via `ffmpeg -f lavfi -i "sine=frequency=440:duration=1" ... ` using `subprocess.run([...], shell=False)`; return `(png_paths, audio_paths)`. Guard the audio generation so the fixture skips cleanly when ffmpeg is absent (used only by the guarded smoke test).
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble.py --co -q  # collects without import error (tests RED until Tasks 2-4)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_' tests/test_assemble.py` returns >= 6
    - `grep -q 'crossfade_offsets' tests/test_assemble.py` succeeds
    - `grep -q 'probe_drives_duration' tests/test_assemble.py` succeeds
    - `grep -q 'smoke_dimensions' tests/test_assemble.py` succeeds
    - `grep -q 'shutil.which' tests/test_assemble.py` succeeds
    - `grep -q 'loudnorm_pass1_stderr' tests/conftest.py` succeeds
    - `grep -q 'sine=frequency' tests/conftest.py` succeeds
    - `uv run pytest tests/test_assemble.py --co -q` exits 0 (collection succeeds)
  </acceptance_criteria>
  <done>tests/test_assemble.py collects cleanly with >=6 tests using the VALIDATION `-k` selectors; conftest has the loudnorm stderr fixture and the tiny-AV asset helper. Tests are RED (modules not yet implemented).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config fields + integrations/ffmpeg.py (pure math + builders + subprocess wrapper)</name>
  <files>src/avideo/models/config.py, src/avideo/integrations/ffmpeg.py</files>
  <behavior>
    - `crossfade_offsets([3.0,4.0,2.5], 0.5) -> [2.5, 6.0]`; `[3.0,4.0,2.5,3.5],0.5 -> [2.5,6.0,8.0]`
    - `expected_total([3.0,4.0,2.5], 0.5) ≈ 8.5`; `expected_total(d, 0.0) == sum(d)`
    - `clamp_crossfade(XF, prev, next) -> min(XF, prev, next)`; result <=0 means hard cut for that boundary
    - `probe_duration(path)` parses `{"format":{"duration":"3.0"}}` → 3.0; args include `format=duration` + `-of json`
    - `build_filtergraph` (N>=2, XF>0): contains `scale=1920:1080`, `setsar=1`, `format=yuv420p`, `xfade=`, `acrossfade=`
    - `build_filtergraph` (XF==0): contains `concat=`, NOT `xfade=`
    - `build_filtergraph` (N==1): neither `xfade=` nor `concat=` (single segment + `-shortest`)
    - `build_assemble_args(...)` returns list[str] with `-movflags +faststart`; filter_complex is one element; never shell=True
    - `run_ffmpeg(nonzero exit)` raises RuntimeError with stderr tail
  </behavior>
  <read_first>
    - src/avideo/models/config.py (RunConfig field style: `Field(default=..., ...)`, env_prefix, settings_customise_sources — DO NOT change source ordering)
    - src/avideo/integrations/whisperx.py (module docstring + pure-helper-first layout to mirror)
    - .planning/phases/05-montaje-qa/05-RESEARCH.md Patterns 1–4 + 6, "Code Examples", "Anti-Patterns to Avoid", Pitfalls 1/2/3/6 (USE THE VERIFIED FILTER STRINGS VERBATIM)
    - CLAUDE.md (FFmpeg via subprocess arg-list, NEVER shell=True; no moviepy/ffmpeg-python)
  </read_first>
  <action>
    (A) Extend `RunConfig` in config.py with two fields (do NOT touch `settings_customise_sources`):
      - `crossfade_seconds: float = Field(default=0.5, ge=0, description="Crossfade duration between slides; 0 = hard cuts (D-03)")`
      - `target_lufs: float = Field(default=-16.0, description="EBU R128 loudness target for two-pass loudnorm (D-06)")`

    (B) Create `src/avideo/integrations/ffmpeg.py` with module docstring citing D-01/D-02/D-03/D-04 and the verified pitfalls. Sections:

      PURE math (no I/O — unit-tested directly):
      - `crossfade_offsets(durations: list[float], xfade: float) -> list[float]` — exactly the verified algorithm from 05-RESEARCH "Code Examples": merged=d[0]; for d in durations[1:]: append round(merged - xfade, 6); merged += d - xfade.
      - `expected_total(durations: list[float], xfade: float) -> float` — `sum(d) - max(0, n-1)*xfade`.
      - `clamp_crossfade(xfade: float, prev_dur: float, next_dur: float) -> float` — `min(xfade, prev_dur, next_dur)` (Pitfall 1 mitigation); callers treat <=0 as a hard-cut boundary.

      PURE builders (return strings / list[str] — unit-tested via substring asserts):
      - `_input_normalize(label_in: str, label_out: str, fps: int = 30) -> str` — the verified per-input chain `[{in}]scale=1920:1080:force_original_aspect_ratio=decrease,pad=1920:1080:(ow-iw)/2:(oh-ih)/2,setsar=1,fps={fps},format=yuv420p[{out}]`.
      - `build_filtergraph(durations, xfade, *, fps=30) -> str` — dispatch:
          * N==1: one normalized video segment ([vout]) + `[1:a]aresample=48000[aout]` (single-slide path, Pattern 3; caller adds `-shortest`).
          * xfade==0 (and N>=2): concat path (Pattern 4) — normalize each input, then interleaved `[v0][a0][v1][a1]...concat=n=N:v=1:a=1[vout][aout]`.
          * xfade>0 and N>=2: per-boundary clamp via `clamp_crossfade`; if any clamped eff_XF<=0, that boundary becomes a hard cut (chain concat for that join or use eff_XF) — at minimum apply `clamp_crossfade` so no NEGATIVE offset is ever emitted (Pitfall 1). Build xfade(video)+acrossfade(audio) chains with offsets from `crossfade_offsets` using the per-boundary effective XF. Use `transition=fade` hardcoded (Open Question 3).
      - `build_assemble_args(image_paths, audio_paths, durations, *, output_path, xfade, fps=30, crf=20, preset="medium", burn_subs_path: str | None = None) -> list[str]` — assemble the full ffmpeg arg LIST: `-hide_banner`, per-image `-loop 1 -t <dur> -i <png>`, per-audio `-i <audio>`, `-filter_complex <ONE string element>`, `-map "[vout]" -map "[aout]"`, encode flags `-c:v libx264 -preset <preset> -crf <crf> -pix_fmt yuv420p -c:a aac -b:a 192k -movflags +faststart`, `-shortest` only on the single-slide path, and the output path LAST. When `burn_subs_path` is set, add `subtitles=filename='...'` (libass, Pattern 6) into the video chain (burn requires re-encode — keep libx264, do not copy). The whole filter_complex must be ONE element of the list. `-y` to overwrite the tmp file.
      - `probe_duration_args(path: str) -> list[str]` — `["ffprobe","-v","error","-show_entries","format=duration","-of","json", path]` (Pattern 2).

      SUBPROCESS plumbing (mockable; the ONLY place that shells out):
      - `run_ffmpeg(args: list[str]) -> subprocess.CompletedProcess[str]` — `subprocess.run(args, capture_output=True, text=True)` (NEVER shell=True); on returncode!=0 raise `RuntimeError` with the last ~8 stderr lines (D-04, clean message for orchestrator).
      - `probe_duration(path: str) -> float` — run `probe_duration_args`, `float(json.loads(stdout)["format"]["duration"])`.
      - `ffmpeg_available() -> bool` — `shutil.which("ffmpeg") is not None and shutil.which("ffprobe") is not None`.

    Reference `subprocess` as `import subprocess` at module top so tests can patch `avideo.integrations.ffmpeg.subprocess.run`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble.py -k "crossfade or probe_drives_duration or build_filtergraph or build_assemble_args" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'crossfade_seconds' src/avideo/models/config.py` succeeds
    - `grep -q 'target_lufs' src/avideo/models/config.py` succeeds
    - `grep -q 'def crossfade_offsets' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def expected_total' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def clamp_crossfade' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def build_filtergraph' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def build_assemble_args' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'def probe_duration' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'movflags' src/avideo/integrations/ffmpeg.py && grep -q 'faststart' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -q 'setsar=1' src/avideo/integrations/ffmpeg.py` succeeds
    - `grep -v '^#' src/avideo/integrations/ffmpeg.py | grep -c 'shell=True'` returns 0 (never shell=True)
    - `uv run pytest tests/test_assemble.py -k "crossfade or probe_drives_duration or build_filtergraph or build_assemble_args" -x -q` passes
  </acceptance_criteria>
  <done>RunConfig has crossfade_seconds + target_lufs; integrations/ffmpeg.py provides the pure math, the three filtergraph paths (single / concat / xfade-clamped), the arg-list builder with +faststart, and the mockable run_ffmpeg/probe_duration. The pure-logic and probe tests are GREEN; no shell=True anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: stages/assemble.py (AssembleStage — real encode, idempotent, atomic)</name>
  <files>src/avideo/stages/assemble.py</files>
  <behavior>
    - `AssembleStage.stage_name == "assemble"` and `checkpoint_name == "assembly"`
    - reads `slides`/`voice` checkpoints; slide count == audio count (else clear error — Assumption A1)
    - measures each audio with `probe_duration` (ASMB-01); never reads timings.json for segment lengths
    - dispatches builder by N and config.crossfade_seconds (single / concat / xfade)
    - when config.burn_subs: passes the fixed `subs/output.srt` path to the builder (D-05)
    - writes `output.mp4` atomically (tmp -> os.replace), returns AssemblyOutput(output_path=...)
    - idempotent: if output.mp4 + assembly.json checkpoint already exist, skip re-encode (does NOT call run_ffmpeg)
    - the real-ffmpeg smoke test produces a 1920x1080 yuv420p mp4 with duration ≈ expected_total
  </behavior>
  <read_first>
    - src/avideo/stages/subtitles.py (the canonical multi-checkpoint-reading stage: read_checkpoint usage, TYPE_CHECKING imports, fixed workdir paths, docstring style, returns model WITHOUT writing checkpoint)
    - src/avideo/stages/base.py (CheckpointMixin: set stage_name, override checkpoint_name property; orchestrator writes checkpoint/marks done)
    - src/avideo/utils/workdir.py (root, checkpoint_path, read_checkpoint; output.mp4 lives at workdir.root/"output.mp4")
    - src/avideo/integrations/ffmpeg.py (Task 2 — the functions to import)
    - .planning/phases/05-montaje-qa/05-RESEARCH.md "Runtime State Inventory" + Pitfall 5 (real durations) + D-10 idempotence
  </read_first>
  <action>
    Create `src/avideo/stages/assemble.py` with `AssembleStage(CheckpointMixin)`:
      - `stage_name = "assemble"`; override `checkpoint_name` property to return `"assembly"` (matches AssembleStub + existing `.assemble.done` markers — Runtime State Inventory).
      - `run(self, workdir, config) -> AssemblyOutput`:
        1. IDEMPOTENCE (D-10): if `(workdir.root/"output.mp4").exists()` AND `workdir.checkpoint_path("assembly").exists()`, read and return the existing `AssemblyOutput` from the checkpoint WITHOUT calling run_ffmpeg. (The orchestrator's done-marker skip handles the common resume; this guards re-runs where the marker is absent but artifacts exist.)
        2. Read `slides` checkpoint → `SlidesOutput.png_paths`; read `voice` checkpoint → `VoiceOutput.audio_paths`. Resolve each relative path against `workdir.root` if not absolute.
        3. Validate `len(png_paths) == len(audio_paths)` and both > 0; on mismatch/empty raise a clear `RuntimeError` (surfaces as a Rich message — Assumption A1). Slide k ↔ audio k.
        4. Durations: `durations = [probe_duration(a) for a in audio_paths]` (ASMB-01 / D-02 — REAL audio, never timings.json).
        5. burn-subs (D-05): `subs_path = str(workdir.root / "subs" / "output.srt")` only if `config.burn_subs` (fixed path, no user component — T-05 path safety); else None.
        6. Build args: `tmp = workdir.root / "output.mp4.tmp"`; call `build_assemble_args(png_paths, audio_paths, durations, output_path=str(tmp), xfade=config.crossfade_seconds, burn_subs_path=subs_path)`.
        7. `run_ffmpeg(args)` (captures stderr; raises clean RuntimeError on failure — D-04).
        8. Atomic publish: `os.replace(str(tmp), str(workdir.root/"output.mp4"))` (D-10 — mp4 tmp→rename).
        9. Return `AssemblyOutput(output_path=str(workdir.root/"output.mp4"))`. (QAReport is attached in plan 05-02 once qa.py exists — leave `qa=None` here.)
      - Do NOT call `workdir.write_checkpoint`/`mark_done` (orchestrator's job — StageProtocol contract).
      - This plan does NOT touch stubs.py / PIPELINE_STAGES — the swap happens in 05-02 after QA is wired (single idempotence boundary). The stub stays active until then.
  </action>
  <verify>
    <automated>uv run pytest tests/test_assemble.py -k "probe_drives_duration or idempotent or smoke_dimensions" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'stage_name.*=.*"assemble"' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'checkpoint_name' src/avideo/stages/assemble.py && grep -q '"assembly"' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'from avideo.integrations.ffmpeg import' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'probe_duration' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'os.replace' src/avideo/stages/assemble.py` succeeds
    - `grep -q 'output.mp4.tmp' src/avideo/stages/assemble.py` succeeds
    - `grep -vi 'timings.json' src/avideo/stages/assemble.py >/dev/null && ! grep -q 'read_checkpoint("timings"' src/avideo/stages/assemble.py` (segment durations NOT from timings)
    - `uv run pytest tests/test_assemble.py -k "probe_drives_duration or idempotent or smoke_dimensions" -x -q` passes (smoke runs locally where ffmpeg exists)
    - `uv run pytest tests/test_assemble.py -x -q` all green
  </acceptance_criteria>
  <done>AssembleStage reads slides+voice checkpoints, measures real ffprobe durations, builds and runs the ffmpeg arg-list, writes output.mp4 atomically (tmp→rename), and is idempotent when artifacts exist. The guarded smoke test asserts a 1920×1080 yuv420p mp4 with duration ≈ expected_total. All tests in test_assemble.py pass; PIPELINE_STAGES is left untouched for plan 05-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → ffmpeg subprocess | Slide/audio/subtitle paths and the filtergraph string cross into a spawned ffmpeg process |
| prior-phase checkpoints → assemble | slides.json / voice.json (file paths) consumed as build inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering / EoP | `run_ffmpeg` / `build_assemble_args` | mitigate | All ffmpeg/ffprobe invocations build a Python `list[str]` and call `subprocess.run(args)` with the implicit default `shell=False`; NEVER `shell=True`. The whole `filter_complex` is ONE list element so spaces/newlines are passed literally — no shell parsing. Enforced by grep gate `grep -v '^#' ... | grep -c 'shell=True' == 0`. (CLAUDE.md hard rule + V5/V12.) |
| T-05-02 | Tampering | path construction in `stages/assemble.py` | mitigate | Output paths built only from `workdir.root / fixed-name` (`output.mp4`, `output.mp4.tmp`); subtitle path is the fixed `subs/output.srt` (no user component). Input slide/audio paths come from validated prior-phase checkpoints, not raw CLI input. |
| T-05-03 | Tampering | subtitle filter arg (libass `:`/`'`/`\` in path) | mitigate | Burn path is the fixed `subs/output.srt`; use the `subtitles=filename='...'` form (escaping VERIFIED for spaces in 05-RESEARCH Pattern 6). No user-controlled component. |
| T-05-04 | Information Disclosure | ffmpeg stderr on failure | accept | `run_ffmpeg` surfaces only the last ~8 stderr lines as a RuntimeError message (D-04); local CLI tool, no remote exposure, no secrets in ffmpeg stderr (offline, no API keys). |
| T-05-05 | DoS | huge/many slides → long encode | accept | Slide count is bounded by the storyboard from earlier phases; out of scope for this phase (05-RESEARCH Security Domain). |
</threat_model>

<verification>
- `uv run pytest tests/test_assemble.py -x -q` — all pure-logic + probe + guarded smoke tests green.
- `uv run pytest -q` — full suite still green (no regressions; PIPELINE_STAGES untouched, stub still active).
- `grep -v '^#' src/avideo/integrations/ffmpeg.py | grep -c 'shell=True'` returns 0.
- Manual (optional, deferred to 05-VALIDATION Manual-Only): play a real `workdir/output.mp4` and confirm A/V sync + crossfade smoothness.
</verification>

<success_criteria>
- `RunConfig` exposes `crossfade_seconds` (0.5 default) and `target_lufs` (-16 default), overridable via config.yaml.
- `integrations/ffmpeg.py` measures real audio durations (ffprobe), computes verified crossfade offsets, builds the three filtergraph paths (single / concat for XF=0 / clamped xfade), and runs ffmpeg via an arg-list (never shell=True) with `+faststart`.
- `AssembleStage` produces `workdir/output.mp4` at 1920×1080 yuv420p H.264 using real ffprobe durations, written atomically, idempotent when artifacts exist. (ASMB-01, ASMB-02, ASMB-03.)
- Wave-0 `tests/test_assemble.py` is fully green; the guarded real-ffmpeg smoke asserts dimensions + duration tolerance.
- `PIPELINE_STAGES` is intentionally NOT swapped in this plan (waits for QA wiring in 05-02).
</success_criteria>

<output>
After completion, create `.planning/phases/05-montaje-qa/05-01-SUMMARY.md`.
</output>
