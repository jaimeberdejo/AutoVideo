---
phase: 04-voz-subtitulos
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/avideo/integrations/whisperx.py
  - src/avideo/stages/voice_record.py
  - src/avideo/stages/align.py
  - tests/test_voice_record.py
  - tests/test_align.py
autonomous: true
requirements: [VOICE-03, ALIGN-01, ALIGN-02]
user_setup:
  - service: record-mode-deps
    why: "Modo --voice record requiere el extra opcional 'record' (sounddevice/soundfile/whisperx/torch)"
    dashboard_config:
      - task: "Instalar el extra record para grabar/alinear localmente"
        location: "uv sync --extra record  (torch CPU; portaudio en el sistema para grabar)"

must_haves:
  truths:
    - "En modo record el sistema exporta el guion segmentado por slide para que el usuario lo locute"
    - "El sistema autodetecta workdir/audio/slide_XX.wav aportados; si no existen, graba con sounddevice"
    - "WhisperX alinea cada wav y produce timings por palabra (UnifiedTimings source=whisperx)"
    - "En modo elevenlabs la etapa align NO ejecuta whisperx (no-op idempotente que conserva los timings de la voz)"
    - "Importar/usar el modo elevenlabs no carga whisperx ni torch (import perezoso)"
  artifacts:
    - path: "src/avideo/integrations/whisperx.py"
      provides: "align_wav: import perezoso de whisperx/torch + load_model/transcribe/load_align_model/align → word_segments"
      contains: "import whisperx"
    - path: "src/avideo/stages/voice_record.py"
      provides: "Exporta guion segmentado + autodetecta slide_XX.wav o graba con sounddevice → UnifiedTimings(source=record, words=[])"
      contains: "slide_"
    - path: "src/avideo/stages/align.py"
      provides: "AlignStage: en record corre whisperx por wav; en elevenlabs no-op idempotente"
      contains: 'stage_name: str = "align"'
  key_links:
    - from: "src/avideo/stages/align.py"
      to: "src/avideo/integrations/whisperx.py"
      via: "import a scope de módulo de align_wav (mock point)"
      pattern: "from avideo.integrations.whisperx import"
    - from: "src/avideo/integrations/whisperx.py"
      to: "whisperx (torch)"
      via: "import whisperx DENTRO de la función (lazy)"
      pattern: "import whisperx"
---

<objective>
Implementa el camino `--voice record` (VOICE-03) y la alineación forzada (ALIGN-01/ALIGN-02): la integración WhisperX con import PEREZOSO (`integrations/whisperx.py`), la etapa `voice_record` (exporta el guion segmentado, autodetecta `slide_XX.wav` aportados o graba con `sounddevice`), y la etapa `align` que en modo `record` corre WhisperX por wav (timings palabra a palabra → `UnifiedTimings`) y en modo `elevenlabs` es un no-op idempotente.

Purpose: Cierra el segundo backend de voz y la alineación. Es la única parte de la fase con dependencias pesadas (whisperx/torch/sounddevice) — TODAS de import perezoso y mockeadas en CI para no penalizar el modo por defecto ni romper CI sin hardware/modelos. Ambos backends convergen en el mismo `UnifiedTimings` (D-11) que `subtitles.py` (04-03) consume sin saber la fuente.

Output: `integrations/whisperx.py`, `stages/voice_record.py`, `stages/align.py`, y los tests reales de `test_voice_record.py` + `test_align.py` (scaffold creado en 04-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voz-subtitulos/04-CONTEXT.md
@.planning/phases/04-voz-subtitulos/04-RESEARCH.md
@.planning/phases/04-voz-subtitulos/04-VALIDATION.md
@CLAUDE.md

# Patrón de import perezoso + mock point (replicar):
@src/avideo/integrations/anthropic.py
@src/avideo/stages/storyboard.py

# Contrato unificado creado en 04-01 (entrada/salida de align):
@.planning/phases/04-voz-subtitulos/04-01-PLAN.md

# Contratos de etapa, filesystem, entradas Phase 2:
@src/avideo/stages/base.py
@src/avideo/utils/workdir.py
@src/avideo/models/script.py
@src/avideo/models/config.py
@src/avideo/stages/stubs.py

<interfaces>
<!-- Contratos del codebase + de 04-01. No explorar; usar directamente. -->

UnifiedTimings (src/avideo/models/timings.py — creado en 04-01):
```python
class WordTiming(BaseModel): text: str; start: float; end: float   # SEGUNDOS, RELATIVOS al slide
class SlideTimings(BaseModel):
    slide_index: int; audio_path: str; duration: float; words: list[WordTiming] = []
class UnifiedTimings(BaseModel): source: str; slides: list[SlideTimings]   # source: "elevenlabs"|"whisperx"
```

RunConfig (creado/ampliado en 04-01):
```python
voice: VoiceMode         # enum {elevenlabs, record}
voice_id: str
whisperx_model: str = "small"     # añadido en 04-01 (D-05)
language: str = "es"
```

WorkdirManager: self.root, subdir audio/ ya creado; checkpoint_path(name), read_checkpoint(name, cls), write_checkpoint (lo llama el orquestador). Entrada: script.json -> ScriptOutput (slides: list[SlideScript{slide_index, narration}]).

CheckpointMixin: stage_name: str; checkpoint_name property (default stage_name); run(workdir, config) -> BaseModel.

WhisperX API (VERIFIED en 04-RESEARCH.md Pattern 3 / Code Examples):
```python
import whisperx                      # import PEREZOSO dentro de la función
device, compute = "cpu", "int8"
model = whisperx.load_model(model_size, device, compute_type=compute, language=language)
audio = whisperx.load_audio(wav_path)
result = model.transcribe(audio, batch_size=16)
model_a, metadata = whisperx.load_align_model(language_code=language, device=device)
aligned = whisperx.align(result["segments"], model_a, metadata, audio, device, return_char_alignments=False)
aligned["word_segments"]            # [{"word": str, "start": float_s, "end": float_s}, ...]
```
torch 2.6+ rompe la carga del VAD pyannote (weights_only) → mitigación: extra record pinea torch==2.5.1 (hecho en 04-01); documentar vad_method="silero" como fallback. Sin diarización (no HF token).

VoiceStage (creado en 04-01, src/avideo/stages/voice.py): despacha a VoiceRecordStage cuando config.voice==record (import perezoso dentro del branch). Este plan crea VoiceRecordStage.

Mock points (a scope de módulo, patrón anthropic/storyboard):
- align: `from avideo.integrations.whisperx import align_wav` → tests parchean `avideo.stages.align.align_wav`.
- record: importa sounddevice/soundfile PEREZOSAMENTE dentro de la función de grabación → tests parchean en el módulo voice_record.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: integrations/whisperx.py (align_wav, import perezoso, CPU int8)</name>
  <read_first>src/avideo/integrations/anthropic.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Pattern 3, Pitfall 2, Pitfall 5, Assumptions A1/A2/A5), src/avideo/models/timings.py</read_first>
  <files>src/avideo/integrations/whisperx.py</files>
  <behavior>
    - align_wav(wav_path, language="es", model_size="small") devuelve una lista de dicts word_segments [{"word","start","end"}] (en CPU int8).
    - whisperx/torch se importan DENTRO de la función, no a scope de módulo (verificable: el import top-level del módulo no falla aunque whisperx no esté instalado).
    - Test parchea `whisperx` (módulo) o las funciones load_model/load_align_model/align; nunca carga modelos reales.
  </behavior>
  <action>
Implementa `integrations/whisperx.py` con docstring de módulo explicando: import PEREZOSO (D-06) para no penalizar el modo elevenlabs/arranque; CPU int8 portable/CI; torch pineado a 2.5.1 en el extra record por el pitfall weights_only del VAD pyannote (RESEARCH Pitfall 2); fallback documentado `vad_method="silero"` (verificar en runtime, A2). NO usa diarización.
- `align_wav(wav_path: str, language: str = "es", model_size: str = "small") -> list[dict]`:
  - `import whisperx` y (si hace falta) `import torch` DENTRO de la función (D-06).
  - `device, compute = "cpu", "int8"`.
  - `model = whisperx.load_model(model_size, device, compute_type=compute, language=language)`.
  - `audio = whisperx.load_audio(wav_path)`; `result = model.transcribe(audio, batch_size=16)`.
  - `model_a, metadata = whisperx.load_align_model(language_code=language, device=device)`.
  - `aligned = whisperx.align(result["segments"], model_a, metadata, audio, device, return_char_alignments=False)`.
  - `return aligned["word_segments"]` (cada dict con start/end en SEGUNDOS, A1).
  - Maneja `ImportError` con un mensaje claro: "modo record requiere el extra 'record': uv sync --extra record" (RESEARCH Missing dependencies). Maneja el fallo de carga VAD documentando el remedio (pin torch 2.5.1) en el docstring; no parchees weights_only (frágil, último recurso).
- Helper opcional `word_segments_to_words(segs) -> list[WordTiming]` para convertir a WordTiming (start/end RELATIVOS al wav del slide) — útil para align.py; PURO y testeable.
Tests: en tests/test_align.py (lo amplía Task 3) o un test mínimo aquí — basta cubrir align_wav en Task 3 con whisperx mockeado. En este task, garantiza que `import avideo.integrations.whisperx` NO falle sin whisperx instalado (import top-level limpio).
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run python -c "import avideo.integrations.whisperx as w; assert hasattr(w, 'align_wav'); print('OK import-safe')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def align_wav" src/avideo/integrations/whisperx.py`
    - `grep -A12 "def align_wav" src/avideo/integrations/whisperx.py | grep -q "import whisperx"` (import perezoso dentro de la función)
    - `! grep -n "^import whisperx\|^import torch" src/avideo/integrations/whisperx.py` (NO import a scope de módulo)
    - `grep -q "int8" src/avideo/integrations/whisperx.py` (CPU compute portable)
    - El comando -c imprime "OK import-safe" (módulo importa sin whisperx instalado)
  </acceptance_criteria>
  <done>integrations/whisperx.py con align_wav de import perezoso (CPU int8), import-safe sin whisperx instalado, ImportError con mensaje claro del extra record; torch-pin/vad_method documentados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stages/voice_record.py (export guion segmentado + autodetect/grabar wav)</name>
  <read_first>src/avideo/stages/storyboard.py, src/avideo/stages/base.py, src/avideo/utils/workdir.py, src/avideo/models/script.py, src/avideo/models/timings.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Architecture record mode, Code Examples "Autodetección", Pitfall 5, Security Domain path traversal)</read_first>
  <files>src/avideo/stages/voice_record.py, tests/test_voice_record.py</files>
  <behavior>
    - VoiceRecordStage.run exporta el guion segmentado por slide a un archivo bajo workdir (p.ej. workdir/audio/script_segments.txt o por-slide) con el texto a locutar.
    - Si workdir/audio/slide_XX.wav existe → se usa (autodetección); sounddevice NO se invoca.
    - Si NO existe → se invoca la grabación (sounddevice.rec + soundfile.write) — mockeado en tests.
    - Devuelve UnifiedTimings(source="record", slides=[SlideTimings(audio_path=...wav, words=[])]) — sin palabras todavía (las añade align).
    - stage_name == "voice"; sin red ni audio real en tests.
  </behavior>
  <action>
`stages/voice_record.py` (D-04/D-12): clase `VoiceRecordStage(CheckpointMixin)` con `stage_name = "voice"` (mismo contrato de checkpoint que VoiceElevenlabsStage — el despacho lo hace VoiceStage). `run(workdir, config) -> UnifiedTimings`:
1. Lee `script = workdir.read_checkpoint("script", ScriptOutput)`.
2. Exporta el guion SEGMENTADO por slide (Claude's discretion sobre el formato): escribe un archivo legible bajo `workdir.root/audio/` (p.ej. `script_segments.txt` con encabezado por slide, o `slide_XX.txt` por slide) para que el usuario sepa qué locutar. Documenta el formato elegido.
3. Por cada slide: construye `wav_path = workdir.root / "audio" / f"slide_{slide.slide_index:02d}.wav"` (path SOLO vía workdir.root → sin path traversal, T-04-05). Implementa `resolve_audio(...)` (RESEARCH Code Examples): si `wav_path.exists()` → úsalo (autodetección, D-04b). Si no → graba: importa `sounddevice` y `soundfile` PEREZOSAMENTE dentro de la función (Pitfall 5), prompt rich "graba el slide N", `sd.rec(...)`/`sd.wait()`/`sf.write(wav_path, data, sr)`. Maneja ImportError con mensaje claro (extra record) y documenta que sin micrófono el usuario puede aportar los wav.
4. Calcula `duration` por slide: lee la duración real del wav con `soundfile.info(wav_path).frames / samplerate` (import perezoso) si soundfile está disponible; si no, deja 0.0 (align/Phase 5 la recalculan). Documenta.
5. Devuelve `UnifiedTimings(source="record", slides=[SlideTimings(slide_index=, audio_path="audio/slide_XX.wav", duration=, words=[])])` — words VACÍO (las rellena align). NO escribe checkpoint.
Tests en tests/test_voice_record.py: (a) autodetección: crea workdir/audio/slide_00.wav y slide_01.wav en tmp_path + script.json de 2 slides; parchea sounddevice/soundfile (o soundfile.info) y asserta que sounddevice.rec NO se llama y el resultado es UnifiedTimings(source="record") con 2 slides apuntando a los wav. (b) grabación: sin wav presentes, parchea `sounddevice.rec`/`sounddevice.wait`/`soundfile.write` (en el módulo voice_record) y asserta que se invoca la grabación una vez por slide. (c) el guion segmentado se exporta (archivo existe con el texto). (d) stage_name == "voice". NUNCA grabar/escribir audio real.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_voice_record.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'stage_name: str = "voice"' src/avideo/stages/voice_record.py`
    - `grep -q "exists()" src/avideo/stages/voice_record.py` (autodetección de wav)
    - `grep -q "import sounddevice\|import soundfile" src/avideo/stages/voice_record.py && ! grep -n "^import sounddevice" src/avideo/stages/voice_record.py` (import perezoso, no a scope de módulo)
    - `grep -q 'source="record"\|source=.record' src/avideo/stages/voice_record.py`
    - `uv run pytest tests/test_voice_record.py -x -q` pasa (autodetect + grabación mockeada + export guion)
  </acceptance_criteria>
  <done>VoiceRecordStage exporta guion segmentado, autodetecta o graba (mock) slide_XX.wav, devuelve UnifiedTimings(source=record, words=[]); imports perezosos; tests verdes sin audio real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: stages/align.py (record→whisperx por palabra; elevenlabs→no-op)</name>
  <read_first>src/avideo/stages/stubs.py (AlignStub), src/avideo/integrations/whisperx.py, src/avideo/stages/base.py, src/avideo/models/timings.py, src/avideo/utils/workdir.py, .planning/phases/04-voz-subtitulos/04-RESEARCH.md (Open Question 4, ALIGN-01, ALIGN-02)</read_first>
  <files>src/avideo/stages/align.py, tests/test_align.py</files>
  <behavior>
    - En modo record: AlignStage.run lee el checkpoint de voz (UnifiedTimings source=record), llama align_wav por cada wav, convierte word_segments → WordTiming y devuelve UnifiedTimings(source="whisperx") con words rellenas por slide.
    - En modo elevenlabs: AlignStage.run NO llama whisperx; devuelve el UnifiedTimings de la voz tal cual (no-op idempotente, ALIGN-02).
    - El test de elevenlabs (-k elevenlabs_skip) verifica que align_wav NO se invoca.
    - El checkpoint de align se llama "align" y contiene UnifiedTimings consumible por subtitles.
    - stage_name == "align".
  </behavior>
  <action>
`stages/align.py` (reemplaza AlignStub; D-10/D-12; RESEARCH Open Question 4): clase `AlignStage(CheckpointMixin)` con `stage_name = "align"` (checkpoint_name default "align"). Importa `align_wav` a SCOPE DE MÓDULO (`from avideo.integrations.whisperx import align_wav`) para que los tests parcheen `avideo.stages.align.align_wav` (patrón storyboard). `run(workdir, config) -> UnifiedTimings`:
- Lee el checkpoint de voz: `voice_timings = workdir.read_checkpoint("voice", UnifiedTimings)`.
- `if config.voice == VoiceMode.elevenlabs:` → no-op idempotente (ALIGN-02): devuelve `voice_timings` tal cual (los timings ya vienen del API). NO importa/llama whisperx. Documenta que align es idempotente passthrough en elevenlabs.
- `else` (record, ALIGN-01): por cada SlideTimings, resuelve el wav_path (audio_path relativo → workdir.root/audio_path o reconstruir desde slide_index), llama `segs = align_wav(wav_path, language=config.language, model_size=config.whisperx_model)`, convierte a `list[WordTiming]` (start/end en segundos relativos al wav del slide), y construye un nuevo SlideTimings con `words=[...]` (y duration = último word.end o la existente). Devuelve `UnifiedTimings(source="whisperx", slides=[...])`.
- NO escribe checkpoint (lo hace el orquestador).
Tests en tests/test_align.py:
- ALIGN-01 (record): escribe un checkpoint "voice" = UnifiedTimings(source="record", slides=[2 slides con wav]) vía WorkdirManager; parchea `avideo.stages.align.align_wav` con `fake_word_segments` (de conftest); config.voice=record; asserta resultado UnifiedTimings(source="whisperx") con words pobladas por slide, y que align_wav fue llamado una vez por slide.
- ALIGN-02 (`-k elevenlabs_skip`): escribe checkpoint "voice" = UnifiedTimings(source="elevenlabs", ...); parchea `avideo.stages.align.align_wav` con un MagicMock; config.voice=elevenlabs; asserta que align_wav.called is False y que el resultado es idéntico al de la voz (no-op).
- stage_name == "align". Sin modelos reales.
  </action>
  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_align.py -x -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from avideo.integrations.whisperx import align_wav" src/avideo/stages/align.py` (mock point a scope de módulo)
    - `grep -q 'stage_name: str = "align"' src/avideo/stages/align.py`
    - `grep -q "config.voice" src/avideo/stages/align.py` (branch elevenlabs/record)
    - `grep -q 'source="whisperx"\|source=.whisperx' src/avideo/stages/align.py`
    - `uv run pytest tests/test_align.py -k elevenlabs_skip -x -q` pasa (no-op en elevenlabs)
    - `uv run pytest tests/test_align.py -x -q` pasa (record alinea por palabra)
    - `uv run pytest -q` suite completa verde
  </acceptance_criteria>
  <done>AlignStage corre whisperx por wav en record (UnifiedTimings source=whisperx con words) y es no-op idempotente en elevenlabs; tests ALIGN-01 + ALIGN-02 verdes con whisperx mockeado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → proceso | slide_XX.wav aportados por el usuario entran al proceso (paths construidos vía WorkdirManager) |
| hardware micrófono → proceso | sounddevice captura audio (solo desarrollo local; mockeado en CI) |
| modelos whisperx/torch (pickle) → proceso | Carga de modelos VAD/wav2vec2 (fuentes oficiales faster-whisper/torchaudio) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | path de slide_XX.wav aportado | mitigate | wav_path construido SOLO desde workdir.root/audio con f"slide_{index:02d}.wav"; no se acepta nombre de archivo arbitrario del usuario → sin path traversal (V5 Input Validation) |
| T-04-06 | Tampering / RCE | carga de modelo whisperx (pickle) | mitigate | Modelos de fuentes oficiales (faster-whisper/torchaudio); torch pineado 2.5.1; sin diarización (sin HF token); no parchear weights_only salvo último recurso documentado |
| T-04-07 | Denial of Service | sounddevice sin PortAudio/dispositivo (CI/Docker) | mitigate | Import perezoso de sounddevice; autodetección de wav aportados permite record sin micrófono; tests mockean rec/write; ImportError con mensaje claro del extra record |
| T-04-08 | Information Disclosure | logs de la etapa record/align | accept | No hay secretos en este camino (whisperx local, sin API key); bajo riesgo |
</threat_model>

<verification>
- `uv run pytest -q` — suite completa verde (test_voice_record.py + test_align.py reales).
- `uv run pytest tests/test_align.py -k elevenlabs_skip -x -q` — no-op en elevenlabs verde (ALIGN-02).
- `uv run python -c "import avideo.integrations.whisperx"` import-safe sin whisperx instalado.
- `grep` gates de acceptance_criteria en verde (imports perezosos confirmados).
</verification>

<success_criteria>
- VOICE-03: modo record exporta guion segmentado, autodetecta slide_XX.wav o graba con sounddevice (mock en CI).
- ALIGN-01: WhisperX alinea por palabra → UnifiedTimings(source=whisperx) con words pobladas.
- ALIGN-02: en elevenlabs align es no-op idempotente (whisperx NO se invoca).
- Import perezoso de whisperx/torch/sounddevice confirmado; CI sin red/audio/modelos reales.
- Ambos backends convergen en UnifiedTimings (D-11) listo para subtitles.py (04-03).
</success_criteria>

<output>
After completion, create `.planning/phases/04-voz-subtitulos/04-02-SUMMARY.md`
</output>
