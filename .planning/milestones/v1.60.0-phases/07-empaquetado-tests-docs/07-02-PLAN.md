---
phase: 07-empaquetado-tests-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_storyboard.py
  - tests/test_timing.py
  - tests/test_slides_render.py
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "El test de storyboard pasa con la API de Anthropic mockeada (sin llamada real ni API key)"
    - "El test de timing verifica reparto de duración (suma exacta + clamps) y presupuesto de palabras"
    - "El test de render de slide produce/valida un PNG 1920×1080 (o skip limpio sin Chromium)"
    - "La suite completa sigue verde (≥303 tests)"
  artifacts:
    - path: "tests/test_storyboard.py"
      provides: "TEST-01: storyboard con Anthropic mockeada"
      contains: "mocker.patch"
    - path: "tests/test_timing.py"
      provides: "TEST-02: reparto de duración + presupuesto de palabras"
      contains: "word_budget"
    - path: "tests/test_slides_render.py"
      provides: "TEST-03: render HTML→PNG 1920×1080"
      contains: "1920, 1080"
  key_links:
    - from: "tests/test_storyboard.py"
      to: "avideo.stages.storyboard.call_structured"
      via: "mocker.patch (no real API call)"
      pattern: "mocker.patch.*call_structured"
    - from: "tests/test_timing.py"
      to: "avideo.stages.timing.apportion_seconds / TimingStage"
      via: "import directo + asserts de suma exacta"
      pattern: "apportion_seconds|TimingStage"
    - from: "tests/test_slides_render.py"
      to: "avideo.integrations.playwright.SlideRenderer"
      via: "render_to_png → assert PNG size"
      pattern: "render_to_png"
---

<objective>
Verificar (y extender solo si hay gap real) los tres tests mínimos del core. Los tres archivos YA existen y la suite pasa (303 tests). Este plan NO duplica cobertura: confirma que cada test cubre realmente su requisito y añade una aserción/test focalizado solo si falta algo.

- TEST-01: storyboard con Anthropic mockeada (sin llamada real).
- TEST-02: director de timing — reparto exacto de duración + clamps + presupuesto de palabras.
- TEST-03: render de una slide HTML → PNG 1920×1080 (skip limpio si Chromium no está instalado).

Purpose: garantizar que el "contrato Nyquist" de Phase 7 (07-VALIDATION.md) está cubierto por tests verificables y que la suite completa permanece verde como guarda de regresión.
Output: los tres archivos de test verificados; cambios mínimos solo si se detecta un gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-empaquetado-tests-docs/07-CONTEXT.md
@.planning/phases/07-empaquetado-tests-docs/07-VALIDATION.md
@tests/test_storyboard.py
@tests/test_timing.py
@tests/test_slides_render.py
@tests/conftest.py

<facts>
<!-- Verificado en planning — el executor NO necesita re-descubrir. -->
- Los tres tests YA existen y pasan; total de 303 tests en la suite.
- test_storyboard.py mockea `avideo.stages.storyboard.call_structured` (TEST-01 satisfecho: sin API real).
- test_timing.py prueba `apportion_seconds` (suma exacta, empates, weights cero), clamps MIN/MAX, y `word_budget == round(seconds*wpm/60)` (TEST-01/02 satisfecho).
- test_slides_render.py renderiza HTML con Chromium real vía `SlideRenderer.render_to_png` y asserta `img.size == (1920, 1080)`; hace `importorskip("playwright")` y `pytest.skip` si falta el binario Chromium (TEST-03 satisfecho con skip graceful).
- conftest.py provee fixtures `tmp_workdir`, `minimal_bullets`, etc.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificar TEST-01/02/03 cubren sus requisitos y la suite está verde</name>
  <files>tests/test_storyboard.py, tests/test_timing.py, tests/test_slides_render.py</files>
  <read_first>tests/test_storyboard.py, tests/test_timing.py, tests/test_slides_render.py, tests/conftest.py (todos en contexto)</read_first>
  <action>
VERIFICAR (no reescribir) que cada test cubre su requisito:

1. TEST-01 (storyboard mockeado): confirmar que `tests/test_storyboard.py` mockea `call_structured` (no hace llamada real a Anthropic) y asserta que el storyboard devuelto proviene del mock. Ya cumple — ejecutar y confirmar verde.
2. TEST-02 (timing): confirmar que `tests/test_timing.py` prueba (a) reparto de duración con suma EXACTA == config.duration y (b) `word_budget == round(seconds*wpm/60)`. Ya cumple — ejecutar y confirmar verde.
3. TEST-03 (render PNG): confirmar que `tests/test_slides_render.py` valida `img.size == (1920, 1080)` y hace skip limpio si Chromium no está. Ya cumple — ejecutar y confirmar verde (o skip).

Ejecutar el subconjunto rápido y luego la suite completa. NO reescribir tests que ya pasan. Si los tres pasan y cubren sus requisitos, NO hacer cambios de código — solo documentar la verificación en el SUMMARY.

Procedimiento:
- `uv run pytest tests/test_storyboard.py tests/test_timing.py tests/test_slides_render.py -q`
- `uv run pytest -q` (guarda de regresión: ≥303 tests, 0 fallos; skips aceptables solo en test_slides_render si falta Chromium).
  </action>
  <verify>
    <automated>uv run pytest tests/test_storyboard.py tests/test_timing.py -q 2>&1 | grep -Eq '([0-9]+ passed)' && uv run pytest tests/test_slides_render.py -q 2>&1 | grep -Eq '(passed|skipped)' && echo TESTS_COVER_OK</automated>
  </verify>
  <done>Los tres tests pasan (test_slides_render puede hacer skip si falta Chromium). TEST-01 sin llamada real a la API, TEST-02 con suma exacta + word budget, TEST-03 con assert 1920×1080. Sin reescrituras innecesarias.</done>
</task>

<task type="auto">
  <name>Task 2: Cerrar gap mínimo (solo si existe) + confirmar suite completa verde</name>
  <files>tests/test_storyboard.py, tests/test_timing.py, tests/test_slides_render.py</files>
  <read_first>tests/test_storyboard.py, tests/test_timing.py, tests/test_slides_render.py</read_first>
  <action>
Solo si el Task 1 reveló un gap REAL frente a la columna "Observable signal" de 07-VALIDATION.md, añadir UNA aserción o test focalizado para cerrarlo. Gaps candidatos a comprobar (probablemente ya cubiertos — no añadir si ya están):

- TEST-01: ¿hay una aserción explícita de que NO se hace una llamada de red real? Ya está implícito por el mock de `call_structured` y `assert mock_cs.called`. Si quieres reforzar, añade `assert mock_cs.call_count == 1`. SOLO si no existe ya una aserción equivalente.
- TEST-02: el reparto exacto y el word budget ya están parametrizados. No añadir.
- TEST-03: el assert de tamaño 1920×1080 ya existe. No añadir.

Si NO hay gap (caso esperado), NO modificar ningún archivo — dejar los tests tal cual.

En ambos casos, confirmar al final que la suite COMPLETA está verde:
- `uv run pytest -q` → 0 fallos, ≥303 tests (skips solo aceptables en render sin Chromium).

PROHIBIDO: reescribir tests existentes, duplicar casos ya cubiertos, bajar el número total de tests.
  </action>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3 | grep -Eq '([0-9]+ passed)' && ! uv run pytest -q 2>&1 | grep -Eq '[0-9]+ failed' && echo SUITE_GREEN</automated>
  </verify>
  <done>La suite completa pasa sin fallos (≥303 tests; skips solo en render sin Chromium). Cualquier gap real cerrado con un cambio mínimo; si no había gap, ningún archivo modificado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → API externa (Anthropic) | Los tests NO deben tocar la red ni requerir API keys reales |
| CI/local → entorno de test | Los tests deben pasar de forma determinista y offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure | API keys en tests | mitigate | TEST-01 mockea `call_structured` — ninguna clave real ni llamada de red. Verificado por `assert mock_cs.called`. |
| T-07-05 | Denial of Service | Dependencia de red/Chromium en CI | accept | test_slides_render hace skip limpio si Chromium no está; TEST-01/02 son offline puros. Sin flakiness por red. |
</threat_model>

<verification>
- Subconjunto rápido verde: `uv run pytest tests/test_storyboard.py tests/test_timing.py tests/test_slides_render.py -q`.
- TEST-01 no hace llamada real (mock de call_structured).
- TEST-02 asserta suma exacta == duration y word_budget == round(seconds*wpm/60).
- TEST-03 asserta PNG 1920×1080 (o skip sin Chromium).
- Suite completa verde: `uv run pytest -q` (≥303 tests, 0 fallos).
</verification>

<success_criteria>
- TEST-01, TEST-02, TEST-03 verificados como cubriendo sus requisitos.
- Gap real (si lo hubo) cerrado con cambio mínimo; sin duplicación.
- Suite completa verde como guarda de regresión.
</success_criteria>

<output>
After completion, create `.planning/phases/07-empaquetado-tests-docs/07-02-SUMMARY.md`
</output>
