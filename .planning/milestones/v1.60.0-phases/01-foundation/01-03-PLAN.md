---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/avideo/orchestrator.py
  - src/avideo/stages/__init__.py
  - src/avideo/stages/base.py
  - src/avideo/stages/stubs.py
  - src/avideo/utils/rich_ui.py
  - src/avideo/utils/cost_estimator.py
  - tests/test_orchestrator.py
autonomous: false
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04, CLI-06, CLI-08]
must_haves:
  truths:
    - "Running the pipeline executes all stub stages in order and writes a valid checkpoint per stage so output.mp4 marker exists at the end"
    - "Re-running after completion skips every already-done stage (idempotent — no duplicated work)"
    - "Interrupting mid-run then re-running resumes: completed stages skip, remaining stages run"
    - "--level 1 pauses after every stage; --level 4 never pauses; --level 2 pauses only on creative stages; --level 3 pauses only on warning/fail"
    - "--dry-run prints a Rich cost/token table per stage + total and exits WITHOUT running any stage (no audio, no video)"
  artifacts:
    - path: "src/avideo/stages/base.py"
      provides: "StageProtocol (typing.Protocol) + CheckpointMixin"
      contains: "Protocol"
    - path: "src/avideo/stages/stubs.py"
      provides: "All Phase-1 stub stages writing minimal valid Pydantic outputs"
      contains: "PIPELINE_STAGES"
    - path: "src/avideo/orchestrator.py"
      provides: "run_pipeline: sequential loop, skip-done, approval gates, dry-run branch"
      contains: "def run_pipeline"
    - path: "src/avideo/utils/cost_estimator.py"
      provides: "Static per-stage cost/token estimate table for --dry-run"
      contains: "def estimate_all"
    - path: "tests/test_orchestrator.py"
      provides: "skip-done, resume, approval-gate, dry-run tests"
      contains: "def test_"
  key_links:
    - from: "src/avideo/orchestrator.py"
      to: "src/avideo/utils/workdir.py (WorkdirManager)"
      via: "is_done / write_checkpoint / mark_done"
      pattern: "mark_done"
    - from: "src/avideo/orchestrator.py"
      to: "src/avideo/stages/stubs.py (PIPELINE_STAGES)"
      via: "for stage in PIPELINE_STAGES"
      pattern: "PIPELINE_STAGES"
    - from: "src/avideo/orchestrator.py"
      to: "src/avideo/utils/rich_ui.py (pause_for_approval)"
      via: "should_pause → pause_for_approval"
      pattern: "pause_for_approval"
    - from: "src/avideo/orchestrator.py"
      to: "src/avideo/utils/cost_estimator.py (estimate_all)"
      via: "dry_run branch"
      pattern: "estimate_all"
---

<objective>
Build the sequential orchestrator that turns the stub stages into a working end-to-end pipeline: StageProtocol + CheckpointMixin contract, all Phase-1 stub stages (each writes a minimal valid Pydantic checkpoint), the run loop with skip-done/atomic-checkpoint/done-marker logic, the L1–L4 approval gates, the --dry-run cost-table branch, and the Rich progress/approval UI additions.

Purpose: This is the spine of the project — the resumable, idempotent, level-aware executor. It is the phase acceptance command's behavior (`avideo generate --bullets bullets.yaml --duration 120` runs all stub stages, resumes after interrupt, respects levels, dry-runs).
Output: `src/avideo/orchestrator.py`, `stages/base.py`, `stages/stubs.py`, `utils/cost_estimator.py`, rich_ui approval/progress additions; passing orchestrator tests; phase acceptance command green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-PLAN.md
@.planning/phases/01-foundation/01-02-PLAN.md

<interfaces>
<!-- Created by 01-01 and 01-02. Import directly; do not re-explore. -->

From src/avideo/utils/workdir.py (01-01):
```python
class WorkdirManager:
    def __init__(self, root: Path) -> None: ...
    def checkpoint_path(self, name: str) -> Path: ...
    def is_done(self, stage: str) -> bool: ...
    def mark_done(self, stage: str) -> None: ...
    def write_checkpoint(self, name: str, model: BaseModel) -> None: ...   # atomic os.replace
    def read_checkpoint(self, name: str, model_class: type[BaseModel]) -> BaseModel: ...
```

From src/avideo/models (01-01) — output models + their checkpoint base names:
ContextOutput("context"), StoryboardOutput+SlideSpec("storyboard"), TimingOutput+SlideTiming("timings"),
ScriptOutput+SlideScript("script"), SlidesOutput("slides"), VerificationReport+SlideVerdict("verification"),
VoiceOutput("voice"), AssemblyOutput+QAReport("assembly"). RunConfig has: level:int(1..4), dry_run:bool,
voice:VoiceMode, slides_mode:SlidesMode, workdir:Path, context:Optional[Path].

From src/avideo/utils/rich_ui.py (01-02 created console/show_validation_error/setup_logging; THIS plan ADDS):
```python
def pause_for_approval(stage_name: str, reason: str = "") -> None   # Console.input gate; raises typer.Abort on decline
def stage_progress(stages: list[str])                                # context manager / Progress wrapper (optional helper)
```

This plan CREATES the orchestrator entry the CLI already calls:
```python
# src/avideo/orchestrator.py
def run_pipeline(config: RunConfig) -> None
def should_pause(stage_name: str, level: int, has_fail: bool = False) -> bool
PIPELINE_STAGES: list[StageProtocol]   # (re-exported from stages.stubs)
```
</interfaces>

<level_semantics>
<!-- Locked in CONTEXT.md lines 18-19. Implement EXACTLY. -->
- L1 = pause after EVERY stage
- L2 = pause only on creative checkpoints: {"storyboard", "script", "slides", "verify"}
- L3 = pause only when a stage produced a warning/fail (Phase 1 stubs never fail, so L3 effectively never pauses; logic must still be present and correct)
- L4 = never pause (fully autonomous)

CREATIVE_STAGES = {"storyboard", "scriptwriter", "slides", "verify"}   # use ACTUAL stage_names (scriptwriter, not script)
FAIL_STAGES = {"verify"}   # only the verifier can emit a "fail" verdict

should_pause(stage_name, level, has_fail=False):
  if level == 4: return False
  if level == 1: return True
  if level == 2: return stage_name in CREATIVE_STAGES
  if level == 3: return stage_name in FAIL_STAGES and has_fail
  return False
</level_semantics>

<pipeline_order>
<!-- The canonical stub stage order. checkpoint base names in parens. Must match the workdir layout (CONTEXT.md) and what Phases 2-5 consume. -->
context(context) → storyboard(storyboard) → timing(timings) → scriptwriter(script)
→ slides(slides) → verify(verification) → voice(voice) → align(align) → subs(subs)
→ assemble(assembly)

Notes:
- context stage is SKIPPED-as-unused (writes ContextOutput(used=False)) when config.context is None — pipeline still works without context (CTX-02 forward-compat).
- verify stage runs as a stub here; its real visual logic arrives in Phase 6. Stub writes VerificationReport with one SlideVerdict(status="ok").
- align/subs are lightweight stubs in Phase 1 (real logic Phase 4); align writes a trivial checkpoint, subs writes a trivial checkpoint. Keep stage_name "align" and "subs".
- assemble is the FINAL stage: writes AssemblyOutput(output_path="workdir/output.mp4") AND creates the output.mp4 marker file so the phase acceptance criterion "obtiene un vídeo MP4 final" (stubbed) holds.
</pipeline_order>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StageProtocol + CheckpointMixin + all Phase-1 stub stages + PIPELINE_STAGES</name>
  <files>src/avideo/stages/__init__.py, src/avideo/stages/base.py, src/avideo/stages/stubs.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 StageProtocol + stub lines 389-427; project structure note on stubs.py lines 240-241; Open Question 3 minimal stub outputs lines 707-710)
    - .planning/phases/01-foundation/01-CONTEXT.md (StageProtocol run/checkpoint + CheckpointMixin line 20; stub writes minimal valid checkpoint line 17)
    - src/avideo/models/__init__.py (all output models the stubs instantiate)
    - src/avideo/utils/workdir.py (WorkdirManager API the stubs/orchestrator use)
  </read_first>
  <behavior>
    - Test: every stage in PIPELINE_STAGES has a non-empty `stage_name` and a callable `run(workdir, config)` (ORCH-01)
    - Test: each stub.run returns a Pydantic BaseModel instance whose type matches its declared output (ORCH-05)
    - Test: StoryboardStub().run(...) returns a StoryboardOutput with at least one SlideSpec
    - Test: ContextStub writes ContextOutput(used=False) when config.context is None
    - Test: AssembleStub.run produces AssemblyOutput(output_path ends with "output.mp4") and creates the output.mp4 file
    - Test: isinstance(stage, StageProtocol) is True for each stage (runtime_checkable Protocol)
    - Test: PIPELINE_STAGES order is exactly [context, storyboard, timing, scriptwriter, slides, verify, voice, align, subs, assemble] by stage_name
  </behavior>
  <action>
    Write tests FIRST (in tests/test_orchestrator.py — shared with Task 2; create the file here with stage tests, Task 2 appends loop tests), run RED, implement to GREEN.

    base.py:
    - `@runtime_checkable class StageProtocol(Protocol)` with attribute `stage_name: str`, attribute `checkpoint_name: str`, and methods `def run(self, workdir: WorkdirManager, config: RunConfig) -> BaseModel: ...` and `def is_done(self, workdir: WorkdirManager) -> bool: ...`. Choose the `run(workdir, config)` signature (uniform across ALL stages) so stages can read config.context etc.
    - `class CheckpointMixin`: `stage_name: str = ""` (override per stub); a `checkpoint_name` property defaulting to `stage_name` (override only where they differ); `def is_done(self, workdir) -> bool: return workdir.is_done(self.stage_name)`. Stubs inherit this.
    - Module docstring referencing the StageProtocol contract that Phases 2-5 must satisfy.

    stubs.py — one stub class per pipeline stage, each subclassing CheckpointMixin, each with a `stage_name` class attr and a `run(self, workdir, config)` returning the minimal valid output model (required fields only, per Open Question 3). Concrete:
    - `ContextStub` (stage_name="context") → if config.context is None: ContextOutput(used=False); else ContextOutput(source_path=str(config.context), text="", used=True).
    - `StoryboardStub` (stage_name="storyboard") → StoryboardOutput(slides=[SlideSpec(title="Stub Slide", bullets=["Bullet 1"], visual_type="text")], language=config.language).
    - `TimingStub` (stage_name="timing", checkpoint_name="timings") → TimingOutput(slides=[SlideTiming(slide_index=0, seconds=float(config.duration), word_budget=10)], total_seconds=float(config.duration), wpm=config.wpm).
    - `ScriptwriterStub` (stage_name="scriptwriter", checkpoint_name="script") → ScriptOutput(slides=[SlideScript(slide_index=0, narration="Narración stub.")], language=config.language).
    - `SlidesStub` (stage_name="slides") → SlidesOutput(png_paths=[], mode=config.slides_mode.value).
    - `VerifyStub` (stage_name="verify", checkpoint_name="verification") → VerificationReport(slides=[SlideVerdict(slide_index=0, status="ok")]).
    - `VoiceStub` (stage_name="voice") → VoiceOutput(audio_paths=[], voice_mode=config.voice.value).
    - `AlignStub` (stage_name="align", checkpoint_name="align") → Phase-1 placeholder shape: TimingOutput(slides=[], total_seconds=0.0, wpm=config.wpm). Docstring: "Phase-1 placeholder; Phase 4 replaces with a word-level alignment model."
    - `SubsStub` (stage_name="subs", checkpoint_name="subs") → Phase-1 placeholder shape: ScriptOutput(slides=[], language=config.language). Docstring: "Phase-1 placeholder; Phase 4 replaces with a subtitles model."
    - `AssembleStub` (stage_name="assemble", checkpoint_name="assembly") → `(workdir.root / "output.mp4").touch()` then return AssemblyOutput(output_path=str(workdir.root / "output.mp4")).

    Checkpoint-name handling: the orchestrator (Task 2) uses `stage.checkpoint_name` for write_checkpoint and `stage.stage_name` for done markers. Defaults: checkpoint_name == stage_name; override only for timing/scriptwriter/verify/assemble as above.

    `PIPELINE_STAGES: list = [ContextStub(), StoryboardStub(), TimingStub(), ScriptwriterStub(), SlidesStub(), VerifyStub(), VoiceStub(), AlignStub(), SubsStub(), AssembleStub()]`. Export from stages/__init__.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py -x -q -k "stage or pipeline_order or protocol or stub"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0
    - `grep -q "class StageProtocol" src/avideo/stages/base.py` and `grep -q "Protocol" src/avideo/stages/base.py` and `grep -q "runtime_checkable" src/avideo/stages/base.py`
    - `grep -q "class CheckpointMixin" src/avideo/stages/base.py`
    - `grep -q "PIPELINE_STAGES" src/avideo/stages/stubs.py`
    - `grep -q "checkpoint_name" src/avideo/stages/base.py` (distinct from stage_name)
    - `grep -q "output.mp4" src/avideo/stages/stubs.py` (assemble stub creates the final marker)
    - `uv run python -c "from avideo.stages.stubs import PIPELINE_STAGES; names=[s.stage_name for s in PIPELINE_STAGES]; assert names==['context','storyboard','timing','scriptwriter','slides','verify','voice','align','subs','assemble'], names; print('ok')" | grep -q ok`
    - `uv run python -c "from avideo.stages.base import StageProtocol; from avideo.stages.stubs import PIPELINE_STAGES; assert all(isinstance(s, StageProtocol) for s in PIPELINE_STAGES); print('ok')" | grep -q ok`
  </acceptance_criteria>
  <done>StageProtocol/CheckpointMixin define the uniform contract; ten stub stages each return a valid minimal output (with checkpoint_name mapping for timing/scriptwriter/verify/assemble); PIPELINE_STAGES is in canonical order; assemble stub emits output.mp4 marker; stage tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrator loop (skip-done, atomic checkpoint, approval gates, dry-run) + rich_ui approval/progress + cost_estimator</name>
  <files>src/avideo/orchestrator.py, src/avideo/utils/rich_ui.py, src/avideo/utils/cost_estimator.py, tests/test_orchestrator.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 orchestrator loop lines 429-476; Pitfall 4 done-marker-after-exception lines 561-577; dry-run row CLI-06 line 63; Architectural Responsibility Map lines 75-87)
    - .planning/phases/01-foundation/01-CONTEXT.md (level semantics line 19; --dry-run Rich table line 24; reanudación doble ejecución lines 56-57)
    - src/avideo/stages/base.py and src/avideo/stages/stubs.py (PIPELINE_STAGES, checkpoint_name — Task 1)
    - src/avideo/utils/workdir.py (is_done/write_checkpoint/mark_done — 01-01)
    - src/avideo/utils/rich_ui.py (existing console/setup_logging/show_validation_error from 01-02 — you APPEND, do not remove)
    - src/avideo/models/config.py (RunConfig.level/dry_run/workdir)
  </read_first>
  <behavior>
    - Test (ORCH-01): run_pipeline(config level=4) executes all 10 stages; afterwards workdir.is_done is True for all stage_names and output.mp4 exists
    - Test (ORCH-02/ORCH-03 resume+idempotent): after a full run, calling run_pipeline again runs ZERO stage.run() calls (spy/mock counts) — all skipped
    - Test (resume after interrupt): mark first 3 stages done manually, run_pipeline, assert only the remaining 7 stages' run() are invoked
    - Test (ORCH-04 L4): with level=4, pause_for_approval (monkeypatched) is called 0 times
    - Test (ORCH-04 L1): with level=1, pause_for_approval is called once per executed stage (10 times on a fresh run)
    - Test (ORCH-04 L2): with level=2, pause_for_approval called exactly 4 times — for stage_names {storyboard, scriptwriter, slides, verify}
    - Test (CLI-06 dry-run): run_pipeline(config dry_run=True) calls estimate_all and invokes ZERO stage.run(); no output.mp4 created; estimate_all prints a table with a row per stage and a total
    - Test (CLI-08): a Rich progress indicator is used during the run (assert progress object created OR console.print called per stage) — keep lightweight
    - Test (Pitfall 4): if a stage.run raises, mark_done is NOT called for that stage (is_done stays False) and the exception propagates
  </behavior>
  <action>
    Write tests FIRST (append to tests/test_orchestrator.py), run RED, implement to GREEN.

    rich_ui.py ADDITIONS (append; keep console/show_validation_error/setup_logging intact):
    - `def pause_for_approval(stage_name: str, reason: str = "") -> None`: print a Rich line naming the stage and reason, then prompt with `rich.prompt.Confirm.ask(...)` (or `console.input("Continue? [y/N] ")`). If the user declines, raise `typer.Abort()`. Must be a module-level function so tests can monkeypatch it.
    - `def make_progress() -> Progress` returning a configured `rich.progress.Progress(transient=True)`. Keep minimal.

    cost_estimator.py:
    - `STAGE_COSTS: dict[str, dict[str, float]]` static placeholder, one entry per stage_name in PIPELINE_STAGES, each with `{"tokens": <int>, "usd": <float>}` (Phase 1 = static numbers; real estimation in Phase 2).
    - `def estimate_all(config: RunConfig) -> None`: build `rich.table.Table("Stage", "Est. tokens", "Est. cost (USD)")`; add a row per stage from STAGE_COSTS; add a TOTAL row summing tokens and usd; print to rich_ui.console. Returns None; runs NO stage.
    - Docstrings noting these are placeholders.

    orchestrator.py:
    - Import PIPELINE_STAGES from stages.stubs; WorkdirManager from utils.workdir; from utils.rich_ui import console, pause_for_approval, make_progress; estimate_all from utils.cost_estimator; RunConfig from models.
    - `CREATIVE_STAGES = {"storyboard", "scriptwriter", "slides", "verify"}` and `FAIL_STAGES = {"verify"}`.
    - `def should_pause(stage_name: str, level: int, has_fail: bool = False) -> bool` implementing the level_semantics block EXACTLY.
    - `def run_pipeline(config: RunConfig) -> None`:
      1. `workdir = WorkdirManager(config.workdir)`.
      2. `if config.dry_run: estimate_all(config); return` (no stage runs, no output.mp4).
      3. `for stage in PIPELINE_STAGES`:
         - `if workdir.is_done(stage.stage_name): console.print(f"[dim]Skipping {stage.stage_name} (done)[/dim]"); continue`.
         - `if should_pause(stage.stage_name, config.level): pause_for_approval(stage.stage_name)`.
         - Happy path EXACTLY in this order (Pitfall 4 — mark_done LAST, never in an except branch):
           ```
           output = stage.run(workdir, config)
           workdir.write_checkpoint(stage.checkpoint_name, output)
           workdir.mark_done(stage.stage_name)
           console.print(f"[green]Done:[/green] {stage.stage_name}")
           ```
         - Trap `KeyboardInterrupt`: print "Interrupted at {stage}" and re-raise (or raise typer.Exit(130)); do NOT mark_done on interrupt.
      4. Wrap the loop with `make_progress()` (advance once per stage) for CLI-08; transient=True so tests run without a TTY.
    - Module docstring.

    Wire-up check: cli.py (01-02) already calls `avideo.orchestrator.run_pipeline(config)`; confirm the symbol name matches — no cli.py edits.
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_orchestrator.py -x -q` exits 0
    - `grep -q "def run_pipeline" src/avideo/orchestrator.py`
    - `grep -q "def should_pause" src/avideo/orchestrator.py`
    - `grep -q "CREATIVE_STAGES" src/avideo/orchestrator.py` and `grep -q "scriptwriter" src/avideo/orchestrator.py` (creative set uses real stage_names)
    - mark_done ordering: `uv run python -c "s=open('src/avideo/orchestrator.py').read(); assert s.index('write_checkpoint') < s.index('mark_done'); print('ok')" | grep -q ok`
    - `grep -q "estimate_all" src/avideo/orchestrator.py` and `grep -q "config.dry_run" src/avideo/orchestrator.py`
    - `grep -q "def pause_for_approval" src/avideo/utils/rich_ui.py` AND `grep -q "def show_validation_error" src/avideo/utils/rich_ui.py` (01-02 functions preserved)
    - `grep -q "def estimate_all" src/avideo/utils/cost_estimator.py` and `grep -q "Table" src/avideo/utils/cost_estimator.py`
    - dry_run branch creates no output.mp4: test asserts this — `grep -q "output.mp4" tests/test_orchestrator.py`
  </acceptance_criteria>
  <done>Orchestrator runs all stubs in order, skips done stages (idempotent + resumable), enforces L1–L4 exactly (creative set uses real stage_names), marks done only after atomic checkpoint, dry-run prints a cost table and runs nothing; rich_ui gains pause_for_approval/progress without losing 01-02 functions; test_orchestrator.py green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase acceptance — end-to-end stub run, resume, levels, dry-run</name>
  <files>workdir/ (runtime state), bullets.yaml, config.yaml</files>
  <what-built>Full Phase-1 skeleton: `avideo generate` runs all 10 stub stages, resumes after interrupt, respects L1–L4, and supports --dry-run. Claude has already run the automated tests (all green); this checkpoint is a hands-on confirmation of the acceptance command from CONTEXT.md.</what-built>
  <action>Manual verification by the user — no code changes. Claude pauses here after Tasks 1-2 are green and presents the seven commands below for the user to run and confirm.</action>
  <how-to-verify>
    Run each and confirm the described behavior:
    1. Fresh run (delete workdir first): `rm -rf workdir && uv run avideo generate --bullets bullets.yaml --duration 120`
       Expect: each of the 10 stages prints "Done:", and `workdir/output.mp4` exists at the end. No errors.
    2. Idempotent re-run: `uv run avideo generate --bullets bullets.yaml --duration 120`
       Expect: every stage prints "Skipping ... (done)"; no stage re-runs.
    3. Resume after interrupt: `rm -rf workdir`, start the run, press Ctrl-C after a couple stages, then re-run the same command.
       Expect: completed stages skip, the rest run to completion.
    4. Level 1 pauses: `rm -rf workdir && uv run avideo generate --bullets bullets.yaml --duration 120 --level 1`
       Expect: pipeline pauses for approval after EACH stage.
    5. Level 4 never pauses: `rm -rf workdir && uv run avideo generate --bullets bullets.yaml --duration 120 --level 4`
       Expect: runs straight through, no prompts.
    6. Dry run: `uv run avideo generate --bullets bullets.yaml --duration 120 --dry-run`
       Expect: a Rich table of per-stage token/cost estimates + a total; NO stages run; no output.mp4 created/updated.
    7. Bad input: `uv run avideo generate --bullets bullets.yaml --duration 120 --level 5`
       Expect: a clean Rich error (Configuration Error / range message), exit code non-zero, NO Python traceback.
  </how-to-verify>
  <verify>Manual: user runs the seven commands above and confirms each behavior.</verify>
  <done>All seven acceptance behaviors confirmed by the user (or discrepancies reported for fixing).</done>
  <resume-signal>Type "approved" if all seven behaviors match, or describe what differed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (workdir) → orchestrator | Done markers / checkpoints read at loop start are filesystem state that controls execution |
| stdin → approval gate | User confirm/decline input at pause points |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | Stale/partial checkpoint causing a done stage to be skipped with bad data | mitigate | Atomic write (01-01 os.replace) + mark_done strictly after write_checkpoint (orchestrator ordering, asserted by acceptance criterion + Pitfall-4 test) |
| T-01-09 | Elevation of Privilege | Approval gate bypassed (L1/L2 not pausing) | mitigate | should_pause unit tests assert exact pause counts per level; CREATIVE_STAGES/FAIL_STAGES sets pinned |
| T-01-10 | Denial of Service | Unhandled KeyboardInterrupt corrupting state | accept | Interrupt is trapped to print a clean message and re-raise; because mark_done is last, an interrupt cannot leave a stage falsely-done — no corruption, low risk |
</threat_model>

<verification>
- `uv run pytest tests/test_orchestrator.py -x -q` green
- `uv run pytest tests/ -v --tb=short` full suite green (models + workdir + cli + orchestrator)
- Manual acceptance (Task 3 checkpoint): all seven behaviors confirmed
- Phase acceptance command `avideo generate --bullets bullets.yaml --duration 120` runs end-to-end and produces workdir/output.mp4 (stub)
</verification>

<success_criteria>
- All 10 stub stages execute in canonical order; output.mp4 marker produced
- Re-run skips all done stages (idempotent); interrupt+rerun resumes correctly
- L1 pauses after every stage; L2 pauses on the 4 creative stages; L3 never pauses for stubs; L4 never pauses
- --dry-run prints a per-stage + total cost/token Rich table and runs no stage / writes no output.mp4
- mark_done occurs only after a successful atomic checkpoint write (Pitfall 4)
- Full test suite green; human acceptance checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
