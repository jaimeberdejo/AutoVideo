---
phase: 08-backend-integrations
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_voice_openai.py
  - tests/test_audio_enhance.py
  - tests/test_ffmpeg_music.py
autonomous: true
requirements:
  - VOZ-02
  - VOZ-03
  - EXT-02
  - EXT-03

must_haves:
  truths:
    - "tests/test_voice_openai.py exists with RED failing tests for VoiceOpenAIStage, lazy client, 4096-char guard, and STT mapping"
    - "tests/test_audio_enhance.py exists with RED failing tests for enhance_audio filter string and non-destructive write"
    - "tests/test_ffmpeg_music.py exists with RED failing tests for build_music_mix_args normalize=0 and single loudnorm"
    - "uv run pytest tests/test_voice_openai.py tests/test_audio_enhance.py tests/test_ffmpeg_music.py collects all tests (even if they fail)"
    - "303 existing tests still pass after adding new test files"
  artifacts:
    - path: "tests/test_voice_openai.py"
      provides: "VOZ-02 test scaffold"
      min_lines: 80
    - path: "tests/test_audio_enhance.py"
      provides: "VOZ-03 test scaffold"
      min_lines: 40
    - path: "tests/test_ffmpeg_music.py"
      provides: "EXT-02/EXT-03 test scaffold"
      min_lines: 60
  key_links:
    - from: "tests/test_voice_openai.py"
      to: "avideo.integrations.openai._get_client"
      via: "mocker.patch mock seam"
      pattern: "_get_client"
    - from: "tests/test_ffmpeg_music.py"
      to: "avideo.integrations.ffmpeg.build_music_mix_args"
      via: "direct builder call (no mock needed)"
      pattern: "build_music_mix_args"
---

<objective>
Create Wave 0 test scaffolds for all three new Phase 8 capabilities. Tests reference modules that do not yet exist — they MUST fail (RED) at collection time or on the first run. They turn GREEN when Wave 1–3 implementation plans land. This plan gates all subsequent implementation: no production code is written until test seams are in place.

Purpose: Enforce Nyquist compliance — every behavior tested before it is implemented.
Output: Three new test files with deferred avideo imports; they collect without error even before implementation modules exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-backend-integrations/08-VALIDATION.md
@.planning/phases/08-backend-integrations/08-PATTERNS.md
@.planning/phases/08-backend-integrations/08-RESEARCH.md

<interfaces>
<!-- Analog test files to mirror exactly — deferred imports, SimpleNamespace mocks, fixture patterns -->

From tests/test_voice_elevenlabs.py (module docstring + import pattern):
```python
"""Tests for VoiceElevenlabsStage..."""
from __future__ import annotations
import json, types
from pathlib import Path
import pytest
# ALL avideo imports deferred to inside test bodies with # noqa: PLC0415
```

From tests/test_assemble.py (mock seam pattern for ffmpeg):
```python
with patch("avideo.stages.assemble.run_ffmpeg", side_effect=...), \
     patch("avideo.stages.assemble.probe_duration", side_effect=...):
    ...
# Pure builder tests need NO mock:
def test_build_assemble_args_returns_list():
    from avideo.integrations.ffmpeg import build_assemble_args  # noqa: PLC0415
    args = build_assemble_args(...)
    assert isinstance(args, list)
```

From tests/conftest.py (WorkdirManager fixture):
```python
# conftest provides tmp_path-backed WorkdirManager via wm fixture or direct construction
```

Target mock seam for openai integration (from 08-RESEARCH.md):
```python
mocker.patch("avideo.integrations.openai._get_client", return_value=mock_client)
mock_client.audio.speech.create.return_value.stream_to_file = lambda p: Path(p).write_bytes(b"\xff\xe3\x10\x00")
mock_client.audio.transcriptions.create.return_value = types.SimpleNamespace(
    words=[types.SimpleNamespace(word="hola", start=0.0, end=0.4)]
)
```

Target mock seam for audio_enhance (from 08-PATTERNS.md):
```python
mocker.patch("avideo.utils.audio_enhance.run_ffmpeg")
```

RunConfig constructor for openai tests (from 08-PATTERNS.md):
```python
config = RunConfig(bullets=bullets, duration=60, voice=VoiceMode.openai)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_voice_openai.py (VOZ-02 scaffold)</name>
  <files>tests/test_voice_openai.py</files>

  <read_first>
    - tests/test_voice_elevenlabs.py (analog — copy module docstring style, deferred imports, _write_script helper, mock seam pattern, dispatcher test, lazy-client safety test)
    - tests/conftest.py (available fixtures: wm, tmp_path, RunConfig construction pattern)
    - .planning/phases/08-backend-integrations/08-RESEARCH.md — Validation Architecture section (exact test names and commands)
    - .planning/phases/08-backend-integrations/08-PATTERNS.md — tests/test_voice_openai.py section (SimpleNamespace mock shape)
  </read_first>

  <action>
Create tests/test_voice_openai.py with deferred avideo imports (# noqa: PLC0415 inside every test body — NEVER at file top level). Include:

1. Module docstring explaining Wave 0 purpose, RED until Wave 1+2 land, mock points.

2. Helper `_write_script(workdir_manager, slides)` — defers from avideo.models.script import, mirrors test_voice_elevenlabs.py lines 22-31 exactly.

3. Helper `_make_fake_synthesize(tmp_path)` — returns a side_effect callable that writes 4 dummy MP3 bytes (b"\xff\xe3\x10\x00") to out_path and returns None (synthesis writes to disk, Stage reads disk).

4. Helper `_make_fake_transcribe(slide_index)` — returns a SimpleNamespace(words=[SimpleNamespace(word="test", start=0.0, end=0.5)]) value for transcribe_slide_openai return_value.

5. Class TestVoiceOpenAIStage with these test methods (all deferred imports inside):
   - test_run_returns_unified_timings: write 2-slide script checkpoint; patch avideo.stages.voice_openai.synthesize_slide_openai side_effect=_make_fake_synthesize; patch avideo.stages.voice_openai.transcribe_slide_openai return_value=SlideTimings(...); call VoiceOpenAIStage().run(wm, config); assert result.source == "openai"; assert len(result.slides) == 2
   - test_voice_stage_dispatches_openai: write script, patch both integration functions, call VoiceStage().run(wm, config with voice=VoiceMode.openai); assert result.source == "openai"
   - test_4096_char_limit: patch _get_client; call synthesize_slide_openai(text="x"*4097, slide_index=0, model="tts-1", voice="nova", out_path=tmp_path/"a.mp3"); expect pytest.raises(ValueError)
   - test_transcribe_maps_word_objects: patch avideo.integrations.openai._get_client; mock transcriptions.create returns SimpleNamespace(words=[SimpleNamespace(word="hola", start=0.0, end=0.4), SimpleNamespace(word="mundo", start=0.5, end=0.9)]); call transcribe_slide_openai(audio_path=Path("dummy.mp3"), slide_index=0); assert len(result.words) == 2; assert result.words[0].text == "hola"; assert result.words[0].start == 0.0
   - test_lazy_client_not_instantiated_at_import: `import avideo.integrations.openai as mod; assert mod._client is None` (import must not create the client)
   - test_synthesize_slide_openai_at_module_scope: `import avideo.stages.voice_openai as mod; assert hasattr(mod, "synthesize_slide_openai")` (mirrors elevenlabs import-scope test)
   - test_transcribe_slide_openai_at_module_scope: same but for transcribe_slide_openai

All RunConfig constructions: `RunConfig(bullets=bullets_file, duration=60, voice=VoiceMode.openai)` where bullets_file = tmp_path / "b.yaml" written with "title: T\nbullets:\n  - x\n".
  </action>

  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_voice_openai.py --collect-only -q 2>&1 | grep -E "test session|collected|error" | head -10</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f tests/test_voice_openai.py`
    - Module docstring present: `grep -c "Wave 0" tests/test_voice_openai.py` returns >= 1
    - Mock seam referenced: `grep -c "_get_client" tests/test_voice_openai.py` returns >= 2
    - Deferred import pattern: `grep -c "noqa: PLC0415" tests/test_voice_openai.py` returns >= 3
    - Lazy client test present: `grep -c "test_lazy_client_not_instantiated_at_import" tests/test_voice_openai.py` returns 1
    - 4096 char test present: `grep -c "test_4096_char_limit" tests/test_voice_openai.py` returns 1
    - Dispatcher test present: `grep -c "test_voice_stage_dispatches_openai" tests/test_voice_openai.py` returns 1
    - Collection succeeds (no SyntaxError): `uv run pytest tests/test_voice_openai.py --collect-only -q 2>&1 | grep -v "^$" | tail -3` shows "N tests" line
  </acceptance_criteria>

  <done>tests/test_voice_openai.py exists, collects without error, contains all 7 test methods covering VOZ-02 behaviors; tests are RED (ImportError or AttributeError on avideo.stages.voice_openai which does not yet exist)</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_audio_enhance.py and tests/test_ffmpeg_music.py (VOZ-03, EXT-02, EXT-03 scaffolds)</name>
  <files>tests/test_audio_enhance.py, tests/test_ffmpeg_music.py</files>

  <read_first>
    - tests/test_assemble.py lines 1-30 (module docstring style, deferred imports, mock seam pattern for run_ffmpeg)
    - tests/test_assemble.py lines 194-263 (pure builder test pattern — no mock needed for list[str] builders)
    - tests/test_assemble.py lines 506-509 (skipif ffmpeg guard pattern)
    - .planning/phases/08-backend-integrations/08-VALIDATION.md — Wave 0 Requirements and Per-Task Verification Map
    - .planning/phases/08-backend-integrations/08-RESEARCH.md — Validation Architecture / Test Seams section
    - .planning/phases/08-backend-integrations/08-PATTERNS.md — tests/test_audio_enhance.py section
  </read_first>

  <action>
**File A: tests/test_audio_enhance.py**

Module docstring: "Wave 0 test scaffold for Phase 8 audio enhancement (VOZ-03). All avideo imports deferred. Tests are RED until utils/audio_enhance.py lands (Wave 2b)."

Imports: from __future__ import annotations; from pathlib import Path; import pytest; from unittest.mock import patch, call

Class TestEnhanceAudio:
  - test_enhance_calls_run_ffmpeg: patch "avideo.utils.audio_enhance.run_ffmpeg"; call enhance_audio(Path("/tmp/in.wav"), Path("/tmp/out.wav")); assert mock was called once; args_str = " ".join(mock.call_args[0][0]); assert "afftdn=nr=6:nf=-25" in args_str; assert "loudnorm=I=-16:TP=-1.5:LRA=11" in args_str
  - test_nondestructive: patch "avideo.utils.audio_enhance.run_ffmpeg"; in_path = tmp_path/"in.wav"; out_path = tmp_path/"out.wav"; in_path.write_bytes(b"fake"); call enhance_audio(in_path, out_path); assert not out_path.exists() (run_ffmpeg is mocked — it does NOT write the file); assert in_path.read_bytes() == b"fake" (original unchanged)
  - test_filter_chain_order: patch run_ffmpeg; call enhance_audio; check "afftdn=nr=6:nf=-25,loudnorm=I=-16:TP=-1.5:LRA=11" appears as a single -af value (both filters in one string, comma-joined, in that order)
  - test_no_shell_true: patch run_ffmpeg; call enhance_audio; assert the call used a list (isinstance(mock.call_args[0][0], list)) — shell=True would use a string

All avideo imports deferred inside test bodies: `from avideo.utils.audio_enhance import enhance_audio  # noqa: PLC0415`

**File B: tests/test_ffmpeg_music.py**

Module docstring: "Wave 0 test scaffold for Phase 8 background music FFmpeg builder and AssembleStage music integration (EXT-02, EXT-03). Pure builder tests need no mock. Stage-level tests mock run_ffmpeg and probe_duration. Tests are RED until Wave 1 (RunConfig fields) + Wave 3 (ffmpeg.py + assemble.py) land."

Imports: from __future__ import annotations; from pathlib import Path; import pytest; from unittest.mock import patch, MagicMock

Class TestBuildMusicMixArgs (pure builder — no mock needed):
  - test_normalize_0: call build_music_mix_args("/v.mp4", "/m.mp3", "/o.mp4", music_volume=0.12, fade_in_s=2.0, fade_out_start=40.0, fade_out_s=3.0); assert "amix=inputs=2:normalize=0" in " ".join(args)
  - test_volume_before_amix: same call; check "volume=0.12" appears in args_str; check volume appears BEFORE amix in the filter_complex string
  - test_sidechaincompress_present: assert "sidechaincompress" in args_str
  - test_afade_present: assert "afade" in args_str
  - test_no_shell_true: assert isinstance(args, list); assert all(isinstance(a, str) for a in args)
  - test_video_stream_copied: assert "-c:v" in args; idx = args.index("-c:v"); assert args[idx+1] == "copy" (no video re-encode)
  - test_movflags_faststart: assert "+faststart" in " ".join(args)

Class TestAssembleMusicPath (mocked stage tests):
  - test_assemble_triggers_music_mix: build minimal AssembleStage test with config.bg_music_path set; patch run_ffmpeg and probe_duration; run AssembleStage; check build_music_mix_args was called (or run_ffmpeg called with args containing "amix")
  - test_single_loudnorm_with_music: same setup; count how many times run_ffmpeg is called with args containing "loudnorm"; assert == 1 (not 2 or 3)
  - test_loudnorm_without_music: config without bg_music_path; patch run_ffmpeg; run AssembleStage; assert loudnorm still runs at least once (existing behavior preserved)

All avideo imports deferred inside test bodies with # noqa: PLC0415.
  </action>

  <verify>
    <automated>cd /Users/jaimeberdejosanchez/projects/auto-video-narrado && uv run pytest tests/test_audio_enhance.py tests/test_ffmpeg_music.py --collect-only -q 2>&1 | grep -E "collected|error|SyntaxError" | head -5</automated>
  </verify>

  <acceptance_criteria>
    - Both files exist: `test -f tests/test_audio_enhance.py && test -f tests/test_ffmpeg_music.py`
    - normalize=0 test present: `grep -c "normalize=0" tests/test_ffmpeg_music.py` returns >= 1
    - afftdn filter string present: `grep -c "afftdn=nr=6:nf=-25" tests/test_audio_enhance.py` returns >= 1
    - loudnorm filter string present: `grep -c "loudnorm=I=-16:TP=-1.5:LRA=11" tests/test_audio_enhance.py` returns >= 1
    - sidechaincompress test present: `grep -c "sidechaincompress" tests/test_ffmpeg_music.py` returns >= 1
    - Single loudnorm test present: `grep -c "test_single_loudnorm_with_music" tests/test_ffmpeg_music.py` returns 1
    - No shell=True test: `grep -c "test_no_shell_true" tests/test_audio_enhance.py` returns >= 1
    - Deferred imports: `grep -c "noqa: PLC0415" tests/test_ffmpeg_music.py` returns >= 2
    - Full suite still green: `uv run pytest tests/ -x -q 2>&1 | tail -3` shows 303 passed (new test files add RED tests; existing 303 stay green — use `uv run pytest tests/ -q --ignore=tests/test_voice_openai.py --ignore=tests/test_audio_enhance.py --ignore=tests/test_ffmpeg_music.py 2>&1 | tail -3` to confirm baseline)
  </acceptance_criteria>

  <done>tests/test_audio_enhance.py and tests/test_ffmpeg_music.py exist, collect without syntax error, contain all required test methods for VOZ-03/EXT-02/EXT-03; existing 303 tests remain green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test files → avideo modules | Test files import avideo modules via deferred imports inside test bodies; no module-scope import prevents collection failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | test mock seams | accept | Wave 0 tests mock at the correct import path; if mock path is wrong the test passes trivially — caught by RED/GREEN discipline |
| T-08-00-02 | Denial of Service | 303 existing tests | mitigate | New test files use deferred imports; collection of new files cannot fail even if avideo.stages.voice_openai does not exist yet |
</threat_model>

<verification>
1. All three test files collect without SyntaxError: `uv run pytest tests/test_voice_openai.py tests/test_audio_enhance.py tests/test_ffmpeg_music.py --collect-only -q`
2. Existing 303 tests still pass: `uv run pytest tests/ -q --ignore=tests/test_voice_openai.py --ignore=tests/test_audio_enhance.py --ignore=tests/test_ffmpeg_music.py 2>&1 | tail -3`
3. New tests are RED (ImportError on missing modules): `uv run pytest tests/test_voice_openai.py -q 2>&1 | grep -E "FAILED|ERROR|error"`
</verification>

<success_criteria>
- 3 test files created with deferred imports and correct mock seams
- 303 existing tests remain green
- New test count: >= 7 (test_voice_openai) + 4 (test_audio_enhance) + 10 (test_ffmpeg_music) = 21 new tests
- New tests are RED until Wave 1–3 implementation lands
</success_criteria>

<output>
After completion, create `.planning/phases/08-backend-integrations/08-01-SUMMARY.md`
</output>
