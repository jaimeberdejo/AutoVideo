---
milestone: v2.0.0
audited: 2026-05-31
status: gaps_found
scores:
  requirements: 25/25 implemented (3 partial via integration blockers)
  phases: 6/6 complete (1 passed, 5 human_needed — visual checks deferred)
  integration: 5/8 E2E flows wired
  flows: 1/4 fully complete (3 broken by blockers below)
gaps:
  integration:
    - id: BLOCKER-1
      requirements: [UI-02, UI-04, VOZ-01]
      where: src/avideo/ui/pages/phase_4_voz.py (_render_synthesis)
      issue: "TTS path (ElevenLabs/OpenAI) launches VoiceStage via rerun_voice (marks .voice.done) but never runs AlignStage, so .align.done is never written. PHASE_COMPLETION_STAGE[4]='align' → wizard resets to Phase 4 on every browser refresh after approving voice (default path)."
      fix: "After voice_done in _render_synthesis, call run_stage(AlignStage(), workdir, config) (instant passthrough for TTS) before the gate returns True — mirrors the headless orchestrator."
    - id: BLOCKER-2
      requirements: [EXT-01, ASM-01, UI-02]
      where: src/avideo/ui/pages/phase_5_extras.py + state.py PHASE_COMPLETION_STAGE[5]='subs'
      issue: "Extras page is config-only and writes nothing to workdir. SubtitlesStage only runs in Phase 6 when burn_subs=True. With burn_subs=False (default), .subs.done is never written → workdir_phase_from_done_markers returns Phase 5 on refresh even after output.mp4 exists; cannot recover to Phase 6."
      fix: "Either always run SubtitlesStage in Phase 6 regardless of burn_subs (matches PIPELINE_STAGES in CLI), OR add an 'extras' stage marker to STAGE_ORDER + PHASE_COMPLETION_STAGE[5] and mark_done('extras') on Aprobar."
    - id: BLOCKER-3
      requirements: [VOZ-02]
      where: src/avideo/stages/align.py (lines 91-135)
      issue: "AlignStage.run() passthroughs only for config.voice==VoiceMode.elevenlabs; VoiceMode.openai falls through to the WhisperX forced-alignment branch on OpenAI's MP3s — ImportError if whisperx extra absent, or a redundant WhisperX pass over already-transcribed audio. Breaks the headless `avideo generate --voice openai`. VoiceOpenAIStage already produces UnifiedTimings(source='openai') with word timestamps."
      fix: "Add `if config.voice in (VoiceMode.elevenlabs, VoiceMode.openai): return voice_timings` at align.py line 91. Add a test for VoiceMode.openai in tests/test_align.py."
  warnings:
    - id: WARNING-1
      requirements: [CNT-01]
      where: phase_1_contenido.py:168 → storyboard.py:135
      issue: "context checkpoint written as BulletsInput but read as ContextOutput. Pydantic v2 (no extra=forbid) deserializes to ContextOutput defaults (text='', used=False) → functionally OK (Studio has no context doc) but stores the wrong type."
      fix: "Write ContextOutput(source_path=None, text='', used=False) instead of BulletsInput for the context checkpoint."
    - id: WARNING-2
      requirements: [CNT-01]
      where: phase_1_contenido.py:172
      issue: "run_config['topic'] set in session_state but RunConfig has no topic field (extra='ignore' drops it). Topic flows correctly via bullets.yaml→BulletsInput.title; the session key is dead weight."
      fix: "Drop run_config['topic'] or thread topic where actually needed."
tech_debt:
  - phase: 09-13 (UI)
    items:
      - "5 phases (9-13) have status human_needed — browser/visual verifications deferred (no code gaps; automated layers passed). Run `avideo studio` to confirm."
      - "Code review run only on Phase 8; phases 9-13 ui/ package not code-reviewed (verifier-confirmed + 397 tests)."
  - phase: nyquist
    items:
      - "VALIDATION.md exists only for Phase 8. Phases 9-13 have no VALIDATION.md (leaner autonomous cycle). Nyquist: 1 compliant / 5 missing."
nyquist:
  compliant_phases: [8]
  partial_phases: []
  missing_phases: [9, 10, 11, 12, 13]
  overall: partial
---

# Milestone v2.0.0 — Audit

**Verdict: GAPS FOUND.** All 25 requirements are implemented and the full test suite is green (397 passed), but cross-phase integration checking surfaced **3 blockers** that the isolated per-phase verifications could not catch. Two break the Studio wizard's browser-refresh resume on its DEFAULT paths; one is a real headless-CLI bug for `--voice openai`.

## Core Value Check

The v2.0.0 goal — a Streamlit guided wizard (6 phases, human gates) orchestrating the existing pipeline — is built and unit/integration-tested. However, the wizard's "resume from workdir on refresh" guarantee (UI-02/UI-04) is broken for the default TTS + default `burn_subs=False` paths until BLOCKER-1 and BLOCKER-2 are fixed.

## Phase Completion

| Phase | Verification | Status |
|-------|--------------|--------|
| 8 Backend Integrations | 08-VERIFICATION.md | passed (13/13) + code review (2 blockers+5 warnings fixed) |
| 9 UI Foundation | 09-VERIFICATION.md | human_needed (7/7 automated; browser deferred) |
| 10 Contenido | 10-VERIFICATION.md | human_needed (7/7 automated) |
| 11 Guion + Slides | 11-VERIFICATION.md | human_needed (5/5 automated) |
| 12 Voz | 12-VERIFICATION.md | human_needed (4/4 automated) |
| 13 Extras + Ensamblaje + Polish | 13-VERIFICATION.md | human_needed (5/5 automated) |

## Requirements Coverage

25/25 requirements mapped + implemented (REQUIREMENTS.md traceability all Complete; each phase VERIFICATION passed/human_needed). 3 are PARTIAL because integration blockers degrade them: UI-02/UI-04 (refresh-resume), VOZ-01/VOZ-02 (align), EXT-01 (subs marker). No orphaned or unimplemented requirements.

## Integration / E2E (the gap source)

5/8 flows wired. Broken: (1) Studio ElevenLabs TTS path, (2) Studio `burn_subs=False` path, (3) headless `--voice openai`. See the 3 blockers in frontmatter. All checkpoint/field names, invalidation chains, import paths, and data types are otherwise correctly connected (19 wirings verified).

## Test Suite

`uv run pytest -q` → **397 passed, 0 failed**. Note: no test covers `VoiceMode.openai` in AlignStage (BLOCKER-3 slipped through because of this coverage hole).

## Recommendation

Close the 3 blockers before completing the milestone — they are small, localized fixes (one passthrough line + a test in align.py; one AlignStage call in the voz page; one stage-marker decision in the extras page). The 2 warnings and the Nyquist/UI-code-review tech debt can be accepted into the backlog or addressed in a short cleanup. Then re-run `/gsd-audit-milestone` (or proceed to `/gsd-complete-milestone v2.0.0`).
